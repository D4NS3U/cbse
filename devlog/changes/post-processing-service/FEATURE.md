# FEATURE.md — PostProcessingService reference component and flow integration

**Program context:** first feature of the post-professionalization roadmap (the former charter's M1 "PostProcessingService integration"); authored per the manager's spec-contract, from the live alpha4 code (`api/alpha4/simulationexperiment_types.go`, `scenario-manager/internal/{subject,nats,persistence,lifecycle,effectivejob}`, `component-templates/translator/` as the component-template archetype, `docs/COMPONENT_DESIGN_GOALS.md`'s post-processing boundary).
**Normative for every worker dispatched under it. Read-only for workers.** On contradiction between this spec and live code that repository inspection cannot resolve: stop and ask via the ask channel.

## 1. Title and mission

This feature completes the scenario end-state: a **reference PostProcessingService (PPS)** plus its integration into the CBSE flow. When a scenario's simulation-runner fleet (one batch/v1 Job) completes and the SM has moved the scenario to `PostProcessing`, the SM publishes one **evaluation request** per scenario to the PPS (NATS/JetStream, namespace-aware subjects). The PPS reads the scenario's result rows from the experiment's Result DB, evaluates the scenario's KPI(s) against the **user-supplied accuracy target**, and answers with a verdict: **(a) accuracy met** — the scenario transitions to the new success-terminal `Finished` state (no further runners, results preserved); **(b) accuracy not met** — the PPS returns the **number of additional runners needed**, the scenario returns to `StartingRunners`, and a new runner round executes (a new round-identified Job with the PPS-provided repetition count), after which evaluation repeats. The loop repeats under a PPS-owned stop policy that the user configures with the PPS deployment. Products: the reference PPS component (`component-templates/post-processing-service/`, the "Basic Reference Post Processing Service with evaluation logic"), operator provisioning of the PPS Deployment (`spec.postProcessingService` already exists in the alpha4 API and is currently unprovisioned), SM scenario-machine and messaging extensions, and e2e smoke coverage of the full chain. Component ownership: the Operator provisions the PPS Deployment; the SM owns scenario state and messaging; the PPS owns result evaluation and the stop policy. Future work (explicitly out): experiment-level phase aggregation (`Finished`/`Failed` experiments — a separate roadmap feature), Helm-chart-era validation, and non-reference evaluation strategies (any custom PPS images remain free to implement the wire contract).

## 2. Scope

- **In scope:** (a) reference PPS Go module under `component-templates/post-processing-service/` implementing the evaluation wire contract, a read-only Result-DB client, the reference evaluation strategy (§3 D6), and the additional-runner estimator; (b) SM extensions — new `Finished` scenario state, evaluation-request publication pipeline (triggered on entering `PostProcessing`), evaluation-response handler mirroring the translator-ready path, per-round runner semantics (round-identified Jobs, per-round repetition counts, generalized computed-reps bookkeeping), and deletion-cleanup extension for PPS consumers; (c) NATS/JetStream surface — new subjects + stream + durable consumers in the existing `internal/subject`/`internal/nats` grammar; (d) Operator provisioning of the PPS Deployment from the existing `PostProcessingSpec` (image/port/command/args + NATS and Result-DB environment wiring, ownership and deletion semantics consistent with the Translator deployment); (e) harness/smoke integration — the reference PPS image joins the mandatory component set (image lock, build/preflight/self-test extensions), stack manifests gain the PPS deployment; (f) e2e smoke specs covering the met-after-one-round path and the additional-rounds loop.
- **Out of scope:** experiment-level Completed/Finished semantics changes beyond accepting the new scenario terminal (the existing experiment terminal action must keep working verbatim); any change to the runner result-row format or the Result DB schema; any change to the translator contract; CRD field additions (assessment in workflow decision D1 — the base design requires none); production HA/observability tooling.
- **Breaking changes / compatibility:** the scenario "state" vocabulary grows (`Finished`); `PostProcessing` remains a valid state but loses its de-facto terminal role (a deployed SM observing old flows is unaffected — no external consumer of scenario states exists besides the terminal sweep, which is updated with the feature). `scenario_status` gains additive, nullable columns (schema validation policy: no `ALTER` repair, incompatible → startup fails — the extension must be additive-null-safe). Existing runner Jobs keep their names; new Jobs for additional rounds carry a new round suffix (naming in D5). The smoke harness's hard-pinned 6-component invariant becomes a 7-component truth, updated in the same feature wave with the harness self-tests (extend, never weaken).
- **Compatibility boundaries:** Kubernetes >= 1.30; alpha4-only API (served and stored); no feature-gate changes performed by the software; image references remain immutable digests.

## 3. Workflow decisions (concern → decision)

| # | Concern | Decision for this feature |
|---|---|---|
| D1 | Where does the user's required accuracy come from? | **Per-scenario `confidence_metric`** — the existing `scenario_status` column (documented in CDG as "Optional numeric target for future post-processing", supplied by the EDS batch row, today inserted NULL). The SM forwards it in the evaluation request; the PPS interprets it as the required relative precision (D6). **No CRD change needed**: the existing `spec.postProcessingService` (`image`, `port`, `command`, `args`) configures the deployment; PPS-level policy defaults ride `args`. *(Open question Q1: an experiment-level fallback/override on the CRD can be added later as pure config surface — not part of this feature unless the user wants it now.)* |
| D2 | Messaging surface | Mirror the translator-flow shape: request subject `cbse.<namespace>.<project>.pps.request` (SM-publishes, JetStream stream `cbse_pps`, PPS consumes via a per-experiment durable consumer `pps-<12-char-UID-prefix>` — created and deleted with the experiment like the translator consumer); evaluation reply subject `cbse.<namespace>.<project>.pps.<scenario-id>.evaluation` (PPS-publishes, one stream, SM consumes via its anchored durable). Strict parsers in `internal/subject` (grammar length/domain rules) as for `trans.*`. All templates validated at startup like the others. |
| D3 | When does the SM learn about runner results? | The SM's existing observation scheduler is the trigger: fleet-Job completion already applies the guarded `InProcessing -> PostProcessing` transition. Entering `PostProcessing` now additionally enqueues the evaluation-request publication (exactly-once per scenario+round, guarded by new additive columns using the same attempt-guard pattern as translation publication). **The SM itself does not read the Result DB** — the PPS is the (read-only) Result-DB reader; the SM's request carries the identity context (experiment, scenario, round, `number_of_reps` so far, `confidence_metric`), and the PPS's evaluation response carries back the aggregate it observed (row count, achieved precision, verdict), which the SM may persist for observability. *(Open question Q2: whether the user additionally wants an SM-side row-count gate before publishing — this would add a Result-DB read path to the SM; base design intentionally avoids the coupling.)* |
| D4 | New scenario states and terminal behavior | Add the success-terminal `Finished`. Transitions: `PostProcessing -> Finished` (verdict met, guarded); `PostProcessing -> StartingRunners` (verdict not-met + additional-runner count, guarded, increments the round counter); rounds flow through the existing `StartingRunners -> InProcessing -> PostProcessing` path. `Finished` joins the terminal set for every consumer of terminality (terminal action sweep, completion/cleanup logic); the failure sweep keeps `PostProcessing` in the non-terminal list (an experiment terminal during evaluation fails its scenarios, as today). A PPS "stop unmet" verdict (D7 cap) fails the scenario through the existing guarded failure path from `PostProcessing`, preserving all results and the last evaluated metrics. |
| D5 | Additional rounds: identity, sizes, bookkeeping | New additive columns: a per-scenario monotone `runner_round` counter and a `round_reps` requested-count for the current round (round 1 = the original `number_of_reps`). Additional-round runner Jobs are named `simrun-<UID-prefix>-s<scenario-id>-a<attempt>-r<round>` (today's single-round name reads as `-r1` implicitly — **no renames of existing-job expectations**; new name component only appears from round 2 on, or the label is always present — slice decides against the jobadapter's name-parsing invariants and adds the parse rule). `effectivejob` gains round-suffix and per-round completion-count inputs; `jobadapter` gains the corresponding create/confirm/observe/parse semantics for round-scoped Jobs; the SM generically tracks "current round's Job" only. `number_of_computed_reps` bookkeeping generalizes: for round 1 the existing monotonic clamp applies; for later rounds the observation counts against the per-round target and a new total (e.g., `total_computed_reps`) accumulates — exact column set and invariants are the slice's normative output. |
| D6 | Reference evaluation strategy | KPI = the runner's emitted `mean_wait_time` (reference model contract). Target: `confidence_metric` interpreted as **required relative precision** of the KPI mean — the reference PPS checks that the 95%-CI relative half-width, `(t_{0.975,n-1} · s) / (sqrt(n) · |mean|)`, is `<= confidence_metric` (n = distinct result rows for the scenario, aggregated over all rounds). Additional runners estimate: the standard sequential fixed-width approximation `n_more = ceil(max(0, (t·s / (target·|mean|))² − n))` (documented as a heuristic normal-approximation estimate, computed from the pooled sample; clamped to a sane per-round cap). `s=0` degenerate (zero-variance deterministic results) counts as met. The strategy is the **strategy pattern's reference implementation**: the wire contract is the normative interface; custom PPS images may implement any evaluation they want (any strategy; any KPI class) as long as they answer the verdict contract. |
| D7 | Loop bound (user's "indefinitely?") | The stop policy is **PPS-owned and user-configured** (the user's request: the required accuracy is integrated in the PPS and supplied by the user): the reference PPS takes `MAX_EVALUATION_ROUNDS` (and `MAX_RUNNERS_PER_ROUND`) as deployment args; defaults are generous-but-bounded for safety (suggested: 50 rounds / 1000 reps; **default proposal — user confirmation requested Q4**). On cap: final verdict `stop-unmet`, the scenario fails through the guarded `PostProcessing -> Failed` path with the last metrics visible. The SM never self-limits; it applies verdicts. |
| D8 | Failure and retry taxonomy | Evaluation requests are JetStream-published like translation requests: publish only after a durable PubAck, exactly-once per scenario+round via guarded columns, at-least-once consumer redelivery on the PPS side, poison messages acknowledged-and-dropped per the existing permanent-poison rules. A persistent evaluation failure (PPS unavailable, Result DB unreachable) leaves the scenario in `PostProcessing` (the request consumer re-publishes on the fixed cadence; not an infinite hot loop). PPS evaluation responses handling mirrors translator-ready: identity-checked, gate-checked, guarded transition; poison ACKed with an eventlog record. |
| D9 | Security/identity | Same identity conventions as the translator flow: reserved labels on the PPS Deployment (experiment identity, UID via downward API), per-experiment consumer naming, registry-auth via the existing Secret plumbing; the reference PPS image is a digest-pinned image led by `spec.postProcessingService.image`; the PPS gets the Result DB endpoint and credentials exclusively via operator-provisioned environment (it never receives cluster credentials of its own); result reads use the per-experiment Result DB user. |
| D10 | Deletion/cleanup symmetry | Experiment deletion cleanup extends exactly like the translator's: delete the per-experiment PPS consumer, purge the evaluation subjects, fail/complete any outstanding scenario bookkeeping — owned by the existing deletion-cleanup action, extended in the SM slice; the Operator's deletion of the PPS Deployment rides the established ownership/deletion semantics of translator deployments. |

## 4. Design patterns (Gang of Four)

| # | Concern (from this feature's requirements) | Pattern | Concrete application (types, packages, boundaries) | Why the simpler composition is not enough |
|---|---|---|---|---|
| 1 | Scenario lifecycle extension with round-trip transitions (`PostProcessing -> Finished / StartingRunners`) | **State** | The explicit scenario state machine in `scenario-manager/internal/{lifecycle,persistence}` — guarded SQL transitions as the state-boundary enforcement, `Finished` as new terminal | The machine must stay the single vocabulary; two new edges with guards cannot be free-form flags without breaking terminal semantics |
| 2 | SM ⇄ PPS decoupled evaluation flow | **Observer** | JetStream publication of evaluation requests and verdicts over the `cbse_pps` stream (`internal/nats` adapters; PPS + SM as independent observers), mirroring the translator request/ready flow | Fire-and-forget calls would couple SM to PPS liveness; the stream gives durable at-least-once + redelivery |
| 3 | Evaluation logic interchangeable per PPS image (reference vs user-concrete) | **Strategy** | The wire contract (`scenario-manager/internal/communication` types + the reference implementation in `component-templates/post-processing-service/`) is the strategy interface; the reference CI-based evaluator ships one strategy | Evaluation is the PPS's defining variability; hard-wiring the reference math into the SM or the contract would forbid alternative evaluation policies |
| 4 | Result-DB access from the PPS without a driver leak | **Adapter** | `component-templates/post-processing-service/internal/resultdb` wraps pgx behind an internal read-only interface (same conventions as the translator's `detaildb`/`registryauth` adapters), enabling conformance tests against fakes | A direct pgx dependency in evaluation logic blocks table-driven tests and locks the DB shape |
| 5 | Runner-Job assembly gains round/repetition parameters | **Builder** | `scenario-manager/internal/effectivejob` — existing stepwise merge of template + reserved fields extended with round-suffix and per-round completion counts | Job assembly has grown beyond a constructor: template merge + identity + round identity validation must stay composable and testable |
| 6 | Evaluation responses applied to scenario bookkeeping with poison/stale/gate semantics | **Template Method** | New evaluation handler in `internal/nats`/`internal/ready`-style package reusing the established handler skeleton (validate → gate → guarded transition → ack/naq), with the verdict application as the single varying step | Two parallel hand-rolled handlers would drift; the skeleton already encodes the failure taxonomy and provability |

*Deliberately not used:* Mediator (the SM composition stays explicit pairwise wiring), Singleton, any speculative pattern without a requirement row.

## 5. Global constraints (per worker, verbatim discipline)

- **Obey `AGENTS.md`.** Root test contract and cluster-safety rules incorporated by reference: `make test-fast` is mandatory for every Go/CRD/Dockerfile/harness change; `make test-smoke` additionally for operator reconciliation, API/CRD, SM k8s/NATS/database integration, image, or manifest changes; harness owns its namespace; immutable digest images; no TLS/insecure-registry bypasses; nothing in `default`/`kube-system`.
- **Scope discipline.** Exactly the slice's ownership partition; no unrelated cleanup; discovered gaps stop-and-ask.
- **Test integrity.** Never weaken, delete, skip, or rewrite a test to conceal a failure; harness invariants that this feature legitimately outgrows (the 6-component set) are updated *with the feature*, never around it.
- **Secrets hygiene.** No credential material in commands, logs, specs, or artifacts; environment only.
- **Verification honesty.** `complete` / `incomplete` / `verification-blocked` vocabulary; every change maps to a numbered requirement; every requirement verifies in a named tier.
- **Runtime attestation.** Pi workers run `printf '%s/%s\n' "$PI_PROVIDER" "$PI_MODEL"` at the first checkpoint and repeat the verbatim output in their `worker_done` summary; expected line `ai.forge/qwen3.8-27b-nvfp4`; mismatch → stop, change nothing, report failed.
- **Normative spec.** This FEATURE.md + the assigned slice + `AGENTS.md` are read-only for workers; slice specs cite this file.
- **Idempotency across restarts.** Every new externally-visible effect (evaluation publication, round Jobs, verdict application, PPS consumer lifecycle) survives an SM/PPS restart exactly once per its guard.
- **No commits** (workers leave the tree reviewable; the user or the delegated manager commits).
- **Schema policy.** `scenario_status` extensions are additive and NULL-safe; no ALTER repair; incompatible → `ErrSchemaIncompatible` (startup fail) unchanged.

## 6. Slices (ordered; five-part task specs are authored per slice at its dispatch wave)

| Slice | Content | Dependency | Tier gate |
|---|---|---|---|
| **S1 — SM state machine & persistence** | `Finished` state; guarded `PostProcessing -> Finished / -> StartingRunners`; additive columns (`runner_round`, per-round reps, totals, evaluation publish guards); generalized computed-reps bookkeeping; `effectivejob` round-suffix + per-round counts; `jobadapter` round parse/create/observe; terminal-set updates; eventlog records | — | `make test-fast` |
| **S2 — SM messaging & wire contract** | `internal/subject` grammar (`pps.request`, `pps.<id>.evaluation`, wildcards, parsers, startup validation); `internal/nats` stream `cbse_pps`, consumers, per-experiment PPS consumer reconciliation+purge; `internal/communication` wire types (request/verdict payloads, validation domain constants); evaluation publication pipeline; evaluation-response handler (Template Method); deletion-cleanup extension | S1 | `make test-fast` (+ integration-tags against real NATS) |
| **S3 — Operator provisioning** | PPS Deployment from `PostProcessingSpec` (image/port/command/args; NATS URL/streams/subject templates, Result DB endpoint wiring, identity labels, downward-API identity); ownership/deletion semantics; RBAC unchanged unless needed; envtest coverage incl. the round-trip env facts | — | `make test-fast` + `make test-smoke` |
| **S4 — Reference PPS module** | `component-templates/post-processing-service/`: go.mod (repo module path convention), request consumer (durable `pps-<UID-prefix>` policy, AckExplicit), `resultdb` adapter (read-only, scenario-row aggregation), evaluation strategy (D6) + additional-rep estimator + round/rep caps (D7) from `args`-supplied config, verdict publisher, conformance suite against fake pact tests shared with SM's `communication` types, Dockerfile + module README (component doc, licensing header discipline) | — | `make test-fast` |
| **S5 — Harness & stack integration** | Reference PPS image in the mandatory component set (image lock, `build-images.sh`, preflight, self-test invariant 6→7, stack.yaml deployment + env); `CBSE_PPS_IMAGE` for SKIP_BUILD; smoke.sh plumbing | S1–S4 | `make test-fast` + `make test-smoke` |
| **S6 — E2E smoke specs** | Extend `test/e2e/smoke_test.go`: (a) scenario drives to `Finished` with a met verdict (asserting `Finished` in Core DB + `confidence_metric`/achievements observable, pps-consumer/subject lifecycle visible); (b) the additional-round loop path with a deterministic not-met-then-met setup (PPS arg-driven deterministic round policy for the test profile only — see Q5) | S5 | `make test-smoke` (real cluster, private K3s) |

Wave plan: W1 = [S1]; W2 = [S2 ∥ S3 ∥ S4] (ownership-disjoint); W3 = [S5]; W4 = [S6]. Slices spawn one worker each; the manager re-runs every acceptance recipe before settlement.

## 7. Verification gates

- **Per-tier rule** as in §5, plus: S2's NATS integration-tagged tests must compile and (in the smoke tier) round-trip; S4 ships the conformance suite mirroring the translator's coverage bar; S3/S5/S6 end with `make test-smoke` rc=0 on the private cluster (harness-owned namespace, immutable digests, existing prerequisites).
- **Feature-level acceptance (S6):** the smoke run demonstrates, on the cluster: EDS intake → … → runner Job completed → evaluation request visible on `cbse_pps` → PPS verdict → scenario `Finished` (and, for the loop spec, one additional round then `Finished`), with all artifacts in `artifacts/test/<run-id>/` and the previous five specs still green — the S07-A3 standard extended by the evaluation stage.
- **Forbidden shortcuts:** accepting prose-only evidence; loosening the schema policy; faking the PPS at smoke time; weakening the harness self-tests instead of extending them; skipping the attestation check.

## 8. Completion and handoff

State vocabulary per slice: `complete` / `incomplete` / `verification-blocked`, recorded with evidence (commands, rc's, receipts) in `IMPLEMENTATION_HANDOFF.md` after manager-verified settlement; every dispatch prompt embeds attestation + read order + no-commit + this file's global constraints; workers leave the tree reviewable. After S6, the handoff records the final smoke evidence and this feature's `devlog/changes/README.md` row turns `implemented`.

## Open questions for the user (block slice authoring where marked)

- **Q1** — accuracy-target source: per-scenario `confidence_metric` (EDS-supplied) is the base design; do you additionally want an experiment-level `spec.postProcessingService` evaluation default (new optional CRD fields) in this feature, or later?
- **Q2** — SM Result-DB awareness: base design keeps the SM Result-DB-free (PPS is the reader). Is that right, or do you want the SM to gate evaluation publication on a result-row count?
- **Q3** — `Finished` semantics: confirmed as scenario success-terminal; experiment-level `Finished/Failed` phases stay a separate future feature — correct?
- **Q4** — loop bounds: defaults for the reference PPS (`MAX_EVALUATION_ROUNDS=50`, `MAX_RUNNERS_PER_ROUND=1000`) — keep, adjust, or truly unbounded (not recommended)?
- **Q5** — determinism knob: the reference PPS accepts an evaluation-policy arg (e.g., `FORCE_ADDITIONAL_ROUNDS=1`: always demand one extra round then met on the second) used by the smoke smoke-test profile to make the loop deterministic — acceptable reference-component surface for tests/demo?
