# Dispatch prompt — E1: editorial micro-slice (reviewer-driven doc corrections and the CDG alpha4 refresh)

**Task title:** E1 — Editorial micro-slice: `COMPONENT_DESIGN_GOALS.md` alpha4 refresh, operator-README un-scaffolding, image-layout corrections, missing `scenario-manager/README.md`

**Read order (mandatory, normative for your run; all read-only):** 1) repository-root `AGENTS.md` — the test/cluster-safety contract for this public tree; 2) this task spec (it is your complete authority — there are no program specs in this repository); 3) the code and docs named below. On any contradiction between this spec and the repository state: stop and ask via your ask channel rather than resolving by opinion.

**Background you may rely on (from an external review at the current tip):** the repository documentation carries four defects this slice repairs: (i) `docs/COMPONENT_DESIGN_GOALS.md` still frames the extension contract as "the current `alpha3` contract" with alpha3 CR examples and a namespace-less subject scheme, while the live API is `alpha4`-only and the real subject grammar is namespace-aware; (ii) `experiment-operator/README.md` is an unedited kubebuilder scaffold (`TODO(user)` placeholders, "Kubernetes v1.11.3+", "docker 17.03+", "go v1.24.0+" claims) that also points at `config/samples/` which no longer exists; (iii) an image-layout contradiction — `docs/CBSE_TESTING_GUIDE.md` correctly documents that every component image is published in the **nested** layout (`${CBSE_REGISTRY}/<component>:<version>`; the flat `.test.` layout is retired and its return is rejected by an automated check), while `component-templates/translator/README.md` and `test/e2e/README.md` still describe the **flat** layout (`${CBSE_REGISTRY}:<component>.test.<version>`); (iv) `scenario-manager/` has no README at all.

## Target (your ownership — exactly these four paths)

1. `docs/COMPONENT_DESIGN_GOALS.md`
2. `experiment-operator/README.md`
3. `component-templates/translator/README.md` **and** `test/e2e/README.md` (layout-truth fixes only)
4. `scenario-manager/README.md` (new file you create)

Untouchable: everything else — in particular `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `SECURITY.md`, `AGENTS.md`, `docs/CBSE_TESTING_GUIDE.md` (it already documents the nested truth — verify, do not edit), all Go sources, generated artifacts, the harness, `.github/**`. If you encounter references to a development log, program, or relocation process inside your files, rewrite them into product-documentation language; do not hunt for them elsewhere.

## Change

1. **`docs/COMPONENT_DESIGN_GOALS.md` — alpha4 refresh.** This is the extension guide: a component author (custom EDS/Translator/PostProcessingService) reads it to implement against the live contract. Ground every claim in the code you read: the API surface from `experiment-operator/api/alpha4/simulationexperiment_types.go`; the wire payloads and acknowledgement semantics from `scenario-manager/internal/communication/`; the namespace-aware subject grammar from `scenario-manager/internal/subject/`; the reference Translator's contracts from `component-templates/translator/` (a component template you may cite, not edit). Replace every alpha3-as-current framing: the only served/stored API version is `alpha4` (alpha2/alpha3 retired); CR examples must use `experiment.cbse.terministic.de/v1alpha4`-correct `apiVersion` (read the types file for the exact group/version strings) and the actual alpha4 field names; the subject scheme documented must be the namespace-aware grammar the code implements. Historical mentions of retired versions are allowed only as explicit retirement statements (the document already contains one such notice — keep its wording class). **Preserve**: the cross-cutting design-goals section, the "Licensing your components" section (verbatim — it is normative contract), and the document's overall structure/purpose.
2. **`experiment-operator/README.md` — un-scaffold and write the real module README.** Document the operator's actual role and shape: the `SimulationExperiment` custom resource and its alpha4-only CRD (served + stored); the reconciler's responsibilities as the provisioning component (project databases and secrets, experiment descriptors/workloads, the Translator Deployment with its rootless BuildKit sidecar, the runner ServiceAccount; the deterministic `simrunner-<UID-prefix>` runner identity) — read the code instead of trusting this list where details matter (`internal/controller/**`, `internal/controller/alpha4`, `internal/dbendpoint`, `internal/jobtemplate`); development workflow (root `make test-fast` is the hermetic tier and includes the operator's envtest suite; per-module `go test ./...`; `make verify-generated` for CRD/RBAC/deepcopy regeneration). All scaffold claims and `config/samples/` references are removed. Every link resolves.
3. **Image-layout truth fixes (nested-only).** In `component-templates/translator/README.md`: replace the flat-layout claims (`${CBSE_REGISTRY}:translator.test.<VERSION>` class) with the nested form (`${CBSE_REGISTRY}/translator:<VERSION>` and the digest-pinned `<registry>/<component>@sha256:<hex>` references) consistent with `docs/CBSE_TESTING_GUIDE.md` and `test/e2e/images.lock.env`. In `test/e2e/README.md`: same correction to the smoke manifest/layout descriptions. **Discipline**: `test/e2e/README.md` contains documented private-infrastructure references owned by a later package — do NOT touch or "improve" any line other than the layout-contradiction content; the rest of that file stays byte-identical.
4. **`scenario-manager/README.md` — create.** Mirror the structure of the rewritten operator README: the Scenario Manager's role (experiment lifecycle ownership: EDS intake via JetStream, translation-request selection loop, translator-ready handling, runner-Job orchestration, observation, result persistence into per-scenario scenario-status tables; the post-processing boundary), a package map of `scenario-manager/internal/**` at the right granularity (one line per package; read the package doc comments), development workflow (root `make test-fast` incl. race suites; per-module `go test ./...`), and links to `docs/CBSE_TESTING_GUIDE.md` and the cluster requirements. No new claims that code reading cannot verify.

## Constraints

- Documentation-only slice: no Go, CRD, Dockerfile, manifest, or harness changes (`test/e2e/README.md` text fixes are documentation; nothing in `test/e2e/` runtime files may change).
- Test integrity: never weaken/delete/skip tests (you touch none); run `make test-fast` at your final checkpoint as the no-collateral proof (rc=0 expected; it must not regress).
- No private values anywhere in your additions: no registry names, no endpoints, no kubeconfig paths, no hostnames, no AI provider names; existing documented private references in `test/e2e/README.md` stay untouched (they belong to a later package).
- Truthful-docs discipline: anything you cannot verify from the repository's code or existing docs does not go in; if a claim in this spec disagrees with the code, the code wins — and note it in your report.
- No commits (changes stay in the working tree for review); no cluster operations; no kubectl/pushes/registry actions.
- P3: institution attribution must survive; you remove no license/copyright content.

## Observable acceptance (all echoed in your `worker_done`; the manager re-runs each independently)

1. Attestation first: `printf '%s/%s\n' "$PI_PROVIDER" "$PI_MODEL"` → must print `ai.forge/qwen3.8-27b-nvfp4` (verbatim repeat in your executive summary; mismatch → stop, change nothing, `--outcome failed`).
2. CDG refresh proof: `grep -n "alpha3" docs/COMPONENT_DESIGN_GOALS.md` output echoed — every remaining mention is an explicit retirement statement, zero "current alpha3 contract"-class framing; `grep -n "Licensing your components" docs/COMPONENT_DESIGN_GOALS.md` shows the section intact; the documented subject grammar matches the code (echo one example from `scenario-manager/internal/subject/` next to your documented grammar line).
3. Operator README proof: `grep -nE "TODO\(user\)|kubebuilder|v1\.11\.3|17\.03|config/samples" experiment-operator/README.md` → rc=1; the README names the alpha4 API, the envtest path, and `make verify-generated`.
4. Layout truth proof: `grep -nE "\.test\.\$?|:translator\.test\.|:runner-base\.test\.|:eds-mock\.test\." component-templates/translator/README.md test/e2e/README.md` → no flat-layout claims remain (echo output); nested form present in both; `test/e2e/README.md` diff shows changes ONLY in the layout-contradiction hunks (`git diff --stat` + hunk count echoed; every non-layout line byte-identical).
5. SM README proof: file exists; `grep -nE '192\.168\.|registry\.unibw|ai\.forge|/home/' scenario-manager/README.md` → rc=1; its package map lines correspond one-to-one with `ls scenario-manager/internal/`.
6. Containment: `git status --short` shows exactly ` M docs/COMPONENT_DESIGN_GOALS.md`, ` M experiment-operator/README.md`, ` M component-templates/translator/README.md`, ` M test/e2e/README.md`, `?? scenario-manager/README.md` — nothing else (the tree was clean at `9e3cddc` when you started).
7. R3 link check: every relative markdown link in your four files resolves (run the tree-wide recipe: README.md, CONTRIBUTING.md, CHANGELOG.md, SECURITY.md, AGENTS.md, docs/*.md, test/README.md, test/e2e/README.md, experiment-operator/README.md, scenario-manager/README.md, component-templates/*/README.md): zero `BROKEN:` lines.
8. `make test-fast` rc=0 recorded — echo the trailing rc line.
9. Completion protocol: `worker_done` with a three-sentence executive summary, both lifecycle IDs (task + dispatch), explicit `--outcome succeeded|failed`, the verbatim attestation line, and the evidence blocks above.
