# Slice A1 — OSS staples

Normative and self-contained for its worker. Read order and global constraints: [../FEATURE.md](../FEATURE.md) §5 (P1–P8 verbatim, operating rules, attestation requirement). This slice spec extends, never overrides, the umbrella. All three documents (umbrella, this slice, root `AGENTS.md`) are read-only for you.

## Target

Exactly: `README.md` (rewrite), `CONTRIBUTING.md` (create), `CHANGELOG.md` (create), `SECURITY.md` (create), `LICENSE` (verification only — never edit). Nothing else: no `docs/*`, no `AGENTS.md`, no module files, no `.github/**`, no `devlog/**` (including this spec), no `MANAGER.md`.

## Change

1. **`README.md` — product rewrite.** Re-orient from "the current repository" to the product: what CBSE is (Kubernetes-native simulation-experiment framework), why (publications table stays, P3), an architecture overview naming the components (`experiment-operator`, `scenario-manager`, `component-templates/translator`, `component-templates/scenario-detail-database`, `test/` harness), a **quickstart for source use** today (clone; Go toolchain ≥ 1.26 era per `go.work`; `make test-fast`; `make help`), with a clearly-labeled forward reference to the future Helm chart (Package B) — state plainly that installation-by-chart is future work and today's path is source. Keep the "Research Prototype – Early Development Stage" framing and the interfaces-may-change caveat. Keep and update (truthfully) the testing section: `make test-fast` publicly runnable; `make test-smoke` requires a user-provided cluster + registry via environment (link `docs/CBSE_TESTING_GUIDE.md`); the cluster-tier environment variables (`KUBECONFIG`, `CBSE_REGISTRY`, `CBSE_REGISTRY_AUTH_FILE`) are **environment-provided — no repository default** (already the A3 target state; your README must not invent any registry value or kubeconfig path; placeholders like `<your-registry>` where a value is needed). Keep the Publications and License sections intact. Add a CI badge template pointing at the repo's public `verify.yml` workflow (the canonical workflow file name `verify.yml` is its content source; use a placeholder GitHub org/repo in the badge URL consistent with a public repo — do not link a private runner). All relative links must resolve (R3 recipe below).
2. **`CONTRIBUTING.md` (new).** Module-tier participation rules: prerequisites (public Go toolchain only), how to get and verify generated artifacts (`make verify-generated`), the test contract in developer language (module tier `make test-fast` mandatory for any Go/CRD/Dockerfile/harness change; cluster tier exists but is environment-gated and requires user-provided cluster/registry context; documentation-only changes need no test tier), development setup (`go.work`, per-module `go test`), where the extension points live (`component-templates/` — link `docs/COMPONENT_DESIGN_GOALS.md` and `docs/CLUSTER_REQUIREMENTS.md`), a short PR/disclaimer process section (GitHub flow, small focused changes), and a pointer to `AGENTS.md` for coding-agent etiquette. No private infrastructure assumptions, no private endpoints, no fabricated maintainer contact — where a maintainer contact would naturally live, state that the disclosure/collaboration contact is being finalized by the maintainers and reference `SECURITY.md`'s disclosure channel. Contribution guidance must remain truthful for the very next runs (charter R2 discipline applies to any contract-adjacent text).
3. **`CHANGELOG.md` (new).** Keep-a-Changelog format. Seed from `git log` (143 commits, single tag `v0.1-jos-paper`) at **feature granularity** — don't enumerate every commit; use the devlog index (`devlog/changes/README.md`) as the milestone map. Sections: `## [Unreleased]` capturing the alpha4 product state (alpha2/alpha3 retirement + alpha4-only API; reference Translator with rootless BuildKit runner-image generation; smoke-verified end-to-end chain incl. result persistence; docs refresh), plus a historical section summarizing the alpha2 → alpha3 evolution and the `v0.1-jos-paper` tag as the paper-linked anchor. Keep-a-Changelog semantics (Added/Changed/Deprecated/Removed subsections) must remain immediately extensible by the release process (Package B4 owns it formally later).
4. **`SECURITY.md` (new).** Structure: supported-versions statement (research milestone; supported = current `main` until a semver cadence exists — D6 is the user's future decision, so phrase it as "until a support policy is adopted"), a **private disclosure** section whose contact is an explicit, clearly-marked placeholder pending a maintainer decision (do not invent an email address — mark it as the known open user decision), reporting scope (this repository's code and docs; GitHub security advisories as the default channel), and a note that the cluster tier is user-operated infrastructure (their cluster, their registry).
5. **`LICENSE` — verify, do not edit.** Verify it is the explicit Apache License 2.0 text (marker lines: `Apache License`, `Version 2.0, January 2004`, the `APPENDIX: How to apply the Apache License` block; line count ≈ 201). Scan all repository `*.go` and generated files for the `Apache License` header; compile a findings list (any file lacking it, any LICENSE text anomaly, any institution attribution inconsistency). Deliver the findings **in your `worker_done` report**; edit nothing here. Any deviation the report reveals belongs to the user (P3) as an open decision. If the LICENSE is clean, report "clean".

## Constraints

- P1/P3/P6/P7 bind hardest here: never weaken tests; attribution survives; no commits; product-first.
- **No private values anywhere** in your files: no `registry.unibw.de`, no `i31bdase`, no `192.168.*`, no `/home/*/.kube*`, no self-hosted runner references, no `ai.forge` (P5/P3 distinction verbatim is in the umbrella §5).
- **No fabricated contacts/URLs**: every link either resolves in-repo or shows a documented placeholder.
- Read-only: this spec, the umbrella, `AGENTS.md`, `docs/**` (you may link them, not change them), everything outside your Target. Discovered gaps (e.g., a broken `docs/*` link you don't own) go into your report, not your edits.
- No cluster operations; no network operations beyond none at all — this slice requires no network.

## Ownership

Sole owner of the five paths in Target. The parallel worker owns A2's partition (api trees + one `docs/COMPONENT_DESIGN_GOALS.md` line); neither of you may touch the other's files. `docs/COMPONENT_DESIGN_GOALS.md` is **not** yours — you may change zero lines there.

## Observable acceptance

Run and echo all of these in `worker_done` (manager re-runs each one independently from a fresh shell):

1. Attestation first: `printf '%s/%s\n' "$PI_PROVIDER" "$PI_MODEL"` → must print `ai.forge/qwen3.8-27b-nvfp4` (verbatim repeat in your executive summary; on mismatch — stop, change nothing, `--outcome failed`).
2. Containment: `git status --short` shows only `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `SECURITY.md` — and, co-resident but not yours, A2's in-flight tree if it happens to be running (your proof is: **zero** entries outside your four files that are not ` D`-deletions under `experiment-operator/api/alpha[23]/` or single-line changes inside `docs/COMPONENT_DESIGN_GOALS.md`).
3. Private-string scan on your files: `LC_ALL=C grep -rnE '192\.168\.|registry\.unibw\.de|i31bdase|cbse-k3s|/home/[^ )`]*\.kube|ai\.forge' -- README.md CONTRIBUTING.md CHANGELOG.md SECURITY.md` → rc=1, no output.
4. Link check (umbrella §7 recipe with `CONTRIBUTING.md`, `CHANGELOG.md`, `SECURITY.md`, `README.md` in scope) → no `BROKEN:` lines.
5. Content spot-proofs (echo the greps): READ ME contains a quickstart and an architecture section (`grep -n "Quickstart\|Architecture" README.md` style check), no `trans-mock` (`grep -n "trans-mock" README.md` → rc=1), no claim that alpha3-or-earlier is the current API (`grep -n "alpha3" README.md` finds nothing, or only deliberate historical wording — if uncertain, verify against `devlog/changes/README.md` index); `CHANGELOG.md` contains `Keep a Changelog` and `[Unreleased]`; `SECURITY.md` contains a clearly-marked placeholder disclosure contact.
6. `make test-fast` rc=0 recorded in your final checkpoint (mandatory evidence; you made no Go/CRD changes — this is the charter-required no-collateral proof that the repo-wide build/remedy tiers are green in the tree you leave behind). Echo the trailing `rc=0` line of your run.
7. README file links to the public CI canonical file name: state it as a designated forward-reference for Package B; no chart installation claims (`grep -n "helm install" README.md` may only appear inside a clearly forward-looking/chart-era section — institution-neutral language).

Completion protocol: `worker_done` with a three-sentence executive summary, both lifecycle IDs (task + dispatch), explicit `--outcome succeeded|failed`, the verbatim attestation line, the LICENSE/header findings, and the six evidence blocks above.
