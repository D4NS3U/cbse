# Slice A4 — cbse-labs preparation (manifest-only, new files)

Normative and self-contained for its worker. Read order and global constraints: [../FEATURE.md](../FEATURE.md) §5 (P1–P8 verbatim, operating rules, attestation requirement). This slice spec extends, never overrides, the umbrella. All three documents (umbrella, this slice, root `AGENTS.md`) are read-only for you — **including the slice of `AGENTS.md` you may observe being rewritten in parallel (A3): read whatever is on disk when you start, and do not edit it.**

## Target

Exactly: **new files you create** under `devlog/changes/repo-professionalization/labs/` (a directory tree that does not exist yet — you create it). You may **read** (never edit) `/Users/.../`… in-repo sources to build the inventory: `MANAGER.md`, `agents/*`, `devlog/**`, `docs/project-status.md`, `.github/workflows/cluster-smoke.yml`, the charter (`../PROBLEM.md`), and `../prompts/00-manager-package-a.md`. **You must not edit `PROBLEM.md`, anything under `../prompts/`, any spec under `devlog/changes/public-repo-hygiene/`, or anything anywhere else.** You execute **no** moves, deletions, or repository mutations outside your new `labs/` files (R1: relocation is user-executed at D2, P6).

## Change

Create exactly these four deliverables (byte-exact names, so the runbook can cite them):

1. **`labs/inventory.md`** — the cbse-labs content inventory: every item destined for the private repo, itemized with source path → proposed `cbse-labs/` target path and a one-line rationale, covering at minimum (charter §6/D2): `MANAGER.md`, `agents/CODEDOCUMENTATION_AGENT.md`, `agents/ORCHESTRATION_NOTES.md`, the `devlog/**` tree (program governance including the charter itself — note in prose that `devlog/changes/public-repo-hygiene/` and `repo-professionalization/` move wholesale with the rest, since they are process history), `docs/project-status.md`, and a note on `artifacts/` (git-ignored output; local retention is the user's call, not a git move). Mark clearly at top: *manifest only — no relocations executed by this slice; the user performs the moves at D2*.
2. **`labs/move-list.md`** — the executable relocation runbook for the user at D2: ordered `git mv`-style steps (source → target), the suggested cbse-labs directory skeleton from the charter §5 (k3s dev-box manifests & feature-gate config; Harbor/registry conventions; env-value provider; self-hosted runner definition + relocated cluster-smoke workflow copy; devlog history; MANAGER/agents docs; program governance; internal paper/handoff material), what explicitly **stays** public (`README.md`, `LICENSE`, the module trees, `docs/CLUSTER_REQUIREMENTS.md`, `docs/CBSE_TESTING_GUIDE.md`, `docs/COMPONENT_DESIGN_GOALS.md`, `.github/workflows/verify.yml`, `CONTRIBUTING.md`, `CHANGELOG.md`, `SECURITY.md`, `AGENTS.md`, `Makefile`, `test/**`, `component-templates/**`), and the post-move manager note (R1's final clause: after relocation the manager and workers work from the cbse-labs copies; include a one-paragraph orientation for them).
3. **`labs/env-provider.template.sh`** — a template (non-executable marker: comments first, `set -eu` off… make it safe to copy-and-edit) that shows how a private environment provider supplies the smoke-tier variables: `CBSE_REGISTRY`, `CBSE_REGISTRY_AUTH_FILE`, `KUBECONFIG` — all as clearly-marked placeholders (`<your-registry>`, `<path-to-docker-config>`, `<path-to-kubeconfig>`) with documentation comments referencing `docs/CBSE_TESTING_GUIDE.md`. **Zero private values** (P5: the scan below must not find `registry.unibw.de`, `i31bdase`, any `192.168.*`, any host path). Header comment: *template only — fill values inside cbse-labs, never in this repository*.
4. **`labs/cluster-smoke-relocation.md`** — the `cluster-smoke.yml` relocation runbook (D3 = relocate): stage already done by you (record the source file's `shasum` and byte count — 63 lines / 1839 bytes at decomposition time; recompute at your start and record the fresh pair), the copy now exists at `labs/ci/cluster-smoke.yml` (a fifth new file you also create now: **the byte-identical preserved copy**, `cp `.github/workflows/cluster-smoke.yml` `labs/ci/cluster-smoke.yml``, verify `cmp` clean and record both `shasum` values), then the user-steps at D2: create the private repo, move the copy into its `.github/workflows/`, register the self-hosted runner (label reference as it appears in the file), and only then confirm preservation so the follow-up dispatch deletes the public file. Include the explicit condition: *the public `.github/workflows/cluster-smoke.yml` is NOT deleted by Package A until the user confirms this preservation*.

The relocation runbook and inventory carry private-bound strings **by design** (they quote the private workflow, endpoints may be described): this is D2-bound content — the charter's fresh-clone grep-clean item only completes after D2 moves `devlog/**` wholesale. Keep every other labs file placeholder-only.

## Constraints

- **R1 verbatim:** "`MANAGER.md` and this charter move to cbse-labs at D2; while any part of the program runs, those documents remain in this repository unmodified. The Manager must not dispatch a slice that deletes/moves `MANAGER.md` or `devlog/` (including this charter) — relocation is user-executed (P6)." You author manifests; you move nothing.
- **P5:** no credential material anywhere; the env-provider is a template; never echo `CBSE_REGISTRY_AUTH_FILE` contents; the preserved workflow copy is a tracked-public file's bytes — it contains no secrets (it is a workflow definition), so a byte-identical copy is legal and required for relocation.
- **R3:** your markdown links resolve (relative paths from `labs/` — `../../PROBLEM.md`, `../../README.md` under `devlog/changes/repo-professionalization/`, `../../../docs/CBSE_TESTING_GUIDE.md`, etc.).
- **P6:** no commits, no pushes, no repository creation, no external actions.
- **Read-only for you:** the charter, the program prompt (`../prompts/00-manager-package-a.md`), this package's FEATURE/slices/handoff, `AGENTS.md`, everything else on disk. Your new `labs/ci/cluster-smoke.yml` must remain a byte-identical copy — content-editing it (commenting out lines, "improvements") is forbidden; it is preserved evidence for a user-move.
- A3 (parallel) may be editing `Makefile`/`AGENTS.md`/`docs/CBSE_TESTING_GUIDE.md` while you work; co-residency in `git status` is expected and never yours to clean.

## Ownership

Sole owner of the new `devlog/changes/repo-professionalization/labs/**` tree (all files inside are yours and new). Nothing else — exclusive of every other path in the repository.

## Observable acceptance

Run and echo all of these in `worker_done` (the manager re-runs each independently from a fresh shell):

1. Attestation first: `printf '%s/%s\n' "$PI_PROVIDER" "$PI_MODEL"` → must print `ai.forge/qwen3.8-27b-nvfp4` (verbatim repeat in your executive summary; mismatch → stop, change nothing, `--outcome failed`).
2. Containment: `git status --short` shows **only** untracked `??` entries under `devlog/changes/repo-professionalization/labs/` attributable to you (co-resident A3 modifications to `Makefile`/`AGENTS.md`/`docs/CBSE_TESTING_GUIDE.md` are expected, not yours, and must not appear as your edits — your files are exclusively inside `labs/`).
3. Deliverable proof: `find devlog/changes/repo-professionalization/labs -type f` echoes exactly: `inventory.md`, `move-list.md`, `env-provider.template.sh`, `cluster-smoke-relocation.md`, `ci/cluster-smoke.yml`.
4. Preservation proof: `cmp .github/workflows/cluster-smoke.yml devlog/changes/repo-professionalization/labs/ci/cluster-smoke.yml` → clean; both `shasum` values echoed and equal; the public file untouched (`git status --short -- .github` → empty).
5. Template hygiene: `LC_ALL=C grep -rnE 'registry\.unibw\.de|i31bdase|192\.168\.|/home/[^ )`]*\.kube' -- devlog/changes/repo-professionalization/labs/env-provider.template.sh devlog/changes/repo-professionalization/labs/inventory.md devlog/changes/repo-professionalization/labs/move-list.md` → rc=1, no output (expected hits are legal **only** inside `labs/ci/cluster-smoke.yml` and — where quoting is unavoidable — `labs/cluster-smoke-relocation.md`'s runbook citations; if the runbook needs the endpoint, reference it as it appears in the workflow file and no further).
6. R3 link check over the full tree (umbrella §7 recipe additionally covering `devlog/changes/repo-professionalization/labs/*.md` — relative-path test) → no `BROKEN:` lines introduced by your files.
7. R1 integrity: `git status --short -- MANAGER.md devlog/changes/repo-professionalization/PROBLEM.md devlog/changes/repo-professionalization/prompts` → empty (nothing you did touched them; your `labs/` additions are the only additions under the charter directory).
8. `make test-fast` rc=0 recorded (charter §8 no-collateral proof for docs-only slices; you changed no Go/harness file). Note in your summary whether the on-disk `AGENTS.md` you worked under was pre- or post-A3-rewrite at your start — the manager uses this for the R2 observation record. Echo the `rc=0` receipt line.

Completion protocol: `worker_done` with a three-sentence executive summary, both lifecycle IDs (task + dispatch), explicit `--outcome succeeded|failed`, the verbatim attestation line, and the eight evidence blocks above.
