# Slice A3 — Private-infrastructure scrub (tracked defaults and agent-facing prose)

Normative and self-contained for its worker. Read order and global constraints: [../FEATURE.md](../FEATURE.md) §5 (P1–P8 verbatim, operating rules, attestation requirement). This slice spec extends, never overrides, the umbrella. All three documents (umbrella, this slice, `AGENTS.md` — the current text you will replace, which is normative for you until your own wave lands) are read-only to you: **including `AGENTS.md`; you produce a rewritten version through your disjoint-worktree change, but treat the existing text as binding while you work.**

## Target

Exactly: `Makefile` (registry-default removal + documented required variable), `AGENTS.md` (public-tone rewrite), `docs/CBSE_TESTING_GUIDE.md` (re-anchored, public tone). **Blocked sub-step you must NOT perform:** deleting `.github/workflows/cluster-smoke.yml` — it stays on disk, untouched, byte-identical, until the user confirms A4's preservation and a follow-up dispatch removes it. Everything else in the repository is untouchable, with one hard carry-over: **`test/harness/**` — scripts, flags, lease/lock logic, everything — is out of bounds (charter R4: the harness dies at C1, not now). You change defaults, docs, and agent-facing prose only.**

## Change

1. **`Makefile`.** Remove the private default only: `CBSE_REGISTRY ?= registry.unibw.de/i31bdase/cbse-test` (line 11). Replace the variable block with a documented required variable — `CBSE_REGISTRY` is environment-provided, never defaulted here — and add a fast, precise guard inside the recipes of the targets that consume it (`publish-test-images`, `test-smoke`, `test-e2e-retained`): `@test -n "$(CBSE_REGISTRY)" || { echo "CBSE_REGISTRY is required (environment-provided; see docs/CBSE_TESTING_GUIDE.md)" >&2; exit 2; }`. Do **not** guard inside `test-fast`, `verify-generated`, `help`, or any file/target that never needed it — **`make test-fast` must stay runnable with zero private values or guards firing** (your own evidence run proves it). Keep every other Makefile byte intact; the help echo lines that print `$(CBSE_REGISTRY)`'s value today (`make test-smoke KUBECONFIG=/path/to/config CBSE_REGISTRY=$(CBSE_REGISTRY)`) must stop exposing a value by switching to a placeholder form (`CBSE_REGISTRY=<your-registry>`), so `make help` stays truthful with the variable unset.
2. **`AGENTS.md`** — public-tone rewrite, **atomic single wave (R2)**: this document is normative for every worker in the middle of the program; your rewrite must land in a state that is simultaneously true for the very next runs. REQUIRED SEMANTIC PRESERVATION (verbatim-equivalent meaning, public wording — same commands, same tier names):
   - The test tier contract: `make test-fast` mandatory after every Go, CRD, Dockerfile, or test-harness change; `make test-smoke` additionally after changes to the operator reconciliation path, API/CRD, Scenario Manager Kubernetes/NATS/database integration, container images, or Kubernetes manifests; documentation-only changes require no cluster smoke.
   - The cluster-safety rules, genericized (no private endpoint, no private kubeconfig path, no private namespace names): set `KUBECONFIG` explicitly, never into `default` or `kube-system`; the smoke harness identifies the cluster and context it expects before mutating anything; immutable image digests; never insecure-registry or TLS-verification bypasses; the harness owns its isolated `cbse-e2e-<run-id>`-style namespace lifecycle (retention flags only for active debugging, then cleanup); do not delete resources the harness does not create (shared CRDs and helper namespaces of the operating cluster).
   - The commands block: `make test-fast` unchanged; the `make test-smoke` example switches to placeholder values — `KUBECONFIG=/path/to/kubeconfig`, `TEST_IMAGE_VERSION=26.7.16` (keep: it is a public example version string), `CBSE_REGISTRY=<your-registry>`, `CBSE_REGISTRY_AUTH_FILE=<protected-docker-config>`; add one line noting `CBSE_REGISTRY` is required and environment-provided.
   - The diagnostics line: keep, unchanged (`artifacts/test/<run-id>/`, never committed — already private-value-free).
   - P3 verbatim inside the rewrite (state the attribution/infrastructure distinction so the next scrub reader knows the rule): institution attribution in LICENSE/headers/papers stays; operational infrastructure (IPs, registry names, runner labels, kubeconfig paths, hostnames) must not appear in working contracts.
   - Post-rewrite proof of tone: the private-string scan (below) exits clean on `AGENTS.md`.
3. **`docs/CBSE_TESTING_GUIDE.md`** — re-anchor to the same world truth: the module tier is fully public (Go toolchain only, `make test-fast`, `make verify-generated`); the cluster tier exists and is complete but is **environment-gated on user-provided infrastructure** — a conformant cluster (link `CLUSTER_REQUIREMENTS.md`), a registry via environment (`CBSE_REGISTRY` required, `CBSE_REGISTRY_AUTH_FILE` path, `KUBECONFIG` explicit), the harness's namespace lifecycle and cleanup; no private endpoint/path/registry anywhere (placeholders only); documentation-cluster smoke examples use placeholders structurally identical to the rewritten `AGENTS.md` block. All relative links resolve (`../Makefile`, `../AGENTS.md`, `CLUSTER_REQUIREMENTS.md`, `COMPONENT_DESIGN_GOALS.md` — the R3 list). Keep it forward-truthful: everything you write must be true the moment your wave lands, for the next worker reads it as `AGENTS.md`'s companion.

## Constraints

- **R2 atomicity rule (verbatim):** "`AGENTS.md` is normative for every worker while it works; the slice that rewrites it must land it in a state that is simultaneously true for subsequent runs, and the update is recorded in the slice handoff." Your handoff-feeding `worker_done` summary must therefore state the new contract in one sentence.
- **R4 rule (verbatim):** "`test-harness.sh` self-tests inside `test-fast` currently exercise private-cluster scripts; A3 must not remove scripts (they die at C1) — only config/docs/defaults. The sequence keeps `test-fast` green throughout M0/M1." You touch no harness file; `test-fast` stays green — proven twice (post-guard `make test-fast` rc=0 below).
- **R3 link list you must leave intact** (checked by recipe): `README.md → docs/*` (A1's README — do not edit it; it already links your files); `docs/COMPONENT_DESIGN_GOALS.md → ../AGENTS.md`, `CBSE_TESTING_GUIDE.md`; `docs/CBSE_TESTING_GUIDE.md → ../Makefile`, `../AGENTS.md`; `devlog/changes/README.md → ../../MANAGER.md`, `../../AGENTS.md`.
- **P3/P5:** no private values in anything you write; placeholders only; never echo `CBSE_REGISTRY_AUTH_FILE` contents anywhere; LICENSE/headers untouched.
- **P4/P6:** no commits; your `AGENTS.md` rewrite stays truthful for the next runs; `MANAGER.md` is out of bounds.
- Blocked item restated: `.github/workflows/cluster-smoke.yml` stays byte-identical (`shasum` it before and after your work to prove it — see acceptance).
- A4 (parallel, new-files-only under `devlog/changes/repo-professionalization/labs/`) may be running while you work; git-status co-residency with brand-new `??` paths under that directory is expected and not a containment violation of yours.

## Ownership

Sole owner of `Makefile`, `AGENTS.md`, `docs/CBSE_TESTING_GUIDE.md` in this wave. The parallel worker owns only new files under `labs/`. You may not touch anything else — in particular not `README.md`/`CONTRIBUTING.md` (A1, landed), not `api/*` (A2, landed), not the harness, not `MANAGER.md`, not the charter.

## Observable acceptance

Run and echo all of these in `worker_done` (the manager re-runs each independently from a fresh shell):

1. Attestation first: `printf '%s/%s\n' "$PI_PROVIDER" "$PI_MODEL"` → must print `ai.forge/qwen3.8-27b-nvfp4` (verbatim repeat in your executive summary; mismatch → stop, change nothing, `--outcome failed`).
2. Containment: `git status --short` shows only your three files modified (plus expected co-residency: A1/A2's landed-coworktree entries already committed by the user or pending; A4's `??` new files under `devlog/changes/repo-professionalization/labs/`). Zero entries outside your partition attributable to you.
3. Private-string scan **scoped to your three files**: `LC_ALL=C grep -rnE '192\.168\.|registry\.unibw\.de|i31bdase|cbse-k3s|/home/[^ )`]*\.kube|ai\.forge' -- Makefile AGENTS.md docs/CBSE_TESTING_GUIDE.md` → rc=1, no output.
4. Guard proof: `CBSE_REGISTRY= make -n publish-test-images >/dev/null` shows the guard fires (echo the exit output of `sh -c 'CBSE_REGISTRY= make publish-test-images'` reaching exit 2 is NOT required — a dry-run `make -n` of the guard line plus quoting it suffices; do not actually publish anything). And with a value set, `make -n publish-test-images` does not block on the guard.
5. Harness/R4 integrity: `git status --short -- test/harness .github` (empty except nothing; `.github/workflows/cluster-smoke.yml` unchanged, `verify.yml` unchanged) plus `shasum .github/workflows/cluster-smoke.yml` before/after equality (echo the pair).
6. **`make test-fast` rc=0 recorded** (mandatory: Makefile = build-contract change; `AGENTS.md` governs it). Echo the run's final `rc=0` receipt line.
7. R3 link check (umbrella §7 recipe) over the full tree → no `BROKEN:` lines.
8. AGENTS semantic-preservation proof: your report includes one sentence stating the landed contract, plus grep echoes that the tier names (`test-fast`, `test-smoke`), both guarded targets' names, and the diagnostics path `artifacts/test/<run-id>/` survive in the rewrite.

Completion protocol: `worker_done` with a three-sentence executive summary, both lifecycle IDs (task + dispatch), explicit `--outcome succeeded|failed`, the verbatim attestation line, and the eight evidence blocks above.
