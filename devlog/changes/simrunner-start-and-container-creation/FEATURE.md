# Specification for Alpha4 Simulation Runner Startup and Translator On-Demand Image Creation

This document has the following structure:

1. Mission Statement -- explains the intent and attitude of the work.
2. Scope -- explains the boundaries, goals, and explicitly chosen workflow.
3. Change Location -- identifies the code, API, and manifest areas expected to change.
4. Logic Description -- describes the implementation behavior and ownership boundaries.

## Mission Statement

This branch turns the Scenario Manager (SM) from a component that only advances a scenario to `InProcessing` into the component that starts and observes the scenario's Simulation Runner workload on Kubernetes.

Translator remains an always-on, per-experiment service, provisioned and managed by the Experiment Operator. This branch does not replace it with a Job or change its NATS/JetStream request-consumer model. “On-demand container creation” means that Translator creates the executable simulation model and builds its Simulation Runner image only after it receives a request for a claimed scenario. It then returns the immutable runner image reference through the existing ready-message workflow, from which SM starts the runner on demand.

CBSE cannot provide one universal production Translator because translating a scenario into an executable model is user-defined. Instead, this branch provides a production-quality reference Translator template in `component-templates/translator/`. The template owns the stable NATS/JetStream, BuildKit, registry, digest, acknowledgement, and extension contracts. Its included SimPy generator is an intentionally small example and replacement point, not a production simulation model.

The implementation must make Kubernetes runner Jobs a projection of durable scenario state, not a second source of truth. Database state and guarded transitions decide ownership; workloads perform external work. The coding agent should favor small, explicit, idempotent behavior that is safe across SM restarts and retries while preserving the existing Translator handoff boundary.

The coding agent should favor clarity over cleverness: write small, explicit, modular code that follows nearby patterns. Each component should have one obvious responsibility, explicit dependencies, descriptive errors, and no hidden package-global state. Understand the existing lifecycle, ownership boundaries, and tests first; reuse suitable abstractions, and introduce new ones only to clarify a boundary or enable independent testing.

Work incrementally and stay within scope. Preserve the Translator request and ready-message contracts, durable Scenario Manager state machine, and Kubernetes ownership patterns unless this specification changes them. Avoid redesigns, speculative compatibility layers, and unrelated cleanup. Make retries, restarts, and `AlreadyExists` results deliberate and inspectable: confirm the exact expected resource or return a useful conflict; never silently adopt it. Validate image references, Secret types, namespaces, ownership, and lifecycle state at boundaries. Do not weaken security implicitly, hide privileged requirements, or treat a created Job or pushed image as proof of a successful simulation.

Write clear documentation alongside the code. Explain the lifecycle, ownership, configuration, security and prototype assumptions, operational diagnosis, and deferred hardening. Tests must cover normal behavior, the exact validation groups defined below, stale claims, cancellation, identity collisions, repeated reconciliation, partial external success, and cleanup.

## Scope

This change introduces the only supported `SimulationExperiment` API version: `experiment.cbse.terministic.de/alpha4`. It defines the Translator image-build architecture, starts Simulation Runner Jobs, and observes their terminal result through the `PostProcessing` boundary.

`alpha4` is introduced by this feature as the only served, storage, and reconciled version. It is the active API, not a deprecated or transitional version. Only `alpha2` and `alpha3` are retired. There is no legacy reconciler, conversion webhook, compatibility mode, or automatic migration.

### Upgrade from alpha2 or alpha3

This is a breaking upgrade. Before installing the alpha4-only CRD, operators must export the desired old resources, transform each manifest to alpha4 by supplying every new required Translator registry and Builder field, delete all old `SimulationExperiment` resources, and delete the old CRD. They then install the alpha4 CRD and create the transformed resources. The Operator and SM must never perform these destructive steps.

The shared smoke cluster follows the same alpha4-only contract but does not perform the breaking upgrade. The smoke harness must never delete or migrate the shared CRD. Its preflight requires the installed CRD to serve and store only alpha4. It fails before creating a test namespace when it finds alpha2, alpha3, another storage version, or incompatible `status.storedVersions`. A cluster administrator performs the one-time breaking upgrade outside the test harness. Fast envtest coverage installs the generated alpha4-only CRD in an isolated control plane and verifies that alpha2 and alpha3 are not served.

### In Scope

1. **Alpha4 API, identity, and registry authentication.**
   - Carry the alpha3 user-facing schema into `api/alpha4`, retain required `spec.translator.baseimage`, and add required `spec.translator.registryAuthSecretRef corev1.LocalObjectReference` and `spec.translator.builderImage`. In YAML, the reference is `registryAuthSecretRef: {name: cbse-registry-auth}`; it is never an inline, plain, or base64-encoded credential string. `spec.translator.image` continues to select the Translator container. `builderImage` independently selects its rootless BuildKit sidecar, while `baseimage` selects the runner base from which Translator generates scenario-specific images.
   - In alpha4, each `DatabaseSpec` requires exactly one of `image` or `host`. `dbname`, `user`, `password`, and `port` are required for both forms; `dbname`, `user`, and `password` must be non-empty and `port` remains in `1..65535`. A host is a trimmed DNS name or IP literal without a URL scheme, path, query, or embedded port; Unix-domain socket paths are unsupported. An image value must satisfy the owning image-validation rule. These are CRD admission rules where structural CEL can express them and are repeated by the Operator for descriptive provisioning errors. A host-based form must omit `command`, `args`, and `nodePort`, and may only use the absent/default `ClusterIP` value of `serviceType`; those fields configure an Operator-deployed database and have no external-host meaning. A host-based database is not connection-only metadata: the Operator must create or reconcile the same `<experiment-name>-<suffix>-sct` connection Secret produced for an image-based database, using the normalized `spec.host` as `host` and copying the four other connection fields. It creates no Deployment or Service for a host-based database.
   - Add optional `spec.translator.builderResources corev1.ResourceRequirements`. Only CPU and memory entries in `requests` and `limits` are accepted; resource claims, huge pages, extended resources, and every other key are rejected. The Operator computes the effective resources without mutating the CR: missing CPU and memory limits default independently to `1` CPU and `2Gi`, while requests have no defaults and remain absent unless supplied. Every supplied quantity must be positive and each request must not exceed its corresponding effective limit.
   - Add optional `spec.runner.jobTemplate` as a typed Kubernetes `batch/v1.JobTemplateSpec`. When absent, SM uses the minimal built-in runner Job. When present, it provides the permitted Job and Pod customization described below.
   - `spec.translator.repository`, `spec.translator.baseimage`, `spec.translator.builderImage`, `spec.translator.builderResources`, and `spec.runner.jobTemplate` are immutable for the lifetime of an alpha4 `SimulationExperiment`. The generated CRD must enforce this at API admission with `oldSelf` transition rules, not through a later Operator reconciliation error. The required scalar rules are `self.repository == oldSelf.repository`, `self.baseimage == oldSelf.baseimage`, and `self.builderImage == oldSelf.builderImage` on `spec.translator`. The optional-object rules must reject addition, removal, or modification: `has(self.builderResources) == has(oldSelf.builderResources) && (!has(self.builderResources) || self.builderResources == oldSelf.builderResources)` on `spec.translator`, and `has(self.jobTemplate) == has(oldSelf.jobTemplate) && (!has(self.jobTemplate) || self.jobTemplate == oldSelf.jobTemplate)` on `spec.runner`. Each rejection names the immutable field and instructs the user to delete and recreate the CR. An unchanged field passes ordinary metadata or status updates. There is no in-place rollout, migration, or reconciliation behavior for changing these fields; the existing SM cleanup finalizer and deletion contract must complete before a replacement with the same namespace/name is created.
   - `registryAuthSecretRef.name` identifies an existing Secret in the `SimulationExperiment` namespace and must equal `cbse-registry-auth`; every other value is invalid. Before provisioning any experiment component, the Operator gets that exact Secret and requires type `kubernetes.io/dockerconfigjson` and a `.dockerconfigjson` key containing valid Docker configuration JSON. Credential lookup uses the same standard Docker config-file resolver semantics as BuildKit, including well-known registry aliases, rather than a CBSE-specific host normalizer. The resolver must return non-empty basic-auth username and password values for the registry authority parsed from `spec.translator.baseimage` and from `spec.translator.repository`; identity tokens and credential-helper-only entries are unsupported, and the implementation must not fall back to unauthenticated access even if either registry is public. This rule does not apply to unrelated Operator, Translator, Builder, PostgreSQL, EDS, or post-processing image registries, which use the cluster's ordinary image-pull configuration. Translator and BuildKit use the mounted file for authenticated base-image pulls and generated-image pushes; SM adds the same LocalObjectReference to the runner Job so the kubelet can pull the generated image.
   - `translator.image`, `baseimage`, `builderImage`, and a ready-message runner image must be OCI digest references in the exact form `name@sha256:<64 lowercase hexadecimal characters>`. Tags alone are invalid.
   - `translator.repository` is the user-provided OCI repository where generated runner images are pushed. It is a repository name without a tag or digest and is not globally hard-coded by the feature implementation. The smoke profile is intentionally fixed: component and shared base images are published under `registry.unibw.de/i31bdase/cbse-test`, while generated smoke runner images are published under the dedicated repository `registry.unibw.de/i31bdase/cbse-test-runner`. Neither repository is provisioned in advance. Harbor creates `cbse-test` implicitly on the first component or shared-base push and creates `cbse-test-runner` implicitly on the first generated-runner push; feature code and the harness must not call a repository-creation API. The rendered smoke experiment must use that exact runner repository, and smoke preflight rejects an override or mismatch. Registry access uses verified TLS. Insecure registries and TLS-verification bypasses are prohibited.
   - Persist each project by the statically defined `project_namespace` and `project_name`, where `project_name` is exactly `SimulationExperiment.metadata.name`; the pair is unique. Apart from the technical primary key used by `scenario_status.project_id`, the project table has no mutable status, component-count, or experiment-UID columns. Existing Core DB tables are validation-only and must not use automatic `ALTER TABLE` repair. This alpha API defines no migration procedure: an incompatible installation fails startup and must independently provide a fresh compatible database or recreate the tables.
   - Do not add namespace, project-name, or experiment-UID columns to `scenario_status`; its existing `project_id` foreign key remains the only project identity stored on a scenario row. Add only the non-identity operational column `translation_publish_started_at TIMESTAMPTZ NULL`, which distinguishes a claim for which NATS publication definitely never began from one whose publication result may be ambiguous. Kubernetes Job reconciliation has no persisted failure counter. The live Kubernetes object, its UID, owner references, and reserved workload labels provide incarnation identity for Kubernetes operations; the Core DB project identity is only namespace/name.
   - Namespace and project name are the routing identity for EDS and Translator communication. Alpha4 experiment names must be lowercase DNS labels of 1 to 63 characters matching `^[a-z0-9]([-a-z0-9]*[a-z0-9])?$`; dots are rejected even though Kubernetes otherwise permits them in names. Namespace and project are inserted into NATS subjects without lossy normalization.

2. **Namespace-aware EDS and Translator communication.**
   - Change the alpha4 subjects to carry namespace and project as separate tokens:
     - EDS availability: `cbse.<namespace>.<project>.eds.scenarios.available`
     - EDS scenario batch: `cbse.<namespace>.<project>.eds.scenarios`
     - Translator request: `cbse.<namespace>.<project>.trans.request`
     - Translator ready: `cbse.<namespace>.<project>.trans.<scenario-id>.ready`
   - Preserve the existing EDS availability, scenario-batch, Translator request, and Translator ready JSON payloads. Do not add namespace or UID fields to those payloads. Preserve publish confirmation, ACK/NAK behavior, guarded `Created -> Scheduled`, accepted-ready handling, and exact-attempt recovery.
   - SM parses namespace and project from every EDS and Translator subject and resolves the exact `(project_namespace, project_name)` database row. A payload project field, where the existing payload has one, must exactly equal the project subject token. A subject/payload or subject/database identity mismatch is a permanent poison message and must not mutate scenario state.
   - Before accepting any EDS batch or Translator ready message, SM gets the matching alpha4 experiment and applies the lifecycle gate defined below. Only a live, non-deleting experiment in `InProgress` may consume scenario work. For an admitted non-empty Translator ready image, SM validates the image as a digest reference, normalizes its repository and `spec.translator.repository` with the same OCI reference parser, and requires an exact match. A digest from another repository is a permanent ready-message rejection: SM ACKs it as poison, does not persist the image, and does not create a runner Job.
   - SM is cluster-wide for alpha4 communication. Its experiment informer and NATS subscriptions cover every namespace: `cbse.*.*.eds.scenarios.available`, `cbse.*.*.eds.scenarios`, and `cbse.*.*.trans.*.ready`. Components remain namespace-local Kubernetes workloads, but one SM deployment can communicate with EDS and Translator components belonging to experiments in different namespaces. Alpha4 does not use `SCENARIO_MANAGER_WATCH_NAMESPACE` to narrow subjects or Kubernetes discovery.
   - `InProgress` is the only experiment phase in which SM publishes Translator requests or consumes EDS batches and Translator ready messages. In `Error` or `Failed`, and from the moment `deletionTimestamp` is present, SM performs no domain processing for the experiment. No NATS or JetStream request is expected after the experiment reaches `Completed`; a late or in-flight delivery is nevertheless rejected by the same terminal lifecycle gate. Because the two SM JetStream consumers are shared wildcard consumers, the broker may still deliver a project-scoped message after the gate closes. SM ACKs and discards such a terminal delivery without mutating Core DB state, consuming a translation attempt, or creating a Job; this transport-level disposal is not scenario-work consumption. An inactive EDS availability request never receives `status=ready` or a batch subject.
   - Every Operator-managed Pod template carries reserved labels `experiment.cbse.terministic.de/project=<experiment-name>` and `experiment.cbse.terministic.de/experiment-uid=<full-experiment-UID>`. The Operator injects `SIMULATIONPROJECTNAMESPACE`, `SIMULATIONPROJECTNAME`, and `SIMULATIONEXPERIMENTUID` into the per-experiment EDS and Translator through downward-API field references to `metadata.namespace`, `metadata.labels['experiment.cbse.terministic.de/project']`, and `metadata.labels['experiment.cbse.terministic.de/experiment-uid']` respectively. `metadata.name` and `metadata.uid` would identify the Pod rather than the owning experiment and must not be used for these two values. Each component publishes or subscribes only to subjects for that exact namespace/name pair.
   - SM owns the finalizer `scenario-manager.cbse.terministic.de/cleanup`. It adds the finalizer before registering a non-deleting experiment in Core DB. On deletion, the still-present CR supplies the exact namespace, name, and UID while SM removes the experiment's messaging artifacts and project row; only successful cleanup permits SM to remove its finalizer. Kubernetes therefore cannot admit a replacement CR with the same namespace/name until the previous incarnation's durable state is gone. A terminal-but-not-deleted experiment only has its lifecycle gate closed: this branch does not delete its Translator consumer or purge its subjects until the CR is deleted. Cleanup never deletes a shared stream or an SM consumer.

3. **Reference Translator template and on-demand image creation.**
   - Add a Go reference implementation at `component-templates/translator/`. It is the supported integration template for user-defined Translators and replaces `test/mocks/translator` in the full smoke path.
   - Separate the implementation into three explicit layers: the CBSE-maintained reference framework, a replaceable model-generator module, and the generated scenario-specific runner image. Production-quality guarantees apply to the framework and its integration contracts, not to the example model's semantics.
   - The Operator configures Translator through the same authentication-free environment-based process used by the existing smoke mock: `NATS_URL`, Translator stream, exact request subject, ready-subject template, and UID-specific durable-consumer name. `NATS_URL` contains no user information or credential material. No NATS credential environment variable, Secret, token, NKey, JWT, or credentials-file mount is added in alpha4. The Operator also injects `SIMULATIONPROJECTNAMESPACE`, `SIMULATIONPROJECTNAME`, `SIMULATIONEXPERIMENTUID`, `REPOSITORY`, and `BASEIMAGE`. The template validates all values before connecting to NATS.
   - `BASEIMAGE` is sourced only from `spec.translator.baseimage`. EDS payloads, Scenario Status, and existing Translator request and ready payloads do not carry or persist base-image information.
   - The Operator mounts the existing `<experiment-name>-detaildb-sct` and `<experiment-name>-resultdb-sct` Secrets read-only at `/detaildb-connection` and `/resultdb-connection` respectively for Translator. Each Secret contains `host`, `port`, `dbname`, `user`, and `password`. Database credentials may be literal deployment configuration in this trusted prototype, but the generator does not duplicate them in source code: the framework reads the mounted files and passes typed connection values to the generator. This allowance does not apply to third-party credentials such as registry or robot-account credentials, Kubernetes kubeconfigs, or other externally managed service credentials.
   - SM is the only supported request publisher and marshals requests from the typed four-field request struct after validating positive scenario ID and translation attempt. The template performs the lightweight decoding defined below and handles one request at a time. Its workspace is `/workspace/scenario-<scenario-id>/attempt-<attempt>`. It checks for a retained outcome before removing or recreating build input; only an attempt with no retained outcome and no recoverable registry tag has its build directory recreated.
   - The template uses the official BuildKit Go client over `unix:///run/buildkit/buildkitd.sock`. It submits the generated attempt directory as both the Dockerfile and build context, supplies registry credentials from `/registry-auth/config.json` through the BuildKit session, and requests a registry push to the deterministic tag.
   - The pushed tag is `<repository>:runner-<12-char-UID-prefix>-s<scenario-id>-a<attempt>`. The UID prefix is the first 12 lowercase hexadecimal characters of the UID after removing hyphens. The framework adds OCI manifest annotations for the full experiment UID, scenario ID, and translation attempt, verifies them after the push, resolves the tag, and publishes only the digest reference.
   - Translator publishes the ready message and waits for publish confirmation before acknowledging the request. A generator, BuildKit, push, or digest-resolution failure persists and publishes the existing ready message with an empty image and is acknowledged only after that publish succeeds. NATS transport or ready-publication failures are retryable and must not manufacture an empty result.
   - A successful push is a durable external effect. If ready publication then fails, Translator retains the resolved digest and retries publication of the same ready message. It must not regenerate, rebuild, or push another image for that scenario and attempt.
   - After a terminal acknowledged outcome, the template removes the attempt workspace. Before generation on redelivery, it validates any retained success or empty-failure outcome for that scenario and attempt and republishes that exact ready message without repeating the failed operation. A retained success reuses its digest without another registry lookup, generator call, build, or push. If a success marker was lost, Translator resolves the deterministic registry tag and reuses it only when its repository and framework-owned identity annotations exactly match the current experiment UID, scenario ID, and attempt. A missing or mismatched annotation is a permanent tag collision: Translator records and publishes an empty-image outcome, does not adopt or overwrite the tag, and does not invoke the generator or BuildKit. It recreates build input only when no marker or registry tag exists.

4. **Mandatory rootless BuildKit sidecar.**
   - Each Translator Pod has exactly two containers: `translator` and rootless `buildkit`.
   - Both containers run as UID and GID `1000`. The Pod sets `fsGroup: 1000`. They share `emptyDir` volumes mounted at `/workspace` and `/run/buildkit`, so generated build input and the BuildKit socket are writable by both containers. BuildKit listens only on `/run/buildkit/buildkitd.sock`; no TCP endpoint is exposed.
   - BuildKit receives the effective `spec.translator.builderResources`. CPU and memory limits default independently to `1` CPU and `2Gi`; user-supplied values replace those defaults. CPU and memory requests are copied only when supplied and otherwise remain absent.
   - Both containers receive the referenced registry Secret read-only at `/registry-auth/config.json`. The Translator also receives the Scenario Detail DB and Result DB Secrets read-only at `/detaildb-connection` and `/resultdb-connection`; BuildKit does not receive either database Secret.
   - BuildKit runs rootless and non-privileged with the required compatibility exception: `Unconfined` seccomp and AppArmor plus `--oci-worker-no-process-sandbox`. It must not use privilege escalation, privileged mode, host networking, host paths, or host container-runtime sockets. Translator keeps the normal restricted security profile.
   - A namespace that enforces the Kubernetes Baseline or Restricted Pod Security Standard rejects this required BuildKit exception. The smoke harness therefore labels its isolated `cbse-e2e-<run-id>` namespace with `pod-security.kubernetes.io/enforce=privileged`. This admission label does not permit the implementation to set `privileged: true` or weaken any other container restriction. The harness keeps `audit` and `warn` at `restricted` so the exception remains visible.
   - Kubernetes 1.30 or newer is required so the BuildKit container can set `securityContext.appArmorProfile.type: Unconfined`. Cluster installation documentation must call out the namespace admission requirement and the AppArmor prerequisite.
   - Generated source, Dockerfile, and build commands are trusted prototype input. Egress isolation and an untrusted-build sandbox are deferred.

5. **Simulation Runner startup and terminal observation.**
   - For one accepted Translator image, SM creates or confirms one `batch/v1` Job for the scenario and translation attempt. The Job name is `simrun-<12-char-UID-prefix>-s<scenario-id>-a<attempt>`, using the UID-prefix rule above.
   - The default Job has one container named `runner`. A custom template must contain exactly one `runner` container and may use only the explicit public allow-list below for sidecars, init containers, volumes, mounted configuration, resources, metadata, deadlines, and scheduling constraints. Every user-supplied field outside that allow-list is rejected. SM replaces the `runner` image with the accepted Translator image.
   - The Job is indexed. `spec.completionMode` is `Indexed`, and `spec.completions` and `spec.parallelism` both equal `scenario_status.number_of_reps`. Each completion index represents one homogeneous repetition for Kubernetes retry accounting. The runner does not consume the index, and Core DB and Result DB do not persist it as a repetition identity. All repetitions may run concurrently; a user-defined model and durable result sink must support that concurrency.
   - `number_of_reps` is limited to `1..100000`, the Kubernetes Indexed Job maximum when `parallelism` equals `completions`. This branch adds no lower operational cap, separate maximum parallelism, batching, or throttling. The trusted-input prototype accepts Jobs up to that Kubernetes limit. A future EDS template may impose a smaller policy limit.
   - Capacity and fairness follow ordinary Kubernetes Job and scheduler behavior. SM submits the Job without calculating cluster capacity or waiting for free resources. The Job may have at most `number_of_reps` Pods active because `parallelism` equals that value; the configured Kubernetes scheduler creates or binds Pods only as resources and scheduling constraints permit. Pending or unschedulable Pods leave the scenario `InProcessing` and are not Job-reconciliation failures. CBSE adds no custom capacity queue, admission calculation, or cross-Job scheduling policy.
   - Set `restartPolicy: Never`, global `backoffLimit: 4`, no `backoffLimitPerIndex`, no `maxFailedIndexes`, and no TTL-after-finished field. Kubernetes counts failed Pods across the entire Job against this one retry budget and creates replacement Pods while the budget remains. When the budget is exhausted, Kubernetes marks the Job `Failed` and terminates its remaining work. The Job succeeds only when all requested indexes complete, unless an allowed `activeDeadlineSeconds` terminates it earlier. Completed and failed Jobs remain visible.
   - The Job has the exact alpha4 experiment owner reference, with `controller: true` and `blockOwnerDeletion: false`, runs in the experiment namespace, and uses the deterministic ServiceAccount `simrunner-<12-char-UID-prefix>`. The false block-owner-deletion value avoids requiring SM to have delete permission on experiments while preserving normal garbage collection. The Operator creates this ServiceAccount, gives it no RoleBinding, and SM sets `automountServiceAccountToken: false` on the Job Pod. Every Pod template created for an experiment by the Operator or SM carries the reserved project and full experiment-UID labels defined above; this includes internal database, EDS, Translator, post-processing, and runner Pods. The Operator and SM copy both label values from the live `SimulationExperiment`; containers that require the UID read the reserved label through the downward API and need no Pod-read permission.
   - Runner, sidecar, and init-container images must support non-root execution. The effective Pod template uses `RuntimeDefault` seccomp, `runAsNonRoot: true`, disabled privilege escalation, and dropped capabilities. The Operator validates the template before moving the experiment to `InProgress`; SM validates it again before creating a Job.
   - SM observes Jobs for scenarios in `InProcessing` and monotonically records successful completion indexes in `scenario_status.number_of_computed_reps`. A successful Job records all requested repetitions before the guarded `InProcessing -> PostProcessing` transition. A failed Job preserves any partial successful count before the guarded `InProcessing -> Failed` transition. This branch does not execute post-processing or move a scenario out of `PostProcessing`.

### Workflow Decisions

| Concern | Decision for this branch |
| --- | --- |
| Active API | Alpha4 only; alpha2 and alpha3 are retired through a manual breaking upgrade. |
| Messaging identity | Namespace and project are separate NATS subject tokens and resolve one exact project row. |
| Messaging payloads | Existing EDS and Translator JSON payloads remain unchanged. |
| Project incarnation | Namespace/name in Core DB; SM cleanup finalizer prevents same-name recreation until old durable state is deleted. |
| Translator lifetime | Existing always-on Deployment and Service per experiment. |
| Translator implementation | Go reference framework in `component-templates/translator/`; user-defined model generation is plugged into it. |
| Example model | Scenario-specific single-server queue whose generator reads a positive integer `parameterset_id` lookup key from `recipe_info`, retrieves four integer parameters through the fixed Scenario Detail Database query, and writes them into the generated model. |
| Example PRNG | Each runner uses a local PRNG seeded from the Detail DB `seed_policy` plus its runtime Pod hostname, diversifying repetitions and retry Pods without a Core DB or payload change. |
| Build engine | One serial, rootless BuildKit sidecar per Translator Pod. |
| Build handoff | Shared `/workspace` and `/run/buildkit` `emptyDir` volumes, UID/GID `1000`, `fsGroup: 1000`, and a Unix socket. |
| BuildKit resources | Optional `corev1.ResourceRequirements`; CPU and memory limits default independently to `1` CPU and `2Gi`, while requests are user-configurable and have no defaults. |
| Build result | Deterministic attempt tag, then immutable digest in the existing ready message. |
| Ready publish retry | Reuse the successful pushed digest; never regenerate, rebuild, or repush that scenario attempt. |
| Empty translation outcome | Persist and publish an empty ready; retry from `Created` until `SCENARIO_MANAGER_TRANS_MAX_ATTEMPTS` (default `3`), then fail the scenario. |
| Translation publish boundary | Persist publish-start before invoking NATS; refund only an exact claim for which publication definitely never began. |
| NATS security | Authentication-free in this branch, matching the current smoke deployment; exact subjects and ownership metadata are correctness checks, not broker-enforced ACLs. |
| Credential lifetime | Registry, Scenario Detail DB, and Result DB credentials are loaded at Translator startup; live rotation and reload are out of scope. |
| Runner unit | One customizable, indexed Job per scenario and translation attempt; one completion index per required repetition. |
| Runner concurrency | `parallelism = completions = number_of_reps`. |
| Runner concurrency limit | `number_of_reps <= 100000`, the Kubernetes Indexed Job limit; no smaller feature-specific cap. |
| Runner retry | One global Kubernetes Job retry budget with `backoffLimit: 4`; `backoffLimitPerIndex` is absent. |
| Job API retry | Transient Kubernetes API failures are requeued without a persisted counter or scenario-failure limit. |
| Runner completion | Job success moves the scenario to `PostProcessing`; Job failure moves it to `Failed`. |
| Post-processing boundary | `PostProcessing` is reached but not processed in this branch. |
| Experiment terminal phases | Only `InProgress` admits messaging work. SM publishes or consumes no scenario work in `Error`, `Failed`, or while deleting; no requests are expected in `Completed`. SM observes all three terminal phases only to close the lifecycle gate; NATS artifact cleanup waits for CR deletion. |
| Example result sink | Each successful generated runner appends one JSONB result to a PostgreSQL table dedicated to its scenario. Writes are at-least-once; completion indexes are not persisted as result identities. |
| Scenario logging | Log scenario-level creation and terminal outcome plus one runner-controlled failure record per failed Pod attempt; do not log each successful repetition. |
| Durable authority | PostgreSQL guarded transitions; Jobs are idempotent external effects. |

### Registry Credential Boundary

The test pipeline publishes component images, including SM, EDS, and Translator, and shared base images below `registry.unibw.de/i31bdase/cbse-test`; it publishes generated smoke runner images below `registry.unibw.de/i31bdase/cbse-test-runner`. Neither repository exists before its first push. Harbor creates each repository record implicitly when its first image is pushed: a component or shared-base push creates `cbse-test`, and a generated-runner push creates `cbse-test-runner`. Registry authentication is external operational state. The Harbor robot-account identifier, secret, source credential file, complete Docker configuration, and login procedure must never be committed, hard-coded, logged, rendered into test artifacts, or copied into this specification. The repository records only non-secret integration identifiers and requirements: the two registry repositories, Kubernetes Secret name and type, runtime input names, and required registry operations.

`cbse-registry-auth` is the sole registry-credential Secret name. It is a namespace-scoped `kubernetes.io/dockerconfigjson` Secret with a `.dockerconfigjson` key. Outside the smoke profile, the deployment owner supplies that Secret independently with credentials appropriate for the configured base and target repositories; this specification fixes its name and validation, not a production account. In the smoke profile, it is provisioned outside this repository from the protected Harbor robot-account credentials. The credential file and its source location are not repository inputs and must not be copied below the repository, including into an ignored directory. The smoke credential must authorize Repository Pull and Push and Artifact List, Read, and Delete in the Harbor project `i31bdase`. Harbor scopes the robot account's Artifact Delete capability to the project rather than to one repository: after that permission is enabled, the credential can delete artifacts from both `cbse-test` and `cbse-test-runner`, as well as from any other repository in `i31bdase`. Keeping generated runners in `cbse-test-runner` is an organizational and cleanup-target boundary, not a registry authorization boundary. This project-wide artifact-deletion capability is an explicitly accepted smoke-account risk. Feature code may exercise it only through the annotation-verified `cbse-test-runner` cleanup path and must never delete an artifact from `cbse-test`; no feature code uses project, project-metadata, whole-repository deletion, or update operations. Kubernetes uses the Secret as an image pull Secret for component and runner image pulls. Kubernetes does not push images: the Translator and its rootless BuildKit sidecar mount the Secret file read-only and use its credentials for authenticated base-image pulls and generated-runner pushes. “Read-only” describes the volume mount, not the credential's registry permissions. Because the same smoke credential also supports cleanup, those trusted smoke containers technically receive project-wide artifact-delete-capable credentials; this accepted smoke-only boundary must not be generalized into a production credential requirement. Translator and BuildKit must never invoke deletion or expose a deletion interface. Every `SimulationExperiment` must set `spec.translator.registryAuthSecretRef.name` to `cbse-registry-auth`, and the Secret must exist in that experiment's namespace before provisioning begins.

The smoke source Secret resides in `cbse-test-system`; the harness validates its type, `.dockerconfigjson` key, Docker JSON syntax, and `registry.unibw.de` basic-auth entry before copying it only into the ephemeral `cbse-e2e-<run-id>` namespace. The only filesystem credential input is the runtime environment variable `CBSE_REGISTRY_AUTH_FILE`, which must identify a protected Docker `config.json` supplied by CI or the operator and located outside the repository. The source Secret and `CBSE_REGISTRY_AUTH_FILE` must represent the same Harbor robot account. Before any registry or cluster mutation, the harness resolves the `registry.unibw.de` basic credentials from both Docker configurations with the canonical Docker resolver and requires the resolved username and password to match exactly; differing JSON serialization or unrelated registry entries do not constitute a mismatch. Missing, unsupported, or unequal resolved credentials fail preflight without logging which field differed or either credential value. `CBSE_REGISTRY` must equal `registry.unibw.de/i31bdase/cbse-test`, `CBSE_PULL_SECRET_NAME` must equal `cbse-registry-auth`, and `CBSE_PULL_SECRET_NAMESPACE` must equal `cbse-test-system` when those legacy variables are present; a different value fails preflight. The runner repository has no override variable. CI provides the protected Docker configuration only for the duration of a run; it authenticates component publication and generated-runner cleanup without being converted into a repository file. The harness must not print the credential-file path or contents. No registry or robot-account credential, registry Secret payload, runtime registry-credential file, or decoded registry credential may be committed, logged, included in manifests or artifacts, or added to `FEATURE.md`. This restriction is specific to third-party credentials and does not prohibit literal CBSE database credentials in the CR or smoke configuration. If a Kubernetes namespace is retained for debugging, the harness deletes its copied `cbse-registry-auth` Secret before returning and reports only that the credential was removed; the shared source Secret remains untouched.

### Out of Scope

This branch must not add:

- alpha2 or alpha3 serving, reconciliation, conversion, or migration automation;
- a per-scenario Translator Job or a change to Translator lifetime;
- new EDS or Translator JSON payload fields, or a new JetStream acknowledgement model beyond the namespace-aware subject change defined here;
- NATS authentication, per-client NATS identities, account or subject ACL provisioning, credential Secrets, tokens, NKeys, JWTs, credentials files, or credential rotation;
- a universal production model generator or production simulation semantics;
- runner result ingestion, confidence calculation, PostProcessingService invocation, or any scenario transition out of `PostProcessing`;
- aggregation of scenario states into `SimulationExperiment.status.phase`, including SM-owned transitions to experiment phase `Failed` or `Completed`;
- a later execution/run identity for a repeated scenario after post-processing;
- PostProcessingService-driven `n+m` repetitions or any increase of `number_of_reps` after the original EDS batch;
- workload replacement, cancellation recovery, or self-healing beyond the global Kubernetes Job retry budget defined here;
- a second scheduler implementation or alpha4 scheduler-provider selection field;
- concurrent Translator builds, object-storage/PVC build handoff, or remote BuildKit TCP access;
- automatic registry, Scenario Detail DB, or Result DB credential rotation, file watching, or live credential reload;
- production lifecycle deletion or garbage collection of generated runner images;
- Translator-consumer deletion or subject purging for a terminal-but-not-deleted experiment, periodic orphan-consumer sweeping, and automatic recovery of consumers orphaned before the SM finalizer was installed;
- centralized scenario-log storage, retention limits, or automatic deletion of past logs;
- privileged containers, host networking, host paths, container-runtime socket mounts, insecure registries, or registry TLS-verification bypasses;
- PostgreSQL transport encryption, certificate or hostname verification, CA or client-certificate distribution, or database TLS configuration fields; alpha4's reference PostgreSQL clients use `sslmode=disable` on the trusted prototype network;
- mutation of Scenario Detail DB parameter rows, parameter snapshots or versions, and reload of a translated parameter set after image generation;
- untrusted Dockerfile isolation or build egress controls;
- an EDS template, EDS-side repetition policy, runner parallelism cap below Kubernetes' Indexed Job limit, or SM-side runner throttling; or
- deployment of test workloads in `default` or `kube-system`.

## Change Location

1. `experiment-operator/api/alpha4/`, CRD manifests, schemes, samples, and tests -- define alpha4 only, retain required `baseimage`, add `registryAuthSecretRef` as `corev1.LocalObjectReference`, add `builderResources` as `corev1.ResourceRequirements`, and define the optional typed runner Job template, UID environment value, BuildKit sidecar, and runner ServiceAccount.
2. `experiment-operator/internal/controller/` -- validate images, the existing basic-auth Docker configuration Secret, and runner Job templates; reconcile the Translator, its mock-style runtime configuration, Scenario Detail DB and Result DB Secret mounts, effective BuildKit requests and limits, UID-labelled Pod templates, workspace permissions, and the runner ServiceAccount with their exact volume, security, and ownership settings.
3. `scenario-manager/internal/nats/`, communication types, and EDS integration tests -- implement the namespace-aware EDS and Translator subject grammar, parsing, cluster-wide wildcard subscriptions, SM-owned shared stream and consumer reconciliation, exact namespace/name persistence lookups, terminal lifecycle gating, and deletion-time messaging cleanup without changing payload schemas.
4. `component-templates/translator/` and its tests -- implement the Go reference framework, namespace-aware request/ready subjects, per-experiment consumer, replaceable SimPy generator, fixed Scenario Detail Database lookup and bounded retry, attempt workspaces, BuildKit client, authenticated push, durable pushed-outcome recovery, digest resolution, and ready-message acknowledgement ordering. Retire `test/mocks/translator` from the full smoke path.
5. `component-templates/scenario-detail-database/` -- add the reference Scenario Detail Database image, based on the pinned official PostgreSQL server digest, with checked-in `/docker-entrypoint-initdb.d/` SQL that creates `public.simulation_parameters`, grants the required schema/table access, and inserts the four immutable parameter rows. The smoke fixture selects this image but does not create or insert those rows.
6. `scenario-manager/internal/core/`, `internal/coredb/`, and `internal/kube/` -- persist the static namespace/name project identity and translation publish-start marker; implement exact-attempt cancellation and split stale-claim recovery; manage the SM cleanup finalizer; expose the scheduler boundary; implement the basic Kubernetes Job adapter and per-scenario rate-limited reconciliation queue; validate and merge runner templates; create or confirm Jobs; monotonically record successful repetitions; and apply terminal Job results.
7. The repository `Makefile` and test harness -- include the new Translator Go module in formatting, vet, unit, race, and compile checks run by `make test-fast`; build and publish the reference Scenario Detail Database image; replace alpha3 schemes, fixtures, manifests, preflight, and assertions with alpha4; and unconditionally clean generated smoke runner images.
8. SM RBAC and `test/e2e/manifests/` -- grant cluster-wide `get`, `list`, `watch`, and `patch` for alpha4 experiments; `create`, `get`, `list`, and `watch` for Jobs; `get`-only Secret access restricted by `resourceNames` to `cbse-registry-auth`; and `get`-only access, without `list` or `watch`, for ServiceAccounts. SM uses patch only to add or remove its metadata finalizer and does not modify experiment spec or status. It must use direct named reads for the canonical registry Secret and deterministic runner ServiceAccount. Provide digest-pinned Builder and Translator images plus the configured digest-pinned runner smoke base image. Translator generates the scenario-specific runner images; there is no prebuilt runner smoke image.
9. `component-templates/translator/README.md`, `component-templates/scenario-detail-database/README.md`, `COMPONENT_DESIGN_GOALS.md`, `README.md`, `docs/CBSE_TESTING_GUIDE.md`, and developer documentation -- provide the Translator and reference Detail DB integration guides, common component design goals, alpha4-only public and test documentation, and links to those contracts.

## Logic Description

### Namespace-aware messaging contract

Alpha4 replaces project-only routing with a single canonical subject grammar. Namespace and project occupy separate subject segments and are copied directly from `SimulationExperiment.metadata.namespace` and `.metadata.name`. Code must validate both tokens before constructing or accepting a subject. It must not pass identity through `subject.NormalizeToken` or any other lossy conversion. Invalid identity is a permanent configuration or message error.

| Flow | Concrete subject | SM subscription or stream subject |
| --- | --- | --- |
| EDS availability request/reply | `cbse.<namespace>.<project>.eds.scenarios.available` | `cbse.*.*.eds.scenarios.available` |
| EDS scenario batch | `cbse.<namespace>.<project>.eds.scenarios` | `cbse.*.*.eds.scenarios` |
| Translator request | `cbse.<namespace>.<project>.trans.request` | JetStream stream subject `cbse.*.*.trans.request`; each Translator consumes its exact subject |
| Translator ready | `cbse.<namespace>.<project>.trans.<scenario-id>.ready` | `cbse.*.*.trans.*.ready` |

SM applies one canonical lifecycle gate before publishing a Translator request or performing domain processing for an EDS availability request, EDS batch, or Translator ready message. It gets the exact alpha4 `SimulationExperiment` identified by the subject and admits work only while the object exists, has no `deletionTimestamp`, and has phase `InProgress`. `Pending`, `Provisioning`, and an empty phase are not yet active: availability returns `status=error` without `batch_subject`, and a JetStream delivery is NAKed for later redelivery. `Error`, `Failed`, `Completed`, and deletion are permanently inactive for messaging: availability returns `status=error` without `batch_subject`, while an unavoidable JetStream delivery through the shared wildcard consumer is ACKed and discarded without database mutation, attempt consumption, or Job creation. By contract, publishers send no NATS or JetStream requests after `Completed`; the defensive terminal rule covers only a late, duplicate, or already-in-flight delivery. In this specification, “SM does not consume messages” for an inactive experiment means that no scenario-work handler runs; the terminal ACK is transport cleanup that prevents an unprocessable delivery from redelivering forever.

EDS continues its request/reply handshake, but publishes availability to its exact namespace/project subject. SM parses the subject, validates the existing availability payload's `project` against it, resolves the exact project row, and applies the lifecycle gate before replying. A successful reply for an active experiment returns the exact namespace-aware batch subject. When the subsequent JetStream batch arrives, SM parses and validates its subject again, applies the lifecycle gate, requires `ScenarioBatch.project` to match, and requires every scenario's `number_of_reps` to be in `1..100000` before beginning the existing all-or-nothing batch insert. A batch containing an invalid repetition count is permanent poison: SM returns an error acknowledgement, ACKs the JetStream delivery, and inserts no rows. A valid batch associates all inserted scenarios with `ProjectIDByNamespaceAndName(namespace, project)`.

SM resolves the candidate's raw project namespace and name and applies the lifecycle gate before claiming `Created -> Scheduled`. The claim atomically increments `translation_attempts` and clears both `translation_publish_started_at` and `translation_request_published_at`. Immediately before publication, SM applies the lifecycle gate again. If that check rejects the experiment or cannot confirm that it remains active, SM does not call NATS and calls `CancelUnpublishedTranslationClaim(scenarioID, attempt)`. That guarded update requires the exact `Scheduled` attempt and both publication timestamps to be null, restores `Created`, decrements `translation_attempts` by one, and updates `updated_at`. A zero-row result is a successful stale no-op. A database error is retryable and leaves the row for the stale-claim recovery described below. This cancellation is compensation for work that never began and is allowed even after the lifecycle gate closes.

After the second gate passes, SM calls `MarkTranslationPublishStarted`, which sets `translation_publish_started_at = NOW()` only for the exact `Scheduled` attempt while both publication timestamps are null. Only a caller that changes that row may invoke NATS publication. From that point the attempt is never decremented or reused because NATS may have accepted the request even when confirmation is lost. A confirmed publish sets `translation_request_published_at = NOW()` for the same attempt and requires a non-null started marker. A publish error or a failure to persist confirmation uses the existing exact-attempt publish-failure path: below `SCENARIO_MANAGER_TRANS_MAX_ATTEMPTS` it returns the row to `Created`, at the limit it moves the row to `Failed`, and it never refunds the attempt.

Stale unpublished-claim recovery runs before ordinary selection and remains active as bookkeeping even when the experiment lifecycle gate is closed. For an old `Scheduled` attempt with both publication timestamps null, it applies `CancelUnpublishedTranslationClaim` and refunds the attempt. For an old attempt with `translation_publish_started_at` set and `translation_request_published_at` null, publication is ambiguous, so recovery applies the existing attempt-consuming `Created`/`Failed` policy. A confirmed request is not recovered. The unavoidable phase change after the final lifecycle check is handled conservatively: the started attempt remains consumed, and any late ready delivery is subject to the terminal ACK-and-discard rule.

The publisher uses namespace and project to construct the exact request subject. The per-experiment Translator template validates its injected namespace and project at startup, subscribes only to that subject, and publishes ready messages only on the corresponding namespace/project ready subject. SM parses both tokens from a ready subject, applies the lifecycle gate, and requires them to match the scenario's persisted project row before applying empty-image recovery or a non-empty ready result. For a non-empty image, SM validates the digest and requires its normalized repository to equal `spec.translator.repository`. A wrong namespace, project, digest, repository, or stale translation attempt is ACKed as poison and cannot consume an attempt or alter the row.

Transport-neutral and persistence projections carry the validated raw identity explicitly. Extend `ScenarioForTranslation` and `TranslatorReadyMessage` with `ProjectNamespace` and `ProjectName`; do not retain a single ambiguous `Project` field in new alpha4 code. EDS may keep the wire field `project`, but after subject parsing its internal `ScenarioBatch` also carries `ProjectNamespace` and `ProjectName` fields excluded from JSON. Core orchestration and database functions receive this explicit identity and never reconstruct a namespace from process configuration or a normalized subject token.

Malformed subject shape, invalid tokens, subject/payload disagreement, and a batch or ready message that identifies no matching project are permanent poison deliveries: respond with an error where request/reply permits it, ACK JetStream messages, and perform no database mutation. A temporary database, Kubernetes lookup, or NATS failure is retryable: return an error for availability so EDS retries its request, or NAK a JetStream delivery. Availability for a valid identity that has not yet been registered or is not yet `InProgress` returns `status=error` with no `batch_subject`; it does not create a project row. The EDS retries availability and publishes no batch until it receives `status=ready`. Terminal and deleting experiments use the permanent lifecycle rejection above rather than this transient path.

The canonical alpha4 subject templates are:

- `SCENARIO_MANAGER_EDS_AVAILABLE_SUBJECT_TEMPLATE=cbse.{namespace}.{project}.eds.scenarios.available`
- `SCENARIO_MANAGER_EDS_BATCH_SUBJECT_TEMPLATE=cbse.{namespace}.{project}.eds.scenarios`
- `SCENARIO_MANAGER_TRANS_REQUEST_SUBJECT_TEMPLATE=cbse.{namespace}.{project}.trans.request`
- `SCENARIO_MANAGER_TRANS_READY_SUBJECT_TEMPLATE=cbse.{namespace}.{project}.trans.{scenario_id}.ready`

These values are fixed for alpha4. If the existing environment variables are retained for deployment compatibility, an explicitly configured value must equal the corresponding canonical value above or startup fails. Alpha4 removes support for `%s`, fixed project-independent subjects, alternate prefixes, and templates missing `{namespace}` or `{project}`. SM derives its cluster-wide stream and subscription wildcards only by replacing the namespace, project, and scenario placeholders with `*`; callers do not configure a narrower namespace wildcard.

In this document, a JetStream stream is a broker-side named store for messages published on configured NATS subjects; it is not a payload and is not created per Kubernetes namespace. SM owns creation and reconciliation of the two shared streams. `cbse_eds_scenarios` uses `WorkQueuePolicy`, `FileStorage`, `DiscardOld`, and subject `cbse.*.*.eds.scenarios`. `cbse_translator` uses the same retention, storage, and discard policies and subjects `cbse.*.*.trans.request` and `cbse.*.*.trans.*.ready`. Namespace and project remain subject tokens inside those shared streams, so one stream of each kind covers all experiments without mixing their identities. Stream fields not listed here retain an existing stream's value; on creation they use the NATS server or client-library zero-value defaults. Stream reconciliation may append these required alpha4 subjects but must not remove unrelated existing subjects. If the legacy stream-name environment variables remain, they must be absent or equal these canonical names.

All SM replicas attach to the same two SM-owned consumers. The EDS batch consumer is durable `scenario-manager-eds-consumer`, queue group `scenario-manager-eds`, exact filter `cbse.*.*.eds.scenarios`, explicit ACK, `DeliverAll`, `AckWait: 2m`, `MaxAckPending: 1024`, and `MaxDeliver: -1`. The Translator-ready consumer uses durable and queue group `scenario-manager-translator-ready`, exact filter `cbse.*.*.trans.*.ready`, and the same ACK, delivery, timing, pending, and redelivery settings. Queue groups distribute a given message among SM replicas; they do not partition by namespace. Per-experiment Translators still use their exact namespace/project request filter and UID-specific durable consumer, so only the owning Translator processes a request.

Because alpha4 is a coordinated breaking upgrade, SM reconciles an existing SM-owned durable to this exact configuration before starting business processing. If the server cannot update an incompatible filter or immutable setting in place, SM deletes and recreates only that named SM-owned durable, then attaches all replicas to the resulting consumer. Startup reconciliation never rewrites or deletes a per-experiment Translator consumer; the only such deletion in this branch is the ownership-verified CR-deletion cleanup below. Concurrent SM startups treat an already-created matching stream or consumer as success; a remaining mismatch fails startup with the exact stream or consumer name and setting.

Each Translator uses a durable consumer named `translator-<12-char-UID-prefix>` with its exact request subject as the filter. The UID prefix follows the existing lowercase, hyphen-stripped rule. This keeps consumers distinct across namespaces and across deletion/recreation of an experiment. The consumer uses explicit acknowledgement, `DeliverAll`, `AckWait: 2m`, `MaxAckPending: 1`, and `MaxDeliver: -1` for unlimited redelivery. It also carries exact NATS consumer metadata `experiment.cbse.terministic.de/managed-by=translator`, `experiment.cbse.terministic.de/experiment-uid=<full-UID>`, `experiment.cbse.terministic.de/namespace=<namespace>`, and `experiment.cbse.terministic.de/project=<project>`. These metadata values are part of the consumer identity and make deletion-time ownership verifiable without relying only on the truncated durable name. Consumer metadata requires NATS Server 2.10 or newer, which is the minimum NATS version for alpha4. Translator sends `InProgress` every 30 seconds from request acceptance through generation, build, push, digest resolution, and any ready-publication retry. It publishes the ready message with confirmation and then uses a server-confirmed final acknowledgement for the request. If an in-progress acknowledgement cannot be sent, Translator cancels the active processing context and leaves the request unacknowledged for redelivery.

Translator creates its UID-specific consumer when it is absent. When it already exists, Translator attaches only if its stream, exact filter subject, all four ownership metadata entries, acknowledgement policy, delivery policy, `AckWait`, `MaxAckPending`, and `MaxDeliver` match this contract. An incompatible durable consumer is an identity collision and makes Translator startup fail without updating, deleting, or silently adopting that consumer. The Translator creates neither the shared stream nor an SM consumer.

Alpha4 deliberately retains the authentication-free NATS connection model used by the current smoke deployment. SM connects through `SCENARIO_MANAGER_NATS_URL`, and each per-experiment Translator connects through `NATS_URL`; neither URL may contain user information or credential material. The smoke values point to the in-cluster NATS Service over `nats://`, and the smoke NATS server enables JetStream without configuring users, accounts, tokens, NKeys, JWTs, credentials files, or subject permissions. If the legacy `SCENARIO_MANAGER_NATS_USER` and `SCENARIO_MANAGER_NATS_PASSWORD` variables remain in manifests, both must be empty; the reference Translator defines no corresponding credential settings or mounts.

The NATS server's anonymous client permissions must therefore allow the operations required by this feature. SM creates and inspects the two streams; creates, inspects, updates, and deletes its two consumers; inspects and deletes a per-experiment Translator consumer during CR-deletion cleanup; performs the exact subject purges defined above; publishes Translator requests; and consumes and acknowledges EDS batches and Translator-ready messages. Each Translator inspects `cbse_translator`, creates and inspects its named consumer, consumes its exact request subject, and publishes its project-scoped ready subjects. Exact subject construction, consumer ownership metadata, collision checks, and cleanup checks remain application-enforced correctness boundaries; they are not broker-enforced security isolation in this branch. NATS authentication and authorization are deferred and require a later API, Secret, deployment, and operational design before they may be enabled.

### Namespace/name project persistence

The alpha4 project table has this logical schema:

```text
id                    SERIAL PRIMARY KEY
project_namespace     TEXT NOT NULL
project_name          TEXT NOT NULL
UNIQUE (project_namespace, project_name)
```

`scenario_status.project_id` remains a foreign key to `project.id` with `ON DELETE CASCADE`. The schema is a static developer-owned contract, not a user-configurable table shape. No experiment UID or mutable project state is duplicated in either table. Queries obtain namespace and name by joining the project table and obtain the current experiment UID from the live Kubernetes object when a Kubernetes operation needs incarnation identity. The SM cleanup finalizer prevents a replacement object with the same namespace/name from being created before the previous incarnation's project row and scenarios are deleted, so restart-safe incarnation separation does not require a UID column.

Project registration is idempotent for the exact namespace/name pair. `project_name` is copied without normalization from `SimulationExperiment.metadata.name`, and `project_namespace` is copied from `metadata.namespace`. Subject normalization never changes either stored value.

SM adds its cleanup finalizer before inserting or reusing the namespace/name project row, then re-gets the object. If deletion began before that re-get, SM enters deletion cleanup and does not register or reuse a row. When `deletionTimestamp` is set, SM stops accepting new EDS or Translator work for that project, performs deletion cleanup using the still-readable object's namespace, name, and UID, deletes the project row in one transaction, and lets `ON DELETE CASCADE` remove all scenarios. Only then does it remove its finalizer. A subsequently recreated experiment with the same namespace/name therefore registers a fresh row after the old row is gone.

When the project table does not exist, SM creates it with the alpha4 schema. When it already exists, SM validates it without issuing `ALTER TABLE`. Compatibility requires exactly the three columns above with the stated types and nullability, the generated `id` primary key, and the namespace/name uniqueness constraint; legacy `experiment_uid`, `number_of_components`, `status`, or any other extra project column is incompatible. The scenario table additionally requires `translation_publish_started_at TIMESTAMPTZ NULL` with no default. This is an operational workflow field, not identity. A legacy or incompatible table makes startup fail before the informer, NATS consumers, or selector starts. Alpha4 is an unused breaking alpha API and defines no database migration procedure; an affected operator must independently provide a fresh compatible database or recreate the tables.

### Terminal gating and deleted-experiment cleanup

The Experiment Operator owns Kubernetes deletion reconciliation. Its existing finalizer and owner references remove only the experiment's Operator-created Deployments, Services, Secrets, ServiceAccount, and other owned components when a `SimulationExperiment` is deleted, including deletion during scenario execution. The pre-existing user-supplied registry Secret referenced by `registryAuthSecretRef` is not owned or deleted by the Operator. Runner Jobs carry the experiment owner reference and are removed by Kubernetes garbage collection. Component deletion is idempotent and does not depend on SM deleting Kubernetes resources.

When the experiment informer adds an object already in `Error`, `Failed`, or `Completed`, or observes a transition into one of those phases, SM re-gets the current object and requires its UID and terminal phase to match the event before closing that incarnation's lifecycle gate. A stale event for an old UID cannot close a replacement's gate. Terminal handling queues no NATS cleanup: it does not delete the per-experiment Translator consumer, purge a subject, or alter the retained project and scenario rows. A request already pending in the per-experiment consumer may therefore still be handled by Translator, but SM publishes no new request after the gate closes and ACKs and discards any resulting ready delivery without domain mutation. This bounded terminal waste is accepted for this branch and is removed by the later CR-deletion cleanup.

SM requests deletion cleanup only when it observes the current object with `deletionTimestamp`. The still-present object is protected by the SM finalizer and supplies the authoritative namespace, name, and full UID; a replacement with the same name cannot yet exist. SM closes the lifecycle gate before queuing cleanup, so no new scenario work is published or admitted while cleanup retries independently.

For an accepted CR-deletion event, SM performs these idempotent operations in order:

1. Retrieve durable consumer `translator-<12-char-UID-prefix>` from stream `cbse_translator`. A missing consumer is success. Before deletion, require its stream, durable name, exact filter `cbse.<namespace>.<project>.trans.request`, and all four ownership metadata entries to match the deleting CR's full UID, namespace, and project. Any mismatch is an identity collision: do not delete the consumer, fail that cleanup attempt, and retain the finalizer. Delete only a consumer that passes every ownership check.
2. Purge messages matching `cbse.<namespace>.<project>.eds.scenarios` from stream `cbse_eds_scenarios`.
3. Purge messages matching `cbse.<namespace>.<project>.trans.request` from stream `cbse_translator`.
4. Purge messages matching `cbse.<namespace>.<project>.trans.*.ready` from stream `cbse_translator`.
5. Delete the exact `(project_namespace, project_name)` row in Core DB in one transaction; its foreign key removes every scenario belonging to the deleted project.
6. Remove `scenario-manager.cbse.terministic.de/cleanup` only after all preceding operations succeed.

The availability subject uses Core NATS request/reply and has no durable server artifact to remove. Cleanup never deletes or recreates a shared EDS or Translator stream, an SM durable consumer, or a queue group. A missing stream, consumer, matching message, project row, or finalizer is successful cleanup where the corresponding operation is already satisfied. A message already delivered to a shared SM consumer remains subject to ACK-and-discard even if the deletion purge races with that delivery.

SM replicas may request the same deletion cleanup. Each namespace/name/UID is an independent idempotent queue key, so duplicate work is safe and one delayed cleanup cannot block another experiment. Failures retry after `1s`, `2s`, `4s`, `8s`, `16s`, `32s`, and then `60s` until success or shutdown.

This branch performs no startup or periodic orphan sweep and never enumerates consumers to infer ownership from a matching filter. A consumer or project row orphaned because deletion happened before SM installed its finalizer, or because an administrator removed that finalizer, is not automatically recovered; it may block same-name recreation under `WorkQueuePolicy` and requires explicit operator intervention until a future orphan-cleanup feature is implemented. Normal supported deletion after finalizer installation remains fully automated as defined above.

This branch reacts to terminal experiment phases but does not decide them. A future SM lifecycle feature will own scenario aggregation and the experiment transitions from `InProgress` to `Failed` or `Completed`. The Operator continues to own `Pending -> Provisioning -> InProgress` and provisioning failure to `Error`. The lifecycle gate is independent of transition ownership: regardless of which component set the phase or deletion timestamp, SM admits messaging work only while the current object is live and `InProgress`.

### Translator template boundary

The reference Translator is production-quality integration code, but it is not a universal production Translator. The stable framework owns request consumption and validation, serial delivery, workspace lifecycle, BuildKit sessions, registry authentication, push and digest verification, ready publication, and request acknowledgement. It must not contain model-specific decisions outside the example generator.

The Operator provides the framework configuration as environment variables, following the existing authentication-free mock Translator process. `NATS_URL` identifies the NATS server and must not contain user information. `TRANSLATOR_STREAM`, `TRANSLATOR_REQUEST_SUBJECT`, `TRANSLATOR_READY_SUBJECT_TEMPLATE`, and `TRANSLATOR_CONSUMER` identify the exact alpha4 JetStream stream, request filter, ready-subject template, and UID-specific durable consumer. `SIMULATIONPROJECTNAMESPACE`, `SIMULATIONPROJECTNAME`, and `SIMULATIONEXPERIMENTUID` identify the owning experiment. `REPOSITORY` is the configured target repository and `BASEIMAGE` is the digest-pinned `spec.translator.baseimage` value. The template accepts no NATS username, password, token, NKey, JWT, credentials file, or credential mount. It fails startup with a descriptive configuration error when any required value is missing, malformed, or inconsistent with the alpha4 subject grammar. It creates neither shared stream nor Scenario Manager consumer.

The framework derives the ready subject from the configured template and the validated request scenario ID. It must publish only within its injected namespace and project. The stream, request subject, ready-subject template, and consumer naming are configuration, not model-generator responsibilities.

Translator consumes the request schema already published by SM: integer `id`, integer `translation_attempt`, object-or-null `recipe_info`, and number-or-null `confidence_metric`. SM validates the two positive identity values before using `encoding/json` on that typed struct; no valid SM-published scenario request can therefore lack recoverable identity. Translator decodes one JSON object, requires positive `id` and `translation_attempt`, and checks the documented types for the two optional fields. Unknown fields are ignored and do not extend the supported contract. A delivery that cannot recover valid identity is external or corrupt poison rather than a request produced by the supported SM publisher: Translator emits one sanitized log and ACKs it without a ready message, and tests for this branch publish raw poison without creating a Core DB scenario. If identity is valid but `recipe_info` cannot be handled by the selected generator, that is a generator failure and follows the confirmed empty-image ready workflow for that scenario.

An empty-image outcome is terminal only after its ready publication is confirmed. Generator, BuildKit, registry push, and digest-resolution failures first atomically persist an empty-failure outcome marker, then retry publication of that same empty-image ready message until confirmation and finally server-ACK the request. If final request acknowledgement fails, redelivery reuses the marker and does not repeat generation, build, push, or resolution. NATS transport, progress-acknowledgement, or ready-publication failures leave the request unacknowledged for JetStream redelivery and never create a second failure classification. This is the same ACK/NAK boundary used by SM for ready messages: malformed or permanent poison is ACKed, while transient transport or persistence failure is retried.

For an active `InProgress` experiment admitted by the lifecycle gate, SM handles a confirmed empty-image ready message through the existing exact-attempt policy, now made canonical here. `SCENARIO_MANAGER_TRANS_MAX_ATTEMPTS` is a positive integer with default `3`. After validating project, scenario ID, state `Scheduled`, and matching `translation_attempt`, SM calls `MarkScenarioTranslationAttemptFailed`: attempts below the configured maximum transition that exact row back to `Created`, while an exhausted attempt transitions it to `Failed`. The recovery update does not increment `translation_attempts`; the next successful `Created -> Scheduled` claim increments it. SM ACKs the empty ready only after the guarded database update succeeds and NAKs transient database failures. For an inactive terminal or deleting experiment, SM instead applies the terminal ACK-and-discard rule and does not call this recovery update.

The replaceable Go generator boundary has this conceptual shape; the implementation may place these types in an internal package, but it must preserve this dependency direction:

```go
type Generator interface {
	Generate(context.Context, GenerationInput) error
}

type GenerationInput struct {
	ScenarioID         int
	TranslationAttempt int
	RecipeInfo         json.RawMessage
	ConfidenceMetric   *float64
	BaseImage           string
	Workspace           string
	DetailDatabase      DatabaseConfig
	ResultDatabase      DatabaseConfig
}

type DatabaseConfig struct {
	Host     string
	Port     int
	DBName   string
	User     string
	Password string
}
```

`Generate` writes a complete Docker build context into `Workspace`: generated model files, scenario configuration, dependencies, an entrypoint, and a Dockerfile. It does not consume NATS messages, call BuildKit, push images, publish ready messages, or acknowledge deliveries. Those remain framework responsibilities so a user can replace model generation without reimplementing the handoff protocol.

The intended example remains a small parameterizable SimPy model, but its parameters are stored in the PostgreSQL Scenario Detail Database rather than carried completely in the Translator request. SM continues to send the unchanged `recipe_info` field. For this generator, `recipe_info` must be exactly one JSON object containing one positive integer lookup key and no other fields:

```json
{"parameterset_id": 1}
```

This lookup `parameterset_id` identifies a row in the Scenario Detail Database. It is independent of the positive Core DB scenario ID in the request's top-level `id` field; the generator must not require those two values to be equal and this feature makes no Core DB schema or payload change for the lookup rename. A missing or null `recipe_info`, malformed JSON, a non-object value, a missing, non-integer, zero, or negative `parameterset_id`, or any unknown field is a permanent generator-input failure.

The reference Scenario Detail Database image owns this exact schema-qualified table:

```sql
CREATE TABLE public.simulation_parameters (
    parameterset_id INTEGER PRIMARY KEY CHECK (parameterset_id > 0),
    arrival_rate    INTEGER NOT NULL CHECK (arrival_rate > 0),
    service_rate    INTEGER NOT NULL CHECK (service_rate > 0),
    run_duration    INTEGER NOT NULL CHECK (run_duration > 0),
    seed_policy     INTEGER NOT NULL CHECK (seed_policy >= 0)
);

GRANT USAGE ON SCHEMA public TO CURRENT_USER;
GRANT SELECT ON TABLE public.simulation_parameters TO CURRENT_USER;
```

All five values are PostgreSQL `INTEGER`s. `arrival_rate` is the number of arrivals per SimPy time unit, `service_rate` is the number of services per SimPy time unit, `run_duration` is the number of SimPy time units to execute, and `seed_policy` is non-negative seed material from which each runner derives its own pseudorandom seed. The repository-owned image initialization SQL creates the table and supplies exactly four rows. Their randomly selected values are generated once and checked in as fixed, reproducible image inputs rather than regenerated during image build, container startup, or a smoke run:

```sql
INSERT INTO public.simulation_parameters
    (parameterset_id, arrival_rate, service_rate, run_duration, seed_policy)
VALUES
    (1, 2, 4, 100, 1001),
    (2, 3, 5, 120, 1002),
    (3, 4, 7, 150, 1003),
    (4, 5, 8, 180, 1004);
```

The generator, not `recipe_info`, owns the one predefined parameterized SQL statement:

```sql
SELECT arrival_rate, service_rate, run_duration, seed_policy
FROM public.simulation_parameters
WHERE parameterset_id = $1;
```

The generator passes the validated `recipe_info.parameterset_id` as `$1`; it never executes SQL text received in a message. The query must return exactly one row. No row, more than one row, a `NULL`, a non-integer value, or a value outside the table constraints is a permanent generation failure. The primary key makes duplicate rows invalid schema state, but the adapter still checks cardinality defensively rather than silently selecting one row. All implementation SQL names this table as `public.simulation_parameters`; it must not depend on PostgreSQL `search_path`.

The framework reads the existing `<experiment-name>-detaildb-sct` Secret from `/detaildb-connection`, validates exactly the `host`, `port`, `dbname`, `user`, and `password` files at startup, and passes the resulting `DatabaseConfig` to the generator. This is identical whether the alpha4 `DatabaseSpec` selected an Operator-deployed image or an external `host`; the Operator creates the connection Secret in both cases. Database credentials may be literal CBSE deployment configuration, but the generator source contains no duplicated credential constants and the Scenario Detail DB values are not baked into the generated runner image. The Detail DB role requires `CONNECT`, `USAGE` on schema `public`, and `SELECT` on `public.simulation_parameters`; it requires no write or schema-management privilege. The reference image initialization owns creation and population of its four rows. Detail DB rows are immutable for this feature: no CBSE component updates or deletes them, and parameter snapshots, versions, or reload-after-translation behavior are not defined.

Each lookup attempt uses a 10-second connection timeout and a 30-second statement timeout. Connection refusal, either timeout, a PostgreSQL connection exception (`08`), transaction rollback (`40`), insufficient-resource error (`53`), or administrative shutdown/crash/cannot-connect-now result (`57P01`, `57P02`, or `57P03`) is transient. After the first transient failure, the generator keeps the request active, continues the framework's 30-second JetStream in-progress acknowledgements, waits exactly 30 seconds with context cancellation, reconnects, and executes the same query once more. Success on the second attempt continues generation. A second transient failure becomes a permanent generation failure. Authentication failures (`28`), insufficient privilege (`42501`), missing relation or other schema errors, invalid recipe input, and invalid query results fail immediately without the delayed retry. Shutdown or failed JetStream in-progress acknowledgement cancels the lookup or wait and leaves the request unacknowledged; it does not create an empty-failure outcome.

Once a lookup has permanently failed, the framework treats it like another generator failure: it atomically persists the empty-failure outcome, publishes the existing empty-image ready message with confirmation, and acknowledges the request only through the normal confirmed-outcome workflow. A successful lookup supplies the four validated integers to the example generator, which packages the checked-in SimPy model and writes the parameterized model into the generated build context. The generated Dockerfile uses the required digest-pinned runner smoke base image, copies only the generated runner files and required Result DB runtime connection configuration, sets a numeric non-root user, and defines the model launcher as its entrypoint. It does not reinstall unversioned dependencies. The generated image starts the baked scenario without command, arguments, or environment variables from SM; it obtains its Pod hostname from the container runtime with Python's `socket.gethostname()`.

The example is a single-server queue implemented with one `simpy.Resource(capacity=1)`. A source process samples interarrival times with `rng.expovariate(arrival_rate)` and creates customers only while their sampled arrival time is less than `run_duration`. Each customer records the time spent waiting for the resource, samples its service time with `rng.expovariate(service_rate)`, and increments the completed count only if service finishes by the simulation horizon. The runner executes `env.run(until=run_duration)`; unfinished customers are neither completed nor included in the mean. `mean_wait_time` is the arithmetic mean for completed customers and is `0.0` when no customer completes.

Each runner container constructs a local Python `random.Random` instance. It requires a non-empty value from `socket.gethostname()` and applies this exact derivation; it must not use module-global random state:

```python
hostname = socket.gethostname()
digest = hashlib.sha256(f"{seed_policy}:{hostname}".encode("utf-8")).digest()
effective_seed = int.from_bytes(digest[:8], "big") & 0x7FFF_FFFF_FFFF_FFFF
rng = random.Random(effective_seed)
```

Kubernetes uses the Pod name as each container's runtime hostname, and replacement Pods receive new names, so repetitions and retries receive independently derived seeds without changing the Core DB, Translator payload, or Job completion-index contract. The derivation is stable for the same `seed_policy` and Pod name and provides practical collision resistance, not a formal uniqueness proof.

The example runner writes one JSONB result with `parameterset_id`, `arrival_rate`, `service_rate`, `run_duration`, `seed_policy`, `effective_seed`, `completed_customers`, and `mean_wait_time`. The first five fields reproduce the lookup input and parameters; `effective_seed` is the derived non-negative 63-bit integer, `completed_customers` is a non-negative integer, and `mean_wait_time` is a non-negative JSON number. The Result DB table remains named from the top-level Core DB scenario ID as `scenario_<scenario-id>_results`; `parameterset_id` does not replace that Core DB identity.

At startup, the template reads exactly `host`, `port`, `dbname`, `user`, and `password` from both `/detaildb-connection` and `/resultdb-connection` and reads the registry configuration mounted at `/registry-auth/config.json`. It rejects missing or malformed values before handling requests, requires basic credentials for the configured base-image and target-repository authorities through the same Docker resolver used by the Operator, and passes the two validated `DatabaseConfig` values only to the generator. Translator opens neither database at startup. All alpha4 reference PostgreSQL connections explicitly use `sslmode=disable`; database transport security and related API fields are outside this feature. After each database Service or external endpoint and connection Secret exist, Operator provisioning attempts a connection with a 10-second timeout and `SELECT 1` with a 30-second statement timeout. Invalid connection fields are provisioning errors; a well-formed but refused, timed-out, unavailable, or authentication-failed connection leaves the experiment in `Provisioning` and requeues after five seconds without a fixed attempt limit. Only successful checks for both databases permit `InProgress`, and deletion preempts a check or wait. The template does not watch, reload, or rotate registry or database credentials while running. Secret updates after startup have no defined effect until the Translator Pod restarts.

The example generator bakes the Result DB connection configuration and a replaceable result-sink interface into the runner image; it does not bake the Scenario Detail DB configuration because parameter lookup finishes before the image build. This retains the trusted prototype assumption that the generated image contains Result DB credentials: neither the framework, generator, BuildKit progress handling, nor runner may print those values or expose them in image labels or annotations. Database credentials must not be added to NATS messages, Scenario Status, image labels, or annotations.

The configured Result DB role must have `CONNECT` on the configured database, `USAGE` and `CREATE` on its default schema, and permission to create and write the tables it owns; no superuser privilege is required. The example runner uses a 10-second connection timeout and a 30-second statement timeout and performs no inner retry. It connects to PostgreSQL and, in a short transaction, obtains `pg_advisory_xact_lock(0, <scenario-id>)` before creating the scenario table with `IF NOT EXISTS`; the transaction commits immediately after the schema check. This serializes only concurrent first-use table creation by repetitions of the same scenario and prevents catalog races. The runner then performs its result insert in a separate transaction. A timeout or permission failure exits non-zero and consumes the current Kubernetes Pod attempt. The table name and advisory-lock key are derived only from the already validated positive integer scenario ID:

```sql
CREATE TABLE IF NOT EXISTS scenario_<scenario-id>_results (
    id BIGSERIAL PRIMARY KEY,
    result JSONB NOT NULL
);
```

After a successful simulation, each runner appends the JSONB result object defined above. The sequence supplies unique row IDs but does not claim commit-order semantics. Completion indexes are not written to the Result DB, and results have no repetition identifier or uniqueness constraint. A simulation failure performs no result insert. A Result DB connection, advisory-lock, table-creation, or insert failure makes the runner Pod fail and consumes the shared Job retry budget. Kubernetes may create a replacement Pod while that global budget remains.

The result sink is intentionally at-least-once. If PostgreSQL commits an insert but the runner loses the response, the runner exits unsuccessfully and a replacement Pod may run the simulation and append another row. This trusted prototype accepts that an ambiguous commit can therefore produce more result rows than successful completion indexes. The normal no-failure smoke path still requires exactly `number_of_reps` rows. A user-defined generator may replace the per-scenario schema and sink independently, but it must preserve the generated image's ownership of runner startup and result handling.

The namespace-aware subjects above replace the project-only subject names; the existing request and ready JSON payloads remain unchanged. The template uses the exact JetStream consumer contract above and allows at most one active request and build. A successful digest ready message, or a confirmed empty-image failure ready message, is published with JetStream confirmation before the request receives its server-confirmed acknowledgement. NATS transport or publication failures leave the request unacknowledged and retryable; they do not produce an empty-image result.

The BuildKit image exporter adds these credential-free OCI manifest annotations:

- `experiment.cbse.terministic.de/experiment-uid=<full-UID>`
- `experiment.cbse.terministic.de/scenario-id=<decimal-scenario-id>`
- `experiment.cbse.terministic.de/translation-attempt=<decimal-attempt>`

`ready-outcome.json` is the single credential-free durable outcome format. It contains `outcome` (`success` or `empty_failure`), scenario ID, translation attempt, ready subject, and full experiment UID. A success additionally contains deterministic tag and digest; an empty failure contains an empty image and a short non-sensitive failure class. Translator writes the complete file to a temporary file in the same directory and renames it over the marker before publishing ready. It retains the workspace until ready publication and server-confirmed request acknowledgement both succeed.

After BuildKit confirms a successful push, Translator resolves the digest and verifies the repository and all three annotations, then atomically writes the success outcome. A generator, BuildKit, push, digest-resolution, or tag-collision failure atomically writes the empty-failure outcome before its first ready publication. A publish or final-ack retry validates and reuses the exact marker without another generator, build, push, or registry lookup.

On redelivery or process restart, Translator first validates the marker's outcome, experiment UID, scenario ID, attempt, ready subject, and outcome-specific fields. A valid marker is authoritative for that attempt. If the marker was lost because the Pod was replaced, Translator resolves the deterministic registry tag. It treats the tag as the successful build outcome only when the normalized repository and all three manifest annotations exactly match the current request. A missing or mismatched annotation is a permanent identity collision that creates the empty-failure marker; Translator never adopts or overwrites the tag. A registry not-found result permits generation and build; another resolution failure creates the empty-failure marker.

An empty-failure marker is durable across Translator process or container restart while the Pod-local `emptyDir` survives, but there is deliberately no external empty-failure artifact. If Pod replacement loses that marker, redelivery may repeat generation or another failing operation once; SM's exact-attempt handling makes any resulting ready message stale after the first empty outcome was applied. This bounded duplicate work is accepted and must not change a newer scenario attempt. Successful pushes continue to recover externally from their annotated registry tag.

### Required Translator template documentation

`component-templates/translator/README.md` is a mandatory, detailed integration guide. It is part of the template's supported interface, not optional explanatory material. It must give a user enough information to replace the example generator without changing the framework-owned communication or build protocol.

The guide must document:

- every required environment variable, its source, expected format, and startup validation; the mounted registry, Scenario Detail DB, and Result DB Secret paths; the exact required keys in each Secret; the rule that CBSE database credentials may be literal deployment configuration while third-party credentials remain protected; and the authentication-free NATS boundary, including the prohibition on credential-bearing URLs, credential settings, and NATS credential mounts;
- the namespace-aware request and ready subjects, durable-consumer name and exact consumer settings, unchanged four-field request and two-field ready JSON payloads, lightweight request validation, malformed-request ACK behavior, empty-outcome redelivery behavior, serial delivery, 30-second in-progress acknowledgements, publish-confirmation-before-server-confirmed-acknowledgement rule, and permanent poison-message handling;
- the framework-versus-generator ownership boundary, the `Generator` and `GenerationInput` contract, and how a replacement generator produces a complete build context without consuming NATS, invoking BuildKit, or publishing a ready message;
- the exact `recipe_info` object containing only the positive integer Scenario Detail DB `parameterset_id` lookup key; its independence from the top-level Core DB scenario `id`; the `public.simulation_parameters` schema and four immutable reference-image rows; the schema-qualified predefined parameterized query and exact-one-row rule; the mounted Detail DB credential source for both image- and host-based databases and its read-only role; the two-attempt lookup with one 30-second delayed retry; the transient and permanent PostgreSQL failure classes; and the rule that the generator obtains model parameters through that lookup rather than treating `recipe_info` as SQL or as the complete parameter object;
- the `/workspace` attempt layout, cleanup and redelivery behavior, `/run/buildkit/buildkitd.sock`, UID/GID and `fsGroup` requirements, registry authentication, deterministic tag, framework-owned OCI identity annotations, push, and digest-resolution process;
- success and empty-failure outcome persistence and recovery, including marker validation, same-Pod replay, accepted duplicate work after empty-marker loss, annotation-checked success-tag recovery, collision refusal, and the rule that a ready-publish retry with a retained marker never repeats that attempt's work;
- the self-contained runner-image contract: the image owns its executable, dependencies, entrypoint, simulation behavior, and result handling even when no custom Job template is supplied;
- the exact Job-template allow-list, the default-deny rule for unlisted fields, regular-versus-native-sidecar completion behavior, and how permitted integration fields complement rather than replace the runner image's capabilities;
- the non-root execution contract for the runner and all auxiliary containers;
- how the example generator reads the Scenario Detail DB and Result DB configurations, looks up and validates the four model parameters, embeds only the parameters and Result DB configuration in the generated runner, implements the defined single-server queue, derives a container-specific PRNG seed from `seed_policy` and the runtime Pod hostname, creates `scenario_<scenario-id>_results`, appends the defined JSONB result after each successful repetition, and accepts at-least-once duplicate rows after an ambiguous commit and Pod retry;
- how users replace the example generator and result schema while preserving the framework's required runtime configuration, acknowledgement protocol, and immutable-digest handoff; and
- the explicit trusted-prototype limitation that generated images contain PostgreSQL credentials, plus deferred hardening such as secret-at-runtime delivery, untrusted-build isolation, and build egress controls.

### Runner Job customization contract

`spec.runner.jobTemplate` is optional. It is a typed `batch/v1.JobTemplateSpec`, not raw YAML and not a ConfigMap containing a Job. The built-in template is used when the field is absent. A supplied template must contain exactly one regular container named `runner`; other regular containers and init containers have unique names. The public template API is an allow-list against the repository's pinned `k8s.io/api` v0.33 Kubernetes types. A user-supplied field not listed below must be nil, empty, or the Go zero value and is rejected when it has any other value. An explicit JSON value that decodes to the same Go zero value as an omitted field is treated as omitted. API-server defaults and admission-added values are recovery-time live-object state, not an expansion of the user allow-list; protected live fields must still equal the canonical CBSE contract.

| Object area | Allowed user fields | Required validation and merge behavior |
| --- | --- | --- |
| Job metadata | `metadata.labels`, `metadata.annotations` | Permit user keys except the reserved keys defined below. CBSE merges its required identity labels and contract-hash annotation afterward. |
| Job lifecycle | `spec.activeDeadlineSeconds` | When present, it must be a positive integer. `spec.suspend` is not supported because neither SM nor the runner ServiceAccount has a Job-update path that can resume a suspended Job. |
| Pod metadata | `spec.template.metadata.labels`, `spec.template.metadata.annotations` | Permit user keys except the reserved keys defined below. CBSE merges its required identity labels afterward. |
| Workload composition | `spec.template.spec.containers`, `spec.template.spec.initContainers` | Require exactly one regular `runner` container and unique names across regular and init containers. Additional regular containers and ordinary init containers are supported. `initContainers[*].restartPolicy` may be absent or `Always`; `Always` selects Kubernetes native-sidecar behavior. A regular sidecar must terminate by itself after the runner exits or the Job will not complete. |
| Volumes | `spec.template.spec.volumes` | Permit only `emptyDir`, `configMap`, `secret`, `persistentVolumeClaim`, `downwardAPI`, and `projected` volumes. A projected volume may contain only ConfigMap, Secret, or downward-API sources. Reject `hostPath`, projected service-account tokens, and every other volume source. Volume names must be unique. |
| Image pulling | `spec.template.spec.imagePullSecrets` | Permit additional distinct `LocalObjectReference` names. De-duplicate and sort by name, then retain the experiment registry Secret exactly once; a user cannot remove or replace it. |
| Scheduling | `nodeSelector`, `affinity`, `tolerations`, `topologySpreadConstraints`, `priorityClassName`, `preemptionPolicy`, `runtimeClassName` | Copy these standard constraints after Kubernetes API validation. Reject `nodeName`, `schedulerName`, and `schedulingGates`; this branch uses the cluster's default Kubernetes scheduler and defines no external actor that removes scheduling gates. |
| Pod runtime and networking | `terminationGracePeriodSeconds`, `dnsPolicy`, `dnsConfig`, `hostAliases`, `enableServiceLinks` | A termination grace period must be non-negative. Pod-level `activeDeadlineSeconds`, custom hostname/subdomain, host namespaces, and shared process namespace are not supported. |
| Pod numeric identity | `securityContext.runAsUser`, `runAsGroup`, `fsGroup`, `supplementalGroups`, `fsGroupChangePolicy` | Every supplied UID or GID must be greater than zero. CBSE owns the remaining Pod security fields. |
| Runner container | `env`, `envFrom`, `resources.requests`, `resources.limits`, `ports`, `volumeMounts`, `volumeDevices`, `livenessProbe`, `readinessProbe`, `startupProbe`, `imagePullPolicy`, `terminationMessagePath`, `terminationMessagePolicy` | CBSE owns the `runner` name, image, command, arguments, and executable contract. Resource claims are not supported; ordinary and extended resource requests and limits remain subject to Kubernetes validation. Mount and device paths must be unique according to the Kubernetes API. |
| Auxiliary regular and init containers | The runner-container fields plus `name`, `image`, `command`, `args`, `workingDir`, and `lifecycle` where Kubernetes permits them | Users own auxiliary images and executable behavior. Init-container probes and lifecycle hooks remain invalid under ordinary Kubernetes validation, except probes valid for a native sidecar. All auxiliary containers must satisfy the non-root security contract below. |
| Container numeric identity and filesystem | `securityContext.runAsUser`, `securityContext.runAsGroup`, `securityContext.readOnlyRootFilesystem` | Supplied UID and GID values must be greater than zero. CBSE owns every other container security field. |

SM constructs the effective Job by copying only these allowed values and then applying CBSE-owned fields. Users cannot set or override the Job name, namespace, owner references, metadata finalizers, reserved metadata, `selector`, `manualSelector`, `managedBy`, `ttlSecondsAfterFinished`, `completionMode`, completions, parallelism, `backoffLimitPerIndex`, global `backoffLimit`, `maxFailedIndexes`, `podFailurePolicy`, `podReplacementPolicy`, `successPolicy`, `suspend`, the Pod restart policy, Pod-level active deadline, ServiceAccount fields, token automounting, runner image, runner command or arguments, or required security settings. Also reject ephemeral containers, Pod resource claims or Pod-level resources, readiness gates, scheduling gates, `overhead`, custom `schedulerName`, direct `nodeName`, hostname/subdomain overrides, `hostNetwork`, `hostPID`, `hostIPC`, `hostUsers`, shared process namespace, sysctls, SELinux or Windows security options, custom seccomp or AppArmor profiles, privileged mode, privilege escalation, capability additions, custom process masks, and projected service-account tokens. Every user field not explicitly allowed by the table is prohibited even when it is not named in this summary.

The effective Pod sets `restartPolicy: Never`, `automountServiceAccountToken: false`, `runAsNonRoot: true`, and `RuntimeDefault` seccomp. Every runner, auxiliary regular container, and init container receives `privileged: false`, `allowPrivilegeEscalation: false`, and a capability set that drops `ALL`; user-provided positive UID/GID values and `readOnlyRootFilesystem` are retained. The accepted runner digest always replaces the custom template's `runner` image. The image must work with the built-in template; user customization supplies optional integration and scheduling concerns, not missing executable behavior. The Operator rejects any disallowed field or invalid allowed value and moves the experiment to provisioning `Error`; SM repeats the same validation before any Job create call and treats a conflict that escapes provisioning as a permanent scenario startup failure.

`JobTemplateSpec.metadata.labels` and `.annotations` become user Job metadata; `spec.template.metadata.labels` and `.annotations` remain Pod-template metadata inside the Job specification. Empty user maps and slices are normalized to nil before hashing, set-like name lists such as image pull Secrets are de-duplicated and sorted, and CBSE applies all defaults and protected overrides before normalization. Job or Pod `name`, `generateName`, namespace, owner references, finalizers, reserved metadata keys, status, and server metadata are never accepted from the template. Reserved metadata means every key under `experiment.cbse.terministic.de/` plus Kubernetes Job controller identity keys `controller-uid`, `job-name`, `batch.kubernetes.io/controller-uid`, and `batch.kubernetes.io/job-name`; other user annotations remain permitted integration settings.

SM computes `experiment.cbse.terministic.de/job-contract-sha256` as lowercase hexadecimal SHA-256 over `encoding/json` output from a typed hash-input struct containing finalized client-side Job labels, user Job annotations, and the normalized effective Job specification. Go JSON encoding provides sorted string-map keys; the normalization above removes nil-versus-empty and ordering ambiguity. The hash excludes Job name, namespace, owner references, status, TypeMeta, server metadata, the hash annotation itself, and the API-server-generated Job selector and controller/job-name labels that cannot be known before creation. It represents the client-side desired contract before API admission. The same effective-Job builder and normalization function are used at creation and recovery.

An `AlreadyExists` result is an expected recovery case when Kubernetes accepted Job creation but SM restarted or lost the subsequent Core DB transition. SM accepts the existing Job only when its stored contract-hash annotation equals the newly computed desired hash and its experiment owner UID, identity labels, and normalized protected live fields match. The API-server-generated selector must select exactly the live Job's controller UID, and the corresponding legacy and `batch.kubernetes.io` controller-UID and Job-name labels on the live Pod template must agree with the live Job UID and name; these canonical server-generated values are verified separately rather than hashed. Recovery accepts Kubernetes defaults within allow-listed fields, `schedulerName: default-scheduler`, `priority` derived from an allowed `priorityClassName`, and `overhead` derived from an allowed `runtimeClassName`. Admission-added metadata or values composed entirely from allow-listed fields do not alter the stored desired hash, but they are still checked for reserved metadata, supported volume kinds, non-root security, and every protected field. Any other admission-added field, a noncanonical value for one of the live-only defaults above, or any protected-field mutation is a collision. SM never adopts or rewrites a mismatch.

### Scheduler boundary and RBAC

SM places workload creation and observation behind a small, resource-neutral internal scheduler interface. Core orchestration supplies a validated runner projection and receives successful repetition count, active state, and terminal success or failure without importing `batch/v1` types. This branch provides only the `batch/v1` Kubernetes Job adapter, which owns Job-template merging and Kubernetes-specific identity checks. Alpha4 has no scheduler-provider selection field. A future Volcano or other scheduler adapter must add its own explicit API configuration, template type, resources, third-party integration, and additive RBAC without changing this branch's core state-machine contract or broadening base permissions speculatively.

The default SM ServiceAccount receives only these workload permissions:

- `get`, `list`, `watch`, and `patch` on alpha4 `simulationexperiments`;
- `create`, `get`, `list`, and `watch` on `batch/jobs`;
- `get`, but not `list` or `watch`, on the core Secret named `cbse-registry-auth`, enforced with `resourceNames: ["cbse-registry-auth"]`; and
- `get`, but not `list` or `watch`, on core `serviceaccounts`.

Alpha4 SM uses a ClusterRole and ClusterRoleBinding because it discovers experiments and creates or observes Jobs across namespaces. Kubernetes RBAC cannot restrict a metadata-finalizer patch to one field or restrict `get` to dynamically named runner ServiceAccounts, so cluster-wide experiment `patch` and ServiceAccount `get` without `list` or `watch` are the narrowest standard RBAC boundaries for those resources. The registry Secret name is fixed, so the Secret rule must use `resourceNames: ["cbse-registry-auth"]`; SM must not receive permission to read another Secret. Application code uses experiment patch only for the exact SM finalizer, performs only direct named reads for `cbse-registry-auth` and the deterministic runner ServiceAccount, and never exposes a generic lookup endpoint. SM receives no Pod, ConfigMap, Job update, Job patch, Job delete, experiment-status update, experiment update, experiment delete, or RBAC-management permission. ConfigMap and Secret volumes other than the required registry Secret are resolved by Kubernetes and do not require SM to read those objects. Per-experiment runner ServiceAccounts have no RoleBinding and set `automountServiceAccountToken: false`.

Before starting informers, NATS consumers, runner selection, or observation, SM uses Kubernetes authorization checks to verify every required verb and resource above across namespaces, including experiment patch for finalizer management. A denied startup check is a fatal installation error and prevents startup because the deployment cannot execute its contract. If an operation for an individual scenario later returns HTTP `403 Forbidden`, SM does not terminate the process: it logs the verb, resource, namespace, and scenario, then applies the guarded transition for that scenario to `Failed`. A later `Forbidden` on the process-wide experiment list/watch has no single scenario to fail; SM marks readiness false, keeps liveness true, and retries after `1s`, `2s`, `4s`, `8s`, `16s`, and then `30s` until a successful list/watch resets the delay and readiness. It exits neither the process nor any domain state. Neither path may stop unrelated scenario keys already available to workers.

### Configuration validation and failure classes

The implementation must not use “invalid configuration” as an undifferentiated error. Validation is grouped and classified as follows:

1. **Alpha4 experiment admission and provisioning.** The Kubernetes API server rejects an update that adds, removes, or changes any immutable alpha4 field listed above; the rejected update is never persisted and therefore does not cause an Operator-owned phase transition. For an admitted new object, the Operator rejects an invalid namespace or experiment name; an invalid Detail or Result `DatabaseSpec`; a non-digest Translator, Builder, or base image; a tagged or digested `translator.repository`; a `registryAuthSecretRef.name` other than `cbse-registry-auth`; a referenced Secret that does not yet exist in the experiment namespace; a wrong-type or malformed Docker configuration Secret; a missing or invalid basic-auth entry for a required registry host; invalid Builder request or limit quantities; or a runner Job template containing an unlisted field, an invalid allowed value, a protected-field override, or a non-root violation. The Operator reports the exact field or referenced object and moves provisioning to `Error` before creating experiment components.
2. **Translator startup.** Translator refuses readiness for a missing or malformed NATS URL, a NATS URL containing user information, stream, request subject, ready-subject template, consumer name, experiment identity, repository, or base image; missing or malformed registry, Scenario Detail DB, or Result DB Secret files; a non-numeric or out-of-range database port; or an unwritable workspace. The reference template accepts no NATS credential setting or mount. The error names the setting or mounted path without printing its value when it may contain credentials.
3. **Scenario Manager startup.** SM fails startup for a malformed Core DB DSN, incompatible required table schema, an invalid or credential-bearing NATS URL, a non-empty legacy NATS username or password, invalid NATS subject template, invalid configured stream or consumer name, or a denied required cluster-wide Kubernetes authorization check. It reports the exact setting, schema check, or denied Kubernetes verb/resource before starting informers, consumers, scenario selection, or runner observation.
4. **Runtime dependencies.** A syntactically valid but unavailable NATS server or Kubernetes API is a retryable dependency failure. A registry, BuildKit, generator, push, or digest-resolution failure while handling a Translator request follows the confirmed empty-image workflow. A Result DB failure makes the current runner Pod fail and consumes the shared Job retry budget. A scenario-specific Kubernetes `Forbidden` or terminal Job failure transitions only that scenario to `Failed`; SM continues other work. None of these failures is relabeled as a startup configuration error.
5. **Messages and scenario work.** Invalid subject shape, payload shape, attempt identity, repository, or scenario-specific generator input follows the poison-message or guarded scenario-failure rules defined by the owning workflow. It is not a process startup configuration failure. A lifecycle rejection is classified separately: pre-`InProgress` work is transient and retryable, while `Error`, `Failed`, `Completed`, and deletion are permanent for messaging and use ACK-and-discard without scenario mutation.

### Lightweight scenario observability

Scenario execution uses concise, credential-free logs and Kubernetes' existing Job and Pod diagnostics. SM logs Job creation or adoption and one terminal scenario outcome. Those records include experiment namespace and name, scenario ID, translation attempt, Job name, requested repetitions, computed repetitions, outcome, and a short reason. SM does not log every successful repetition or every unchanged observation poll.

The basic generated runner emits no framework debug stream and no per-simulation-event log. On each runner-controlled failed Pod attempt, it emits one timestamped terminal record containing scenario ID, Pod hostname, failure stage, and a sanitized reason, then exits non-zero. A controlled simulation failure writes no result row; an ambiguous PostgreSQL commit may have written a row before the failure became visible. Kubernetes owns the completion index used for retry accounting, but the Pod hostname and index do not create a repetition identity in the Core DB or Result DB. Failures before the runner starts remain visible through standard Job conditions, Pod status, and Kubernetes events. SM does not gain Pod-read RBAC for logging.

Automatic collection, centralized storage, size limits, retention periods, and deletion of old scenario logs are future operational work. This branch keeps completed and failed Jobs available for inspection and does not implement log garbage collection.

### State boundary

This branch implements this prefix of the scenario diagram:

```text
Created -> Scheduled -> StartingRunners -> InProcessing
                          | startup failure       | Job success
                          v                       v
                       Failed             PostProcessing

InProcessing --Job failure--> Failed
```

`Scheduled -> StartingRunners` remains owned by accepted Translator ready handling. `StartingRunners -> InProcessing` is owned by SM after a matching Job exists. SM then observes that exact Job. Job success owns `InProcessing -> PostProcessing`; Job failure owns `InProcessing -> Failed`. `PostProcessing` is a boundary state in this branch. SM does not invoke the PostProcessingService or move a scenario from `PostProcessing` to `StartingRunners` or `Finished`.

The experiment diagram remains unchanged. The Operator moves an alpha4 experiment through `Pending`, `Provisioning`, and `InProgress` when components are ready, and may set `Error` for a provisioning problem. The intended future owner of execution-level `Failed` and `Completed` transitions is SM. This branch does not implement those two experiment transitions. It observes `Error`, `Failed`, and `Completed` only to close the messaging lifecycle gate; it does not delete or purge NATS artifacts for a terminal CR. Deletion closes the same gate as soon as `deletionTimestamp` is observed and then runs the deletion-time cleanup defined above. No NATS or JetStream request is expected in `Completed`, and SM performs no scenario-work consumption in `Error`, `Failed`, `Completed`, or during deletion.

### Runner-start projection and validation

SM reads one runner-start projection from the Core DB: scenario ID, `number_of_reps`, `number_of_computed_reps`, persisted runner digest, translation attempt, and project namespace and name. It then gets that exact alpha4 experiment from Kubernetes, reads its current UID, and requires phase `InProgress`. It revalidates the runner digest and requires its normalized repository to equal `spec.translator.repository` before creating a Job. The live UID becomes the owner-reference and workload-label identity; it is not loaded from Core DB.

`number_of_reps` must be between `1` and `100000`, and `number_of_computed_reps` must initially be zero for this execution. A larger value is a permanent invalid-repetition failure and no Job create is attempted. The runner digest and template must be valid before Job creation. SM performs direct named `GET` operations for the experiment's registry Secret and deterministic runner ServiceAccount. It revalidates the Secret type, key, Docker JSON, and basic credentials for the generated image's registry authority with the same standard resolver used by the Operator; this catches a Secret replaced after provisioning. The Job includes that Secret reference in `imagePullSecrets`; a custom template may add other distinct pull Secrets but cannot remove or replace it.

The Job and its Pod template must carry these labels:

- `experiment.cbse.terministic.de/project=<experiment-name>`
- `experiment.cbse.terministic.de/experiment-uid=<full-UID>`
- `experiment.cbse.terministic.de/scenario-id=<decimal-scenario-id>`
- `experiment.cbse.terministic.de/translation-attempt=<decimal-attempt>`

The Job also carries `experiment.cbse.terministic.de/job-contract-sha256=<lowercase-SHA-256>` using the effective-contract hashing rules above. Before accepting `AlreadyExists`, SM compares the owner UID, identity labels, contract hash, runner image and executable fields, indexed completion mode, completions, parallelism, absent `backoffLimitPerIndex`, global `backoffLimit: 4`, absent `maxFailedIndexes`, restart policy, required ServiceAccount and image pull Secret, token setting, pod security settings, and every other protected field. A mismatch is a collision and is never adopted.

### Startup result handling

SM creates or confirms the Job before calling the guarded `StartingRunners -> InProcessing` update. If the update affects zero rows, SM logs a stale result and does not change the Job. This is expected when another worker confirmed the same Job and won the transition.

The following are permanent startup failures. SM calls the guarded `StartingRunners -> Failed` transition: invalid repetition state, image, image repository, or runner template; missing or wrong-type pull Secret; missing, deleting, or terminal-phase experiment; a runtime Kubernetes `Forbidden`; and a Job collision.

Ordinary Kubernetes transport failures and the fixed 10-second deadline on each get or create call are retryable without changing scenario state. SM requeues the scenario after `1s`, `2s`, `4s`, `8s`, `16s`, and then `30s` until the operation succeeds or the process shuts down. The delay resets after a successful create or matching `AlreadyExists` and is not persisted across SM restarts. An experiment in an empty, `Pending`, or `Provisioning` phase requeues after five seconds. Retries always target the same deterministic Job and never sleep a worker or block another key.

### Runner terminal observation

Runner observation is independent of selection and translation. SM performs one discovery immediately after startup and every five seconds thereafter, listing all `InProcessing` rows and adding their scenario keys to one de-duplicating, process-local, rate-limited work queue. Four fixed workers reconcile keys concurrently; delayed retries use the queue's `AddAfter` mechanism and never sleep a worker. Every Kubernetes call made for one key has a 10-second deadline. `InProcessing` is not part of the ordinary lowest-ID actionable query. A missing, forbidden, colliding, slow, or failing Job therefore consumes only its own retry state and bounded worker time and cannot prevent discovery or eventual reconciliation of other `InProcessing` scenarios.

The observation projection contains the scenario ID, requested and computed repetition counts, translation attempt, project namespace, project name, and the fields needed to verify the deterministic Job identity. The scheduler adapter gets the current experiment to obtain its live UID, then gets the exact Job and confirms its owner UID, reserved UID label, and protected contract before applying progress or a terminal result. SM does not watch Pods.

The result rules are:

- For every observation, SM parses the Kubernetes compressed-index syntax in `job.status.completedIndexes`, validates that every interval is ordered, non-overlapping, and within `0..number_of_reps-1`, and counts the represented indexes without expanding them into a large slice. It atomically sets `number_of_computed_reps` to `GREATEST(current_value, completed_index_count)` while guarding scenario ID and state `InProcessing`. `completedIndexes`, not the aggregate `status.succeeded`, is the authoritative unique-index set. The value never decreases or exceeds `number_of_reps`, so duplicate Pods for one index, informer redelivery, Pod retry, and SM restart cannot double-count an index. A malformed or out-of-range server-owned index string is treated as a Job identity/status collision and fails only that scenario.
- A failed Pod consumes the Job's global retry budget. Kubernetes creates a replacement Pod while that budget remains. A Job with `Failed=True` first preserves the observed partial successful-index count, then causes a guarded `InProcessing -> Failed` update.
- A Job with Kubernetes `Complete=True` first records `number_of_computed_reps = number_of_reps`, then causes a guarded `InProcessing -> PostProcessing` update.
- A Job without either terminal condition remains `InProcessing` without error.
- A missing Job, per-operation deadline, or ordinary Kubernetes transport failure leaves the row `InProcessing` and requeues only that scenario after `1s`, `2s`, `4s`, `8s`, `16s`, and then `30s` until observation succeeds or the process shuts down. The delay is process-local and resets after a successful observation. This branch does not recreate a Job after the scenario entered `InProcessing`.
- A runtime `Forbidden` while getting the experiment or Job is a permanent failure for that scenario. SM preserves the current successful-index count, applies the guarded `InProcessing -> Failed` transition, and continues processing other scenarios.
- A Job that exists under the deterministic name but does not match the observation projection is a collision. SM applies the guarded `InProcessing -> Failed` transition and does not adopt the Job.
- A stale projection or zero-row guarded update is a successful no-op.

A non-terminal active or pending Job is a successful observation and resets the process-local retry delay. Kubernetes scheduling delay, `Job.status.active > 0`, and unschedulable Pending Pods are not failures; Kubernetes remains responsible for admitting Pods as resources become available.

If an invalid Job reports both terminal conditions, SM treats `Failed=True` as failure and does not move the scenario to `PostProcessing`. `number_of_computed_reps` records Kubernetes-confirmed successful completion indexes; SM does not derive it from Result DB rows. Completed and failed Jobs remain present for inspection.

### Pinned example runtime and PostgreSQL databases

The reference versions are frozen as of 2026-08-18: Python 3.14.6, SimPy 4.1.2, Psycopg 3.3.4, and PostgreSQL 18.6. Python 3.14.6 is the current stable Python release and satisfies SimPy 4.1.2's Python `>=3.8` requirement; PostgreSQL 19 is still beta, so 18.6 is the current stable server release. The runner smoke base image is built from the versioned official `python:3.14.6-slim` source image and preinstalls exactly `simpy==4.1.2` and `psycopg[binary]==3.3.4`. The published base image and its `FROM` image must be locked to full OCI digests when the three deferred runtime-image digests are supplied. Generated runner images inherit those packages and add only the generated model and connection configuration. They do not contain a PostgreSQL server; Psycopg is the client used to reach the experiment's Result DB.

The reference Scenario Detail Database image in `component-templates/scenario-detail-database/` uses the official PostgreSQL 18.6 server image as its digest-pinned `FROM`. Its Dockerfile copies the checked-in initialization SQL to `/docker-entrypoint-initdb.d/10-simulation-parameters.sql`; the standard PostgreSQL first-start initialization creates `public.simulation_parameters`, applies the explicit grants, and inserts the four fixed rows before server readiness. The image and SQL contain no database credentials. The repository test pipeline builds and publishes this image with the other test components and resolves it to an immutable digest.

The alpha4 smoke `SimulationExperiment` uses that repository-built digest for `spec.detailDatabase.image` and the official PostgreSQL 18.6 server digest for `spec.resultDatabase.image`. It removes the existing support-image sleep commands and arguments. The smoke manifest supplies explicit database names, usernames, passwords, port `5432`, and `ClusterIP` services, but the smoke fixture performs no Detail DB DDL or row insertion. The Operator-generated `<experiment-name>-detaildb-sct` and `<experiment-name>-resultdb-sct` Secrets expose their deployed services through the existing `host`, `port`, `dbname`, `user`, and `password` keys. Translator uses the first only for parameter lookup; only the Result DB configuration is baked into the example runner. Outside smoke, either `DatabaseSpec` may instead select `host`; the Operator then creates the same connection Secret from the CR fields without creating a database Deployment or Service.

No Dockerfile, CR, manifest, or test configuration may use `python:latest`, `postgres:latest`, an unversioned dependency, or another floating `latest` tag. The version numbers above are intentionally hardcoded for reproducible implementation and smoke results; adopting newer releases is a later explicit specification update rather than an automatic build-time choice.

### Smoke registry cleanup

The smoke test components and shared runner base are published under `registry.unibw.de/i31bdase/cbse-test`; generated runner images are published only under `registry.unibw.de/i31bdase/cbse-test-runner`. The repositories are initially absent and are created implicitly by their respective first pushes; the cleanup adapter never creates either repository. The rendered alpha4 experiment sets `spec.translator.repository` to the exact runner repository, sets `spec.translator.registryAuthSecretRef.name` to `cbse-registry-auth`, and supplies digest-pinned `spec.translator.image`, `spec.translator.builderImage`, and `spec.translator.baseimage` values from test configuration. The referenced Secret supplies the Docker configuration mounted read-only by Translator and BuildKit. Translator uses the configured runner base to generate scenario-specific images on demand; there is no prebuilt runner smoke image. The full smoke path uses the reference Translator's basic SimPy generator and rootless Builder sidecar, not the synthetic Translator mock.

The cleanup adapter is part of the shell harness and uses the already-required `curl` and `jq`; no separate registry CLI or credential store is introduced. Its API base URL is `https://registry.unibw.de/api/v2.0`, and it authenticates with the protected basic credentials resolved from the Docker configuration. It lists runner artifacts through `GET /projects/i31bdase/repositories/cbse-test-runner/artifacts`, resolves a tag or digest and reads its digest, tags, and OCI annotations through `GET /projects/i31bdase/repositories/cbse-test-runner/artifacts/<reference>`, and deletes a verified artifact only by digest through `DELETE /projects/i31bdase/repositories/cbse-test-runner/artifacts/<digest>`. Listing must follow Harbor pagination until all artifacts are examined. The adapter must not call Harbor project, project-metadata, or whole-repository deletion or update endpoints, and it does not use the OCI Distribution deletion endpoint. Registry cleanup removes Harbor artifact records and tags; Harbor's later physical blob garbage collection is external to this feature.

The harness records the full experiment UID and every exact runner tag and digest observed during the smoke run. The deterministic `runner-<UID-prefix>-s<scenario-id>-a<attempt>` prefix may discover a push that completed before the harness recorded its digest, but prefix match alone never authorizes deletion. Before deleting any discovered or recorded manifest, the cleanup adapter resolves the tag and digest, reads the manifest, and requires all three framework OCI identity annotations to match the full experiment UID, scenario ID, and attempt encoded by that exact expected tag. A missing or mismatched annotation fails cleanup for that candidate without deleting it. Harbor artifact deletion by digest removes every tag attached to that digest, so the adapter must inspect the complete tag set before deletion. Any attached tag other than the exact expected deterministic runner tag makes the candidate unsafe: cleanup fails for that candidate and must not issue its DELETE request. An artifact with no additional tag may be deleted after its identity checks pass. If both the expected tag and recorded digest are already absent when cleanup begins, that candidate is already clean and counts as success without a DELETE request; absence is therefore idempotent. If only one reference is absent while the other still resolves, the adapter must inspect the remaining artifact and apply the same annotation and complete-tag-set checks rather than treating it as absent. After deletion, the adapter requires both the exact tag and digest to be absent. An unconditional exit trap attempts every verified candidate whether smoke succeeds, fails, or retains Kubernetes resources and never targets `cbse-test` or any shared Operator, SM, Builder, base, PostgreSQL, EDS, or Translator image.

Smoke preflight requires the built-in cleanup adapter, a readable protected Docker configuration, the `cbse-registry-auth` source Secret, and the exact component and runner repository names. Before cluster mutation it sends an authenticated Artifact List request for `i31bdase/cbse-test-runner`. HTTP `200` is accepted when the repository already exists; Harbor repository-not-found HTTP `404` is also accepted before the first generated-runner push because `cbse-test-runner` is intentionally not pre-provisioned. HTTP `401`, `403`, or any other result fails preflight. The accepted initial `404` establishes only that the repository is absent, not that Artifact List is authorized. The first generated-runner push must create the repository, after which the harness requires Artifact List to return `200`; this post-push list and the first annotation-verified cleanup are the authoritative verification of Artifact List, Read, and Delete. A denied list, read, or deletion fails cleanup. Preflight also requires the runner smoke base-image digest, its pinned Python 3.14.6 source-image digest, and the PostgreSQL 18.6 Result DB digest. Credential material is never a source-code or specification input and is supplied only through the protected runtime sources defined above. Cleanup attempts every candidate even when one deletion fails. Cleanup failure is written to run artifacts without request headers, tokens, credential values, or credential paths and fails an otherwise successful smoke run; on an already-failed run it is reported in addition to the original failure. The only unresolved external inputs are the runner smoke base-image digest, Python source-image digest, and PostgreSQL Result DB digest. Core API, Operator, SM, and Translator implementation may proceed without inventing those three values, but the full smoke suite and final feature acceptance remain blocked until all three are supplied.

### Verification and shutdown

Test alpha4-only CRD guidance; retained required `baseimage`; the exact `corev1.LocalObjectReference` registry-reference shape; the CPU/memory-only `corev1.ResourceRequirements` shape; 1-to-63-character DNS-label project validation; namespace/name-only project registration and lookup; deletion cascade; and required Core DB schema failure for missing, wrongly typed, obsolete, or extra project columns. Verify that the project table contains only its technical ID, namespace, and name, while `scenario_status` adds only the non-identity `translation_publish_started_at TIMESTAMPTZ NULL` column. Test independently defaulted limits, optional requests, request-not-greater-than-limit validation, and rejection of claims or non-CPU/memory resources. Exercise CRD admission updates that independently change `translator.repository`, `translator.baseimage`, `translator.builderImage`, `translator.builderResources`, and `runner.jobTemplate`; require API-server rejection with the immutable field named, including addition and removal of each optional object. Verify an update that leaves all five fields unchanged can modify otherwise supported metadata or status. Require `registryAuthSecretRef.name` to equal `cbse-registry-auth` and reject an empty or different name. Require that same-named Secret to exist in the experiment namespace before provisioning; test the standard Docker resolver's host-alias behavior, missing references, wrong Secret type, a missing `.dockerconfigjson` key, malformed Docker JSON, missing basic credentials, unsupported token/helper-only credentials, replaced-Secret revalidation by SM without logging credentials or credential-file paths, preservation of the user-owned Secret during experiment deletion, and removal of the copied Secret before a retained smoke namespace is returned. Require the source Secret and `CBSE_REGISTRY_AUTH_FILE` to resolve to the same `registry.unibw.de` username and password; test equivalent differently serialized Docker configurations, a username mismatch, a password mismatch, and secret-safe mismatch diagnostics before mutation. Verify the exact smoke repositories, rejection of repository overrides, absence of both repositories before their first pushes, implicit creation of `cbse-test` by the first component or shared-base push, and implicit creation of `cbse-test-runner` by the first generated-runner push without a repository-creation API call. Test authenticated Harbor Artifact List preflight with both an existing-repository `200` response and the accepted initial repository-not-found `404`, then require a post-push `200`; verify that `401`, `403`, or another preflight result fails without mutating the cluster. Test denied Artifact List, Read, and Delete after the runner repository exists and require the smoke result to fail without attempting a project or whole-repository deletion. Verify that cleanup targets only `cbse-test-runner` even though the robot credential's Artifact Delete capability applies project-wide and can delete artifacts in `cbse-test`. Test that an already-absent expected tag and recorded digest count as successful idempotent cleanup without DELETE, that a remaining reference prevents the absent shortcut, and that any additional tag attached to an otherwise verified digest fails cleanup without DELETE. Test the absent default runner template and at least one valid value from every allow-list row: Job and Pod metadata, active deadline, regular sidecar, ordinary init container, native sidecar, every supported volume source, additional pull Secret, each scheduling field, Pod runtime and DNS fields, Pod numeric identity, runner environment/resources/ports/mounts/devices/probes/pull policy/termination fields, auxiliary executable fields, and allowed container identity/filesystem settings. Verify regular-sidecar and native-sidecar completion behavior and retention of the experiment registry pull Secret. Test the zero-value-as-omitted rule and default-deny behavior for fields newly present in the pinned Kubernetes structs. Reject `suspend`, every prohibited Job policy field, every unsupported Pod control or volume field, projected service-account tokens, resource claims, missing or duplicate `runner` containers, runner executable overrides, reserved metadata, zero or negative identities, and every privileged or otherwise protected security override.

At CRD admission and again in Operator unit tests, exercise `DatabaseSpec` for both Detail and Result databases: accept one valid image form and one valid DNS-name or IP host form; reject neither or both selectors, blank credentials, an invalid port, URL/path/embedded-port hosts, and host forms with `command`, `args`, `nodePort`, or non-`ClusterIP` service type. Verify that the host form produces the five-key connection Secret with the trimmed external host and produces no database Deployment or Service.

Test the exact EDS and Translator subject grammar, rejection of noncanonical configured templates or stream names, cluster-wide wildcard derivation, rejection of lossy or dotted identity, EDS subject/payload mismatch, all-or-nothing rejection of EDS repetition counts outside `1..100000`, exact namespace/name project lookup, Translator-ready identity mismatch, per-experiment durable naming, and isolation of same-named projects in different namespaces. Prove that SM creates or reconciles the two exact shared streams and two exact SM-owned consumers, including update or delete/recreate of old-filter SM durables, preservation or defaulting of unspecified stream fields, and idempotent concurrent matching startup. Verify the authentication-free NATS contract: smoke NATS has no users, accounts, tokens, NKeys, JWTs, credentials files, or subject ACLs; SM and Translator URLs contain no user information; legacy SM username and password values are empty; Translator receives no NATS credential setting, Secret, or mount; and the anonymous client can perform the exact operations required by each workflow. Exact subject construction and ownership checks must still prevent application code from consuming, adopting, purging, or deleting another experiment's artifacts, but tests must not claim broker-enforced isolation. Messages for experiments in at least two namespaces must share those streams and SM queue groups while each Translator consumes only its exact request subject. Existing payload fixtures must continue to decode without namespace or UID fields. Verify the canonical lifecycle gate for every flow: only a live `InProgress` experiment receives `status=ready`, accepts an EDS batch or ready message, or publishes a Translator request; `Pending`, `Provisioning`, and empty phase return an unavailable response or NAK without mutation; and `Error`, `Failed`, `Completed`, and deletion return no ready availability response and ACK-and-discard injected JetStream deliveries without changing scenario state, consuming an attempt, or creating a Job. Assert that after `Completed` no component or SM publishes to any of the experiment's four canonical subjects, while an artificially injected late delivery is defensively discarded.

Test the translation publication boundary independently. A lifecycle rejection before claim creates no attempt. A rejection or transient lifecycle lookup failure after claim but before publish-start invokes no NATS call and `CancelUnpublishedTranslationClaim` restores `Created`, decrements the exact attempt, and treats a zero-row result as stale success. A cancellation database error leaves `Scheduled` for retry. Require `MarkTranslationPublishStarted` to be the only path that permits publication and to succeed for only one concurrent worker. After publish-start, a publish error, lost confirmation, process restart, or lifecycle closure never decrements or reuses the attempt. Stale recovery refunds an old claim only when both publication timestamps are null, applies attempt-consuming recovery when only confirmation is null, and ignores confirmed publication. Verify exact-attempt guards, restart recovery, attempt-limit behavior, and rejection of stale cancellation or publication markers.

Test finalizer insertion and the post-patch re-get before project registration; immediate deletion before finalizer insertion; informer add for an already-terminal experiment; transitions to `Error`, `Failed`, and `Completed`; and CR deletion during execution while SM is unavailable. Verify that each confirmed terminal observation closes only the current UID's lifecycle gate, queues no cleanup, publishes no later Translator request, and leaves its per-experiment consumer, subjects, project row, and scenario rows unchanged. Inject a pending terminal request and verify that any resulting ready delivery is ACKed and discarded without domain processing. Verify that a stale terminal event for an old UID neither closes a live replacement's gate nor triggers cleanup. For CR deletion, verify the gate closes before cleanup is queued, deletion resumes after restart, and the old namespace/name cannot be recreated while its SM finalizer remains. Require the finalizer to be removed only after retrieval and ownership verification of the exact UID consumer, consumer deletion, subject-filtered purges, and project/scenario cascade succeed. Test the four exact consumer metadata values, NATS Server 2.10 minimum, the specified cleanup backoff, concurrent replicas, missing artifacts, and refusal to delete a consumer with a mismatched stream, durable name, filter, full UID, namespace, project, or managed-by marker. Confirm that shared streams, SM consumers, replacement experiments, and another experiment's messages remain unchanged. Assert that no startup or five-minute orphan sweep is registered and document immediate pre-finalizer deletion and administrative finalizer removal as unsupported cases requiring operator cleanup.

Unit-test the template's runtime configuration, exact durable-consumer configuration, typed SM request publication, lightweight decoding of the four established request fields, generator invocation boundary, serial processing, deterministic workspace and tag construction, BuildKit calls, basic Docker-config authentication, digest and OCI identity-annotation validation, 30-second in-progress acknowledgements, confirmed ready publication, and server-confirmed request acknowledgement ordering. Raw poison without usable identity is ACKed without creating a DB scenario; generator failures with usable identity atomically persist and publish one empty-image outcome. Force ready publication and final request acknowledgement failures for both success and empty outcomes and verify redelivery or same-Pod restart reuses the exact marker with zero repeated generator, build, push, or registry calls. Test SM's default-three and configured translation-attempt limit, guarded `Scheduled -> Created` retry, and exhausted `Scheduled -> Failed`. Reject an incompatible existing durable without modifying it. If an in-progress acknowledgement fails, verify cancellation and redelivery. After success-marker loss, verify that a tag with matching repository and annotations is reused and that a missing, mismatched, or unannotated tag creates an empty outcome without rebuilding or overwriting it. After empty-marker loss through Pod replacement, permit one repeated operation and verify its ready message is stale and cannot change a newer attempt. Cover each currently defined startup setting and mounted path and verify Secret changes are not reloaded. Test the exact `public.simulation_parameters` schema, grants, and four reference-image rows; the exact `recipe_info` object and rejection of missing, null, malformed, non-positive, non-integer, or additional fields; independence of lookup `parameterset_id` from the top-level request `id`; schema-qualified parameterized SQL with no message-supplied SQL or `search_path` dependency; zero-, one-, and defensive multiple-row results; integer and constraint validation; the read-only Detail DB role; 10-second connection and 30-second statement timeouts; one cancellable 30-second delayed retry and no third attempt; immediate authentication, authorization, schema, recipe, and result failures; retry of the defined transient SQLSTATE classes; continued in-progress acknowledgements during lookup and delay; and cancellation without an empty outcome on shutdown or progress-ack failure. Verify that a permanent lookup failure follows the confirmed empty-image workflow and that success parameterizes the generated SimPy model and defined JSONB result without baking Detail DB credentials into the runner. Unit-test the exact queue horizon and no-completion behavior, local PRNG use, rejection of an empty runtime hostname, stable seed derivation for one hostname, different effective seeds for different hostnames, and all result fields. Verify Operator provisioning's five-second readiness retry and 10-second connection and 30-second statement timeouts for both databases, required non-superuser privileges, exact-one-of image/host validation, and creation of correctly keyed connection Secrets for both image- and host-based Detail and Result databases without a Deployment or Service for host-based forms. Verify Result DB advisory-lock serialization of concurrent first-use table creation, concurrent inserts, controlled failure with no row, and a possible duplicate after ambiguous commit. Verify shared workspace access as UID/GID `1000` with `fsGroup: 1000`.

Replace the synthetic Translator mock in the mandatory full smoke path with the reference template. The smoke path must demonstrate request consumption; four `recipe_info.parameterset_id` lookup keys resolving the four fixed rows initialized by the repository-built PostgreSQL Scenario Detail Database image; example SimPy context generation from those parameters and the configured digest-pinned runner smoke base image; a rootless sidecar build selected by alpha4; authenticated registry push through the referenced alpha4 registry Secret; immutable digest publication; runner Job creation; non-root model execution; PostgreSQL result persistence; and the scenario transition through `InProcessing` to `PostProcessing`. Assert that the runner base contains Python 3.14.6, SimPy 4.1.2, and Psycopg 3.3.4, that the Detail DB image is based on the digest-pinned PostgreSQL 18.6 server and owns its initialization, and that the Result DB uses the official digest-pinned PostgreSQL 18.6 server with its CR-derived connection Secret; reject floating `latest` tags. For each scenario, configure at least two repetitions, verify they run as distinct completion indexes through one indexed Job, query exactly that many rows from `scenario_<scenario-id>_results` in the no-failure path, verify every result contains the expected `parameterset_id`, lookup parameters, derived `effective_seed`, and result fields, require distinct effective seeds across its normal no-retry repetitions, and verify `number_of_computed_reps = number_of_reps`. Forced-failure cleanup must discover any unrecorded prefix match, verify full OCI identity annotations, and refuse an unannotated or mismatched candidate.

Test the exact Translator sidecar, default and overridden Builder requests and limits, volumes, UID/GID `1000` workspace access, socket, serial request handling, digest publication, empty-image failure handling, repository-mismatch rejection, OCI identity collisions, and request acknowledgement ordering. Verify that Baseline and Restricted admission reject the required BuildKit profiles, that the smoke namespace uses the explicit admission exception, and that all runner-workload containers remain non-root and non-privileged. Test Job naming, metadata merge rules, owner reference, indexed manifest shape, typed normalized contract-hash input, nil/empty normalization, set-like ordering, deterministic JSON, separately verified canonical API-server selector and Job labels, accepted API defaults, admission additions composed only from allow-listed fields, derived priority and RuntimeClass overhead, rejection of every other admission-added field, protected admission collisions, `N` parallel indexed completions, absent `backoffLimitPerIndex`, global `backoffLimit: 4`, absent `maxFailedIndexes`, matching `AlreadyExists`, collision refusal, and Job-before-state ordering. Verify replacement Pods while the global retry budget remains, terminal Job failure when that budget is exhausted across all indexes, termination of remaining work, and preservation of successful-index counts. Test active, partial-success, successful, failed, missing, stale, and contradictory Job observations; compressed `completedIndexes` parsing and unique-index counting; rejection of malformed or out-of-range index status; monotonic repetition updates; redelivery and restart safety; and preservation of partial counts on failure. Confirm that runner observation cannot block ordinary scenario selection. Include a maximum-valid `100000` repetition manifest unit test without creating Pods and reject `100001` and `math.MaxInt32` repetition counts before Job creation.

Test the resource-neutral internal scheduler interface with a fake adapter and the built-in Kubernetes Job adapter. Assert the exact cluster-wide RBAC resources and verbs, including experiment patch for the SM finalizer, `get`-only ServiceAccount access, and `get`-only Secret access constrained by `resourceNames` to `cbse-registry-auth`. Prove that SM is forbidden from getting a differently named Secret. Verify that finalizer code uses only patch, Job owner references set `blockOwnerDeletion: false`, and SM lacks experiment update or delete permission. Denied startup authorization must prevent informers, consumers, selection, and observation from starting. At runtime, simulate Kubernetes `Forbidden` for Job creation and observation and verify only the affected scenario transitions to `Failed` while SM processes another scenario; a process-wide informer `Forbidden` must follow the exact readiness and capped-backoff contract without exit or domain transition. Verify 10-second operation deadlines and process-local retries at `1s`, `2s`, `4s`, `8s`, `16s`, and then `30s`; transient Kubernetes API failures must not change scenario state or exhaust a failure budget, successful reconciliation resets the delay, and SM never recreates an `InProcessing` Job. Verify immediate and five-second `InProcessing` discovery, de-duplication, four-worker concurrency, nonblocking delayed retries, and independent progress when another key is slow or failing. Verify active, Pending, and unschedulable Jobs are successful observations and Kubernetes, not SM, gates Pod creation on resource availability.

Assert the reserved project and experiment-UID labels on every Operator-managed Pod template and on the runner Job and Pod template. Verify that EDS and Translator obtain namespace from Pod `metadata.namespace` and project name and experiment UID from the two reserved labels through the downward API, never from Pod `metadata.name` or `metadata.uid`; that SM reads the current experiment UID from the live CR; that mismatched Job ownership or labels are rejected; and that the finalizer—not a Core DB UID column—separates recreated incarnations.

Test lightweight scenario observability without requiring a logging backend: SM emits one creation or adoption record and one terminal record with the required scenario fields, each runner-controlled failed Pod attempt emits one sanitized failure record, a controlled simulation failure writes no result row, success does not emit one framework log per repetition, and no credential or full recipe payload appears. Admission, pull, and pre-entrypoint failures remain available through Kubernetes diagnostics and are not expected to emit a runner record. Log retention and automatic deletion receive no implementation test in this branch.

The repository test pipeline must import only alpha4 experiment types in active Operator, SM, envtest, compatibility, and smoke paths. Generated CRDs, samples, fixtures, schemes, RBAC, preflight storage-version checks, and smoke assertions use alpha4. Tests assert alpha2 and alpha3 are not served or reconciled and never treat alpha4 as deprecated or transitional. Image publication builds the reference Translator instead of `trans-mock`, builds the reference Scenario Detail Database image from the configured PostgreSQL digest, and supplies the configured Builder digest, runner smoke base-image digest, PostgreSQL digest, and registry Secret. Once all three deferred runtime-image digests are supplied, an unconditional cleanup test proves that annotation-verified generated runner artifacts are removed from `registry.unibw.de/i31bdase/cbse-test-runner` through Harbor's artifact-by-digest API after both a successful smoke run and a forced failure, without calling project or whole-repository deletion APIs and without deleting anything from `registry.unibw.de/i31bdase/cbse-test`.

`make test-fast` must format, vet, compile, and test `component-templates/translator/` in addition to the existing Go modules, and validate the reference Scenario Detail Database Dockerfile and initialization SQL. Run Translator unit tests with the race detector. A Translator or Detail DB validation failure fails the repository-root target.

After implementation, run `make test-fast`. Once all deferred runtime-image inputs listed above have been supplied, run the mandatory `make test-smoke` with an explicit `KUBECONFIG`, immutable test image digests, and the protected registry-auth runtime input. Smoke preflight must require the already-installed alpha4-only CRD and must not delete, migrate, or downgrade the shared CRD. Full feature acceptance remains blocked until the runtime-image inputs are supplied, the smoke suite can run, and it passes. Do not add test artifacts to commits.
