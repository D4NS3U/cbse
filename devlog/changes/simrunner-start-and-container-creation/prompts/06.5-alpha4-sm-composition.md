# Slice 06.5 implementation prompt: Alpha4 Scenario Manager composition

Implement Slice 06.5 only. Leave the worktree reviewable and resumable; do not begin Slice 07 in this run.

## Why this slice exists

Slices 04 through 06 delivered the alpha4 Scenario Manager as a transport-neutral library with no composition entry point; the alpha4 code itself documents that composition belongs to the cutover (e.g. `scenario-manager/internal/alpha4/lifecycle/nats_adapter.go` and `projectstore.go` say they are "constructed in the alpha4 wiring (Slice 07)"). The cutover checkpoint in [`../FEATURE.md`](../FEATURE.md#alpha4-cutover-checkpoint) permits additive alpha4 work before Slice 07. This slice is that additive work, separated from the thin Slice 07 cutover (which only switches `cmd/main.go`, the CRD/scheme, and the smoke manifests) so that neither is rushed.

This slice writes the missing composition layer only. It does **not** switch `cmd/main.go`, the active CRD/scheme, fixtures, or any smoke manifest. Do not re-implement or refactor any package delivered by Slices 01 through 06; consume them as-is.

## Read order and authority

1. First read [`../FEATURE.md`](../FEATURE.md) completely, especially the [alpha4 cutover checkpoint](../FEATURE.md#alpha4-cutover-checkpoint), [Configuration validation and failure classes](../FEATURE.md#configuration-validation-and-failure-classes), [State boundary](../FEATURE.md#state-boundary), and [Identity and lifecycle invariants](../FEATURE.md#identity-and-lifecycle-invariants).
2. Read the repository-root [`AGENTS.md`](../../../../AGENTS.md) completely and obey all applicable test and cluster-safety instructions.
3. Read [`../IMPLEMENTATION_HANDOFF.md`](../IMPLEMENTATION_HANDOFF.md) completely and validate it against the current worktree. Slice 07 Stage 1 (images/harness/docs/`go.work` translator/`eds_intake`) is already landed and green but uncommitted; treat those changes as pre-existing user work, preserve them, and do not reset or reformat them.
4. Read the target contract in [`../slices/06.5-alpha4-sm-composition.md`](../slices/06.5-alpha4-sm-composition.md) completely.
5. Read [`../slices/04-sm-messaging-and-lifecycle.md`](../slices/04-sm-messaging-and-lifecycle.md) and [`../slices/06-runner-job-orchestration.md`](../slices/06-runner-job-orchestration.md) completely: they own the transport-neutral contracts this slice composes. Read the actual alpha4 packages under `scenario-manager/internal/alpha4/` (`communication`, `subject`, `messaging`, `lifecycle`, `persistence`, `registry`, `scheduler`, `jobadapter`, `runnerstart`, `observation`, `config`, `rbac`, `eventlog`, `translatorconfig`) and the alpha3 composition you are mirroring (`scenario-manager/internal/core/scenario_manager.go`, `selector.go`, `simexp_informer.go`).

Treat `FEATURE.md` and the files under `../slices/` as normative and read-only. Report an irreconcilable contradiction instead of changing them.

## Prerequisite and worktree gate

Before editing, inspect `git status --short`, the relevant diffs, and the alpha4 package surfaces. Verify recorded completion evidence for Slices 01 through 06 against the actual code. Confirm that the alpha4 packages listed in the slice contract exist and export the entry points it names (`messaging.ReconcileStreamsAndConsumers`, `lifecycle.AdmitExperiment/DispatchAction/RunTerminalAction/RunCompletedAction/RunDeletionCleanup/EnsureFinalizer/RemoveFinalizer/DBProjectStore/NewNATSDeletionClient`, `persistence.`, `jobadapter.NewAdapter`, `runnerstart.NewScheduler`, `observation.NewScheduler`, `config.RunnerStartWorkers`, `rbac.Verify`, `registry.ValidateDigestImage/RepositoryFromDigest`, `subject.ParseIdentity/ValidateIdent`). If an entry point the slice depends on is missing or its signature differs, stop and report it rather than inventing a substitute. Otherwise update the handoff with the base commit, target `S06.5-M1`, and `in-progress` state.

## Required outcome

Implement milestones `S06.5-M1` through `S06.5-M6` and acceptance groups `S06.5-A1` through `S06.5-A4` from the slice contract. Concretely, add the five new packages and the two additive persistence queries it lists:

- `scenario-manager/internal/alpha4/app/` — `RunScenarioManager(ctx)` startup, composition, and shutdown join, mirroring `internal/core.RunScenarioManager`.
- `scenario-manager/internal/alpha4/informer/` — the cluster-wide alpha4 experiment informer and lifecycle dispatch.
- `scenario-manager/internal/alpha4/natsadapter/` — the four concrete NATS adapters (translation-request publisher, EDS availability responder, EDS batch consumer, translator-ready consumer).
- `scenario-manager/internal/alpha4/ready/` — the translator-ready workflow as a `communication.TranslatorReadyHandler`.
- `scenario-manager/internal/alpha4/selection/` — the alpha4 selection loop, plus the additive `NextCreatedScenarioForTranslation` and `NextStaleUnpublishedTranslationClaim` queries added to `scenario-manager/internal/alpha4/persistence/`.

Compose the existing packages; do not modify their contracts. Implement the exact alpha4 subject grammar, JetStream consumer contract, lifecycle gate, publication boundary, and ACK/NAK/poison semantics from Slice 04. Apply the SM startup failure-class ordering from FEATURE.md. Keep external calls, guarded database transitions, cancellation, and cleanup ordering explicit and independently testable.

The new packages are written under `scenario-manager/internal/alpha4/` as a **temporary** placement only. Alpha4 is the only served version and alpha3 is deleted at the cutover, so the `alpha4/` version prefix has no permanent meaning; Slice 07 moves every `alpha4/` package to its final `internal/` home, merges the composition into `internal/core` and `internal/nats`, deletes the dead alpha3 code, and removes the `alpha4/` directory. Write the code so that the eventual move is a mechanical rename — avoid package-private coupling across the `alpha4/` packages that would block the move, and prefer the exported surfaces already defined by the 04–06 library.

## Scope discipline

This slice is composition only. Do **not**:

- switch `cmd/main.go`, the active CRD/scheme, samples, fixtures, or any smoke manifest;
- change Dockerfiles, `test/e2e/images.lock.env`, the test harness, or registry cleanup;
- re-implement, refactor, or change the contract of any package delivered by Slices 01 through 06;
- implement NATS authentication, experiment-phase aggregation, `PostProcessingService` invocation, any transition out of `PostProcessing`, or a second scheduler;
- own the deferred `S05-D07` or `S06-D07` smoke checks (they remain Slice 07).

If a focused test reveals a genuine bug in an existing 04–06 package, stop and report it rather than silently fixing it out of scope. If you discover that a composition step requires more than wiring (for example a missing domain function that is genuinely part of this slice's owned deliverables), implement the minimal additive code in the owned package and cover it with a focused test.

## Execution and verification rules

- Follow nearby repository patterns. Prefer small, explicit, idempotent behavior over broad refactors. Introduce new abstractions only to clarify a boundary or enable independent testing.
- Preserve user changes. Do not reset destructive state, expose secrets, or weaken tests. Do not create commits.
- Add focused integration tests for the informer (envtest), the NATS adapters (in-process NATS server), the selection loop, and the ready workflow (integration-tagged Core DB tests where a database is required). Run them during development to localize failures.
- Before claiming completion, run repository-root `make test-fast` and confirm `rc=0`. This slice's required tier is `make test-fast`; the mandatory cluster `make test-smoke` for the alpha4 pipeline is owned by Slice 07 per the alpha4 cutover checkpoint and is not a gate for this slice because this slice changes no active manifest. If a fast-tier prerequisite (for example a database for the integration-tagged tests) is unavailable, finish all safe implementation and unit checks, mark the slice `verification-blocked`, and stop without beginning Slice 07.
- Do not deploy directly into `default` or `kube-system`, delete the shared CRD, or delete the `cbse-test-system` namespace.

## Final handoff

Update `IMPLEMENTATION_HANDOFF.md`, then report using the controller's headings: Slice status (classify Slices 01–07 and this slice); Implemented (the composition layer, the five new packages, the two persistence queries, and the focused tests); Files changed (distinguish pre-existing Slice 07 Stage 1 changes); Tests (each command and result, sanitized); Remaining issues (write `none` only if accurate); Worktree (uncommitted state, confirm no commit was created); Resume at (exactly one stable ID — `S07-M1` Stage 2 if this slice is complete, or the first incomplete `S06.5-*` ID otherwise). State explicitly that the active alpha3 binary, CRD, schemes, manifests, and smoke path are unchanged and that Slice 07 Stage 2 is the next step.
