# Slice 06: Runner Job orchestration

## Goal

Construct, submit, recover, observe, and terminate indexed runner Jobs as idempotent projections of durable scenario state.

## Dependencies

[Slice 01](01-alpha4-api-and-crd.md) supplies alpha4 identity, [Slice 02](02-job-template-policy.md) supplies validated templates, and [Slice 04](04-sm-messaging-and-lifecycle.md) supplies lifecycle gating and guarded state transitions.

## Contracts consumed

This slice consumes the root [state boundary](../FEATURE.md#state-boundary), [identity and lifecycle](../FEATURE.md#identity-and-lifecycle-invariants), [successful CREATE](../FEATURE.md#successful-kubernetes-job-create), and [failure-classification](../FEATURE.md#configuration-validation-and-failure-classes) contracts.

## Files and components owned

6. `scenario-manager/internal/core/`, `internal/coredb/`, and `internal/kube/` -- persist the static namespace/name project identity and translation publish-start marker; implement exact-attempt cancellation and split stale-claim recovery; manage the SM cleanup finalizer and terminal-action queue; remove `StartingRunners` from the serial BSSL actionable query; expose the scheduler boundary; implement the basic Kubernetes Job adapter, the bounded ordered runner-start scheduler, verified experiment-wide Job deletion, and a per-scenario deduplicated five-second observation queue; merge Operator-validated runner templates into effective Jobs; treat a successful Kubernetes Job create as authoritative without validating or comparing any returned field; verify exact ownership only before confirming an `AlreadyExists` Job, consuming an observed Job's status, or performing ordinary destructive cleanup; and create or confirm Jobs; monotonically record successful repetitions; bulk-fail unfinished scenarios for `Error` or `Failed`; and apply terminal Job results. SM does not import or invoke the Operator's template validator or a Kubernetes API-server defaulting scheme.

## Required behavior

5. **Simulation Runner startup and terminal observation.**
   - For one accepted Translator image, SM creates or confirms one `batch/v1` Job for the scenario and translation attempt. The Job name is `simrun-<12-char-UID-prefix>-s<scenario-id>-a<attempt>`, using the UID-prefix rule above.
   - Runner-start reconciliation is a short-lived, process-local SM control-plane workflow that validates one scenario and creates or confirms its deterministic Job. A runner-start reconciler is not a Kubernetes Job, Pod, worker node, or unit of cluster compute capacity.
   - The default Job has one container named `runner`. A custom template must contain exactly one `runner` container and may use only the explicit public allow-list below for sidecars, init containers, volumes, mounted configuration, resources, metadata, deadlines, and scheduling constraints. Every user-supplied field outside that allow-list is rejected. SM replaces the `runner` image with the accepted Translator image.
   - The Job is indexed. `spec.completionMode` is `Indexed`, and `spec.completions` and `spec.parallelism` both equal `scenario_status.number_of_reps`. Each completion index represents one homogeneous repetition for Kubernetes retry accounting. The runner does not consume the index, and Core DB and Result DB do not persist it as a repetition identity. All repetitions may run concurrently; a user-defined model and durable result sink must support that concurrency.
   - `number_of_reps` is limited to `1..100000`, the Kubernetes Indexed Job maximum when `parallelism` equals `completions`. This branch adds no lower operational cap, separate maximum parallelism, batching, or throttling. The trusted-input prototype accepts Jobs up to that Kubernetes limit. A future EDS template may impose a smaller policy limit.
   - Capacity and fairness follow ordinary Kubernetes Job and scheduler behavior. SM submits the Job without calculating cluster capacity or waiting for free resources. The Job may have at most `number_of_reps` Pods active because `parallelism` equals that value; the configured Kubernetes scheduler creates or binds Pods only as resources and scheduling constraints permit. Pending or unschedulable Pods leave the scenario `InProcessing` and are not Job-reconciliation failures. CBSE adds no custom capacity queue, admission calculation, or cross-Job scheduling policy.
   - Set `restartPolicy: Never`, global `backoffLimit: 4`, no `backoffLimitPerIndex`, no `maxFailedIndexes`, and no TTL-after-finished field. Kubernetes counts failed Pods across the entire Job against this one retry budget and creates replacement Pods while the budget remains. When the budget is exhausted, Kubernetes marks the Job `Failed` and terminates its remaining work. The Job succeeds only when all requested indexes complete, unless an allowed `activeDeadlineSeconds` terminates it earlier. Completed and failed Jobs remain visible unless later removed by the owning experiment's `Error`, `Failed`, or deletion action.
   - The Job has the exact alpha4 experiment owner reference, with `controller: true` and `blockOwnerDeletion: false`, runs in the experiment namespace, and uses the deterministic ServiceAccount `simrunner-<12-char-UID-prefix>`. The false block-owner-deletion value avoids requiring SM to have delete permission on experiments while preserving normal garbage collection. The Operator creates this ServiceAccount, gives it no RoleBinding, and SM sets `automountServiceAccountToken: false` on the Job Pod. Every Pod template created for an experiment by the Operator or SM carries the reserved project and full experiment-UID labels defined above; this includes internal database, EDS, Translator, post-processing, and runner Pods. The Operator and SM copy both label values from the live `SimulationExperiment`; containers that require the UID read the reserved label through the downward API and need no Pod-read permission.
   - Runner, sidecar, and init-container images must support non-root execution. The effective Pod template uses `RuntimeDefault` seccomp, `runAsNonRoot: true`, disabled privilege escalation, and dropped capabilities. The Operator alone validates the immutable persisted template and normalizes only a private copy before moving the experiment to `InProgress`; it never repairs or mutates user input. SM admits runner work only for a live, non-deleting `InProgress` experiment and treats that template as already validated.
   - SM observes Jobs for scenarios in `InProcessing` and monotonically records successful completion indexes in `scenario_status.number_of_computed_reps`. A successful Job records all requested repetitions before the guarded `InProcessing -> PostProcessing` transition. A failed Job preserves any partial successful count before the guarded `InProcessing -> Failed` transition. This branch does not execute post-processing or perform an ordinary post-processing transition; only the experiment-level `Error` or `Failed` terminal action may move an unfinished `PostProcessing` scenario to `Failed`.

### Scheduler boundary and RBAC

SM places workload creation and observation behind a small, resource-neutral internal scheduler interface. Core orchestration supplies a validated runner projection and receives successful repetition count, active state, and terminal success or failure without importing `batch/v1` types. This branch provides only the `batch/v1` Kubernetes Job adapter, which owns Job-template merging and Kubernetes-specific identity checks. Alpha4 has no scheduler-provider selection field. A future Volcano or other scheduler adapter must add its own explicit API configuration, template type, resources, third-party integration, and additive RBAC without changing this branch's core state-machine contract or broadening base permissions speculatively.

The default SM ServiceAccount receives only these workload permissions:

- `get`, `list`, `watch`, and `patch` on alpha4 `simulationexperiments`;
- `create`, `delete`, `get`, `list`, and `watch` on `batch/jobs`;
- `get`, but not `list` or `watch`, on the core Secret named `cbse-registry-auth`, enforced with `resourceNames: ["cbse-registry-auth"]`; and
- `get`, but not `list` or `watch`, on core `serviceaccounts`.

Alpha4 SM uses a ClusterRole and ClusterRoleBinding because it discovers experiments and creates, observes, or terminally deletes Jobs across namespaces. Kubernetes RBAC cannot restrict a metadata-finalizer patch to one field or restrict `get` to dynamically named runner ServiceAccounts, so cluster-wide experiment `patch` and ServiceAccount `get` without `list` or `watch` are the narrowest standard RBAC boundaries for those resources. The registry Secret name is fixed, so the Secret rule must use `resourceNames: ["cbse-registry-auth"]`; SM must not receive permission to read another Secret. Application code uses experiment patch only for the exact SM finalizer, performs only direct named reads for `cbse-registry-auth` and the deterministic runner ServiceAccount, and never exposes a generic lookup endpoint. Ordinary lifecycle Job deletion is restricted in application code to the exact label-and-owner verification contract above. When SM's own successful create races lifecycle-gate closure, it instead issues a delete for the submitted deterministic namespace/name using only the returned UID as a precondition, without an ownership `GET` or identity check. SM receives no `deletecollection`, Pod, ConfigMap, Job update, Job patch, experiment-status update, experiment update, experiment delete, PriorityClass, RuntimeClass, or RBAC-management permission. ConfigMap and Secret volumes other than the required registry Secret are resolved by Kubernetes and do not require SM to read those objects. Per-experiment runner ServiceAccounts have no RoleBinding and set `automountServiceAccountToken: false`.

Before starting informers, NATS consumers, runner selection, or observation, SM uses Kubernetes authorization checks to verify every required verb and resource above across namespaces, including experiment patch for finalizer management and Job delete for terminal control. A denied startup check is a fatal installation error and prevents startup because the deployment cannot execute its contract. If an operation for an individual scenario later returns HTTP `403 Forbidden`, SM does not terminate the process: it logs the verb, resource, namespace, and scenario, then applies the guarded transition for that scenario to `Failed`. A `Forbidden` during an `Error`, `Failed`, deletion, or create/gate-closure cleanup follows that cleanup's retry contract instead of changing an additional scenario. A later `Forbidden` on the process-wide experiment list/watch has no individual scenario to fail. SM logs the affected resource and operation, keeps the process running, and retries list/watch every five seconds until access succeeds. It performs no domain-state transition and does not stop unrelated scenario keys already available to workers.

### Lightweight scenario observability

Scenario execution uses concise, credential-free logs and Kubernetes' existing Job and Pod diagnostics. SM logs Job creation or adoption and one terminal scenario outcome. Those records include experiment namespace and name, scenario ID, translation attempt, Job name, requested repetitions, computed repetitions, outcome, and a short reason. SM does not log every successful repetition or every unchanged observation poll.

The basic generated runner emits no framework debug stream and no per-simulation-event log. On each runner-controlled failed Pod attempt, it emits one timestamped terminal record containing scenario ID, Pod hostname, failure stage, and a sanitized reason, then exits non-zero. A controlled simulation failure writes no result row; an ambiguous PostgreSQL commit may have written a row before the failure became visible. Kubernetes owns the completion index used for retry accounting, but the Pod hostname and index do not create a repetition identity in the Core DB or Result DB. Failures before the runner starts remain visible through standard Job conditions, Pod status, and Kubernetes events. SM does not gain Pod-read RBAC for logging.

Automatic collection, centralized storage, size limits, retention periods, and deletion of old scenario logs are future operational work. This branch keeps completed and failed Jobs available for inspection while the experiment remains `InProgress` or `Completed`; `Error`, `Failed`, and deletion remove them under the lifecycle contract above. It does not implement time- or age-based log garbage collection.

### Concurrent runner-start reconciliation

Runner start is not processed by the serial BSSL actionable selector. That selector must exclude `StartingRunners` and retain its existing ascending-positive-`scenario_status.id` FIFO behavior for the states it continues to own. The stored `priority` column remains informational and does not affect BSSL selection or runner-start dispatch.

SM starts one runner-start discovery immediately when the runner-start scheduler starts and repeats discovery every five seconds. Each discovery query returns all rows currently in `StartingRunners`, ordered by ascending positive `scenario_status.id`. The process-local scheduler uses the positive scenario-status ID as its de-duplication key. This key is only an internal bookkeeping identity for one scenario reconciliation; it is not a Kubernetes Job, Pod, worker node, or compute-resource reservation. An ID may appear at most once across the scheduler's ready, delayed, and in-flight sets. A process restart intentionally loses that in-memory state and the new process's immediate discovery reconstructs it from the authoritative database rows.

`SCENARIO_MANAGER_RUNNER_START_WORKERS` fixes the number of runner-start reconciler goroutines for the lifetime of the SM process. Its default and validation rules are defined in the startup configuration section above. Whenever a reconciler becomes available, the scheduler dispatches the lowest currently known and eligible positive ID; it does not preempt an in-flight reconciliation if a lower ID is discovered later. Up to the configured number of distinct scenario IDs may be reconciled concurrently. Kubernetes API completion and guarded database-transition order may differ from dispatch order because those workflows run concurrently.

A reconciler loads and validates the fresh runner-start projection, then creates or confirms that scenario attempt's deterministic Job and applies the guarded state transition. A transient failure records a process-local `nextEligibleAt` equal to five seconds after that failed attempt completes, releases the reconciler immediately, and schedules a wake-up without sleeping or occupying a reconciler goroutine. When the delay expires, the ID rejoins the eligible set and again participates in ascending-ID dispatch ahead of any higher eligible ID that has not already been dispatched. There is no persisted retry counter, exponential backoff, or scenario-failure limit. Other eligible IDs continue to use free reconcilers while a low ID is delayed or while another reconciliation is slow.

The scheduler removes an ID after a successful guarded `StartingRunners -> InProcessing` transition, a permanent startup failure, a stale projection or state, or closure of the owning experiment's lifecycle gate. A zero-row guarded update is stale success. Gate closure cancels matching ready, delayed, and in-flight work; every in-flight reconciler still performs the live experiment and gate checks required below. SM shutdown stops discovery, cancels all delayed and in-flight work, and joins every runner-start reconciler before the process exits.

Multiple SM replicas may discover and reconcile the same database ID. Cross-process correctness does not depend on the process-local de-duplication state: every replica uses the same deterministic Job name, accepts `AlreadyExists` only after the exact ownership verification below, and uses guarded database transitions. At most one guarded transition wins; matching work in another replica converges as stale success.

The worker setting limits only simultaneous SM runner-start validation and Kubernetes API `GET`/`CREATE` workflows. It does not limit the total number of scenario Jobs SM may submit, the number of active Jobs, Job `parallelism`, Pods, Kubernetes worker nodes, or cluster CPU and memory use. A runner-start slot is released once the Job has been created or confirmed and the workflow reaches one of the removal or retry outcomes above. Kubernetes Job controllers and the Kubernetes scheduler exclusively own Pod creation, retry, placement, and resource admission; Pending or unschedulable Pods neither retain a runner-start reconciler nor cause an SM capacity decision.

### Runner-start projection and validation

SM reads one runner-start projection from the Core DB: scenario ID, `number_of_reps`, `number_of_computed_reps`, persisted runner digest, translation attempt, and project namespace and name. It then gets that exact alpha4 experiment from Kubernetes, reads its current UID, and requires phase `InProgress`. It revalidates the runner digest and requires its normalized repository to equal `spec.translator.repository` before creating a Job. The live UID becomes the owner-reference and workload-label identity; it is not loaded from Core DB.

`number_of_reps` must be between `1` and `100000`, and `number_of_computed_reps` must initially be zero for this execution. A larger value is a permanent invalid-repetition failure and no Job create is attempted. The live `InProgress` phase and immutable template establish that the Operator accepted `spec.runner.jobTemplate`; SM does not invoke a template-policy validator. SM performs direct named `GET` operations for the experiment's registry Secret and deterministic runner ServiceAccount. It revalidates the Secret type, key, Docker JSON, and basic credentials for the generated image's registry authority with the same standard resolver used by the Operator; this catches a Secret replaced after provisioning. The Job includes that Secret reference in `imagePullSecrets`; a custom template may add other distinct pull Secrets but cannot remove or replace it. If SM unexpectedly cannot construct the effective Job from an accepted template, it treats that internal contract violation as a permanent scenario startup failure and makes no Job create call; this defensive error path is not a second template-policy validation pass.

The Job and its Pod template must carry these labels:

- `experiment.cbse.terministic.de/project=<experiment-name>`
- `experiment.cbse.terministic.de/experiment-uid=<full-UID>`
- `experiment.cbse.terministic.de/scenario-id=<decimal-scenario-id>`
- `experiment.cbse.terministic.de/translation-attempt=<decimal-attempt>`

The four values above are the reserved Job-label identity used with the deterministic namespace/name and exact controller owner reference for `AlreadyExists`, observation, and destructive cleanup.

### Startup result handling

The authoritative successful-create behavior is defined by the root [Successful Kubernetes Job CREATE](../FEATURE.md#successful-kubernetes-job-create) contract.

The following are permanent startup failures. SM calls the guarded `StartingRunners -> Failed` transition: invalid repetition state, image, or image repository; an unexpected effective-Job construction failure; missing or wrong-type pull Secret; a missing experiment outside the supported finalizer lifecycle; a runtime Kubernetes `Forbidden`; and an `AlreadyExists` ownership collision. An `Error`, `Failed`, `Completed`, or deleting experiment is not an individual startup failure: the worker makes no state transition or Job create, because the experiment phase action owns cancellation and any bulk scenario update. If a successful create crossed gate closure before the guarded `StartingRunners -> InProcessing` update, the worker deletes at the submitted deterministic namespace/name using the process-local returned UID as a delete precondition, without an ownership `GET` or identity check, and follows the terminal cleanup retry cadence instead of leaving the stale Job behind.

Ordinary Kubernetes transport failures and the fixed 10-second deadline on each get or create call are retryable without changing scenario state. An experiment in an empty, `Pending`, or `Provisioning` phase is also retryable. In each case, the concurrent runner-start scheduler applies the fixed five-second delayed-eligibility contract above. Retries always target the same deterministic Job.

### Runner terminal observation

Runner observation is independent of selection and translation. SM performs one discovery immediately after startup and then a discovery tick every five seconds. Each tick lists all `InProcessing` rows and adds their scenario keys to one de-duplicating, process-local work queue. Four fixed workers reconcile keys concurrently. A small process-local keyed state records whether a key is queued or in flight and the first discovery tick at which it is eligible again: a tick never adds a queued or in-flight key, and after an observation completes the key is eligible only at the first strictly subsequent five-second tick. Thus every non-terminal observation, including one following a transport failure, occurs in these five-second slices; observation uses no `AddAfter`, rate limiter, or per-scenario exponential backoff. The keyed state is intentionally lost on SM restart, after which the immediate startup discovery safely resumes observation. Every Kubernetes call made for one key has a 10-second deadline. `InProcessing` is not part of the ordinary lowest-ID actionable query. A missing, forbidden, colliding, slow, or failing Job therefore consumes only its own bounded worker time and cannot prevent discovery or eventual reconciliation of other `InProcessing` scenarios.

The observation projection contains the scenario ID, requested and computed repetition counts, translation attempt, project namespace, and project name. The scheduler adapter gets the current experiment to obtain its live UID and phase. Before using a named Job's status, it requires the exact namespace, deterministic name, controller owner reference, and four reserved Job-label values defined above. It does not compare Job specification or non-identity metadata, so admission-added or admission-mutated values do not affect observation. An ownership mismatch fails only that scenario and leaves the mismatching Job untouched. `Pending`, `Provisioning`, and empty phase requeue without scenario mutation. `Error`, `Failed`, `Completed`, and deletion cause a successful observation no-op because the experiment phase action owns any cancellation or bulk state change. SM does not watch Pods.

The result rules are:

- For every observation, SM parses the Kubernetes compressed-index syntax in `job.status.completedIndexes`, validates that every interval is ordered, non-overlapping, and within `0..number_of_reps-1`, and counts the represented indexes without expanding them into a large slice. It atomically sets `number_of_computed_reps` to `GREATEST(current_value, completed_index_count)` while guarding scenario ID and state `InProcessing`. `completedIndexes`, not the aggregate `status.succeeded`, is the authoritative unique-index set. The value never decreases or exceeds `number_of_reps`, so duplicate Pods for one index, informer redelivery, Pod retry, and SM restart cannot double-count an index. A malformed or out-of-range server-owned index string is invalid Job status and fails only that scenario.
- A failed Pod consumes the Job's global retry budget. Kubernetes creates a replacement Pod while that budget remains. A Job with `Failed=True` first preserves the observed partial successful-index count, then causes a guarded `InProcessing -> Failed` update.
- A Job with Kubernetes `Complete=True` first records `number_of_computed_reps = number_of_reps`, then causes a guarded `InProcessing -> PostProcessing` update.
- A Job without either terminal condition remains `InProcessing` without error.
- A missing Job, per-operation deadline, or ordinary Kubernetes transport failure leaves the row `InProcessing` without a scenario-state change. It is observed again only through the first strictly subsequent five-second discovery tick. This cadence is process-local and restarts with the immediate discovery after an SM restart. This branch does not recreate a Job after the scenario entered `InProcessing`.
- A runtime `Forbidden` while getting the experiment or Job is a permanent failure for that scenario. SM preserves the current successful-index count, applies the guarded `InProcessing -> Failed` transition, and continues processing other scenarios.
- A Job that exists under the deterministic name but does not match the observation projection is a collision. SM applies the guarded `InProcessing -> Failed` transition and does not adopt the Job.
- A stale projection or zero-row guarded update is a successful no-op.

A non-terminal active or pending Job for a live `InProgress` experiment is a successful observation and remains eligible only for the first strictly subsequent five-second discovery tick. Kubernetes scheduling delay, `Job.status.active > 0`, and unschedulable Pending Pods are not failures; Kubernetes remains responsible for admitting Pods as resources become available.

If an invalid Job reports both terminal conditions, SM treats `Failed=True` as failure and does not move the scenario to `PostProcessing`. `number_of_computed_reps` records Kubernetes-confirmed successful completion indexes; SM does not derive it from Result DB rows. Completed and failed Jobs remain present for inspection unless the owning experiment later enters `Error` or `Failed`, or is deleted, in which case the experiment phase action removes them.

## Failure and retry behavior

Transient API errors return a scenario to fixed-delay eligibility; permanent projection, authorization, or ownership failures affect only that scenario. Lifecycle closure and deletion are owned by [Slice 04](04-sm-messaging-and-lifecycle.md).

## Security boundaries

Effective Jobs preserve protected fields, non-root execution, exact ownership identity, constrained Secret and ServiceAccount reads, and the root successful-create trust boundary.

## Acceptance tests

Test the exact Translator sidecar, default and overridden Builder requests and limits, volumes, UID/GID `1000` workspace access, socket, serial request handling, digest publication, empty-image failure handling, repository-mismatch rejection, OCI identity collisions, and request acknowledgement ordering. Verify that Baseline and Restricted admission reject the required BuildKit profiles, that the smoke namespace uses the explicit admission exception, and that all runner-workload containers remain non-root and non-privileged. Test Job naming, metadata merge rules, owner reference, indexed manifest shape, nil/empty normalization, set-like image-pull-Secret ordering, and defined ordering for other lists. Test `N` parallel indexed completions, absent `backoffLimitPerIndex`, global `backoffLimit: 4`, absent `maxFailedIndexes`, and Job-before-state ordering. Verify replacement Pods while the global retry budget remains, terminal Job failure when that budget is exhausted across all indexes, termination of remaining work, and preservation of successful-index counts. Test active, partial-success, successful, failed, missing, stale, and contradictory Job observations; compressed `completedIndexes` parsing and unique-index counting; rejection of malformed or out-of-range index status; monotonic repetition updates; redelivery and restart safety; and preservation of partial counts on failure. Confirm that runner observation cannot block ordinary scenario selection. Include a maximum-valid `100000` repetition manifest unit test without creating Pods and reject `100001` and `math.MaxInt32` repetition counts before Job creation.

Exercise successful-create, `AlreadyExists`, and `InProcessing` observation independently. A successful create must permit `StartingRunners -> InProcessing` without invoking the ownership verifier or validating or comparing any field in the returned Job. Fixtures in which the returned body changes namespace, name, owner references, reserved labels, defaults, sidecars, volumes, resources, non-identity metadata, or any other specification value remain successful; the only returned field retained by the workflow is the UID for possible gate-race cleanup. Identity-mutated fixtures prove control-flow independence and do not make such admission behavior a supported deployment configuration. An `AlreadyExists` Job with the expected namespace, deterministic name, exact alpha4 controller owner reference, and exact project, full experiment-UID, scenario-ID, and translation-attempt Job labels is accepted even when its specification or non-identity metadata differs from the submitted Job. A wrong owner UID or wrong or missing reserved identity label is a collision; retain that Job and fail the scenario without adopting, rewriting, or deleting it.

For observation, require the same ownership identity before reading Job status. Accept specification and non-identity metadata mutations, but fail only the affected scenario and leave the Job untouched when ownership mismatches. Terminal `Error`, `Failed`, and deletion cleanup continues to require label-and-owner verification. Force a successful create to cross lifecycle-gate closure and require deletion at the submitted deterministic namespace/name using the returned UID precondition without an ownership read or check; verify the cleanup retry cadence, including after `Forbidden`.

Test runner-start reconciliation independently. Verify that an absent `SCENARIO_MANAGER_RUNNER_START_WORKERS` resolves to `4`; `1`, `4`, and `64` are accepted; and malformed text, `0`, a negative value, and every value above `64` fail SM startup before scenario selection or runner-start discovery begins. Assert that the BSSL actionable query excludes `StartingRunners` while retaining ascending-positive-ID FIFO for the states it continues to own. Verify immediate discovery followed by five-second discovery of every `StartingRunners` row in ascending positive-ID order, and process-local de-duplication across ready, delayed, and in-flight sets. With four reconcilers, require the lowest currently eligible IDs to be dispatched first while allowing Kubernetes API calls and state transitions to complete out of order. Make one reconciliation slow and another transiently fail, then prove later IDs still have Jobs submitted. The failed low ID must not become eligible earlier than five seconds after its attempt completes; when eligible, it must regain its ascending-ID position without occupying a reconciler during the delay. Verify removal after success, permanent failure, stale state, and lifecycle closure; immediate rediscovery after process restart; cancellation of queued, delayed, and in-flight work; and joining of every reconciler during shutdown.

Run the same runner-start cases with concurrent SM replicas. Require matching deterministic `AlreadyExists` verification and guarded transitions to converge without duplicate state effects. Submit more `StartingRunners` scenarios than the configured worker count and prove that all Jobs are eventually created while no process runs more than its configured number of simultaneous runner-start API workflows. After Job submission, keep Pods Pending or unschedulable and prove that they consume no runner-start reconciler, impose no active-Job cap, and trigger no SM capacity admission or placement decision.

Test the resource-neutral internal scheduler interface with a fake adapter and the built-in Kubernetes Job adapter. Assert the exact cluster-wide RBAC resources and verbs, including experiment patch for the SM finalizer, Job `delete` without `deletecollection`, `get`-only ServiceAccount access, and `get`-only Secret access constrained by `resourceNames` to `cbse-registry-auth`. Prove that SM is forbidden from getting a differently named Secret. Verify that finalizer code uses only experiment patch, Job owner references set `blockOwnerDeletion: false`, and SM lacks Job update/patch and experiment status/update/delete permission. Denied startup authorization, including denied Job delete, must prevent informers, consumers, selection, and observation from starting. At runtime, simulate Kubernetes `Forbidden` for Job creation and observation and verify only the affected scenario transitions to `Failed` while SM processes another scenario; simulate `Forbidden` during terminal Job deletion and verify the gate remains closed, cleanup retries, unrelated projects continue, and a deleting CR retains its finalizer. Simulate a process-wide experiment list/watch `Forbidden` and verify that SM does not exit or change domain state, logs the affected operation, retries every five seconds, and continues processing unrelated scenario keys already available to workers. Verify that successful list/watch recovery resumes normal informer processing. Verify 10-second operation deadlines and fixed five-second process-local runner-start retries; transient runner-start API failures must not change scenario state or exhaust a failure budget, and SM never recreates an `InProcessing` Job. Verify one immediate and then five-second `InProcessing` discovery slices, de-duplication of queued and in-flight keys, four-worker concurrency, no observation before the first strictly subsequent tick after the preceding observation completes, and independent progress when another key is slow or failing. Verify that missing Jobs and ordinary observation transport failures leave the scenario unchanged for the next five-second slice, while active, Pending, and unschedulable Jobs are successful observations and Kubernetes, not SM, gates Pod creation on resource availability.

Assert the reserved project and experiment-UID labels on every Operator-managed Pod template and on the runner Job and Pod template. Verify that EDS and Translator obtain namespace from Pod `metadata.namespace` and project name and experiment UID from the two reserved labels through the downward API, never from Pod `metadata.name` or `metadata.uid`; that SM reads the current experiment UID from the live CR; that mismatched Job ownership or labels are rejected; and that the finalizer—not a Core DB UID column—separates recreated incarnations.

Test lightweight scenario observability without requiring a logging backend: SM emits one creation or adoption record and one terminal record with the required scenario fields, each runner-controlled failed Pod attempt emits one sanitized failure record, a controlled simulation failure writes no result row, success does not emit one framework log per repetition, and no credential or full recipe payload appears. Admission, pull, and pre-entrypoint failures remain available through Kubernetes diagnostics and are not expected to emit a runner record. Log retention and automatic deletion receive no implementation test in this branch.

## Test tier

Implementation changes in this slice require `make test-fast` and `make test-smoke` because they change Scenario Manager reconciliation and Kubernetes Job integration.

## Out of scope

Post-processing, workload self-healing beyond the Job retry budget, custom capacity admission, and additional scheduler implementations remain excluded by the root [Out of Scope](../FEATURE.md#out-of-scope).

## Completion and handoff

Concurrent ordered runner-start, deterministic create or adoption, gate-race cleanup, indexed observation, monotonic repetition accounting, terminal transitions, and verified deletion pass under retries and concurrent replicas.
