---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: simulationexperiments.experiment.cbse.terministic.de
spec:
  group: experiment.cbse.terministic.de
  names:
    kind: SimulationExperiment
    listKind: SimulationExperimentList
    plural: simulationexperiments
    shortNames:
    - simexp
    singular: simulationexperiment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.metrics.scenarioCount
      name: Scenarios
      priority: 1
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: alpha1
    schema:
      openAPIV3Schema:
        description: SimulationExperiment is the Schema for the simulationexperiments
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              detailDatabase:
                description: |-
                  DatabaseSpec model either a containerized or external database
                  Either Image or Host must be specified, not both
                  Note: Xor requires custom validation logic; for CRD we enforce in controller
                properties:
                  args:
                    default: []
                    description: Args to pass to the container
                    items:
                      type: string
                    type: array
                  command:
                    default: []
                    description: Command to override the container's entrypoint
                    items:
                      type: string
                    type: array
                  dbname:
                    type: string
                  host:
                    type: string
                  image:
                    description: Either Image or Host have to specified, not both
                    type: string
                  password:
                    type: string
                  port:
                    format: int32
                    type: integer
                  user:
                    type: string
                required:
                - dbname
                - password
                - port
                - user
                type: object
              experimentalDesign:
                type: string
              postProcessingService:
                description: PostProcessingSpec defines the service used for evaluation
                  after simulation
                properties:
                  args:
                    default: []
                    description: Args to pass to the container
                    items:
                      type: string
                    type: array
                  command:
                    default: []
                    description: Command to override the container's entrypoint
                    items:
                      type: string
                    type: array
                  image:
                    type: string
                required:
                - image
                type: object
              resultDatabase:
                description: |-
                  DatabaseSpec model either a containerized or external database
                  Either Image or Host must be specified, not both
                  Note: Xor requires custom validation logic; for CRD we enforce in controller
                properties:
                  args:
                    default: []
                    description: Args to pass to the container
                    items:
                      type: string
                    type: array
                  command:
                    default: []
                    description: Command to override the container's entrypoint
                    items:
                      type: string
                    type: array
                  dbname:
                    type: string
                  host:
                    type: string
                  image:
                    description: Either Image or Host have to specified, not both
                    type: string
                  password:
                    type: string
                  port:
                    format: int32
                    type: integer
                  user:
                    type: string
                required:
                - dbname
                - password
                - port
                - user
                type: object
              translator:
                description: TranslatorSpec defines the container that performs translation
                  from scenario data to executable simulation models
                properties:
                  args:
                    default: []
                    description: Args to pass to the container
                    items:
                      type: string
                    type: array
                  baseimage:
                    type: string
                  command:
                    default: []
                    description: Command to override the container's entrypoint
                    items:
                      type: string
                    type: array
                  image:
                    type: string
                  repository:
                    type: string
                required:
                - baseimage
                - image
                - repository
                type: object
            required:
            - detailDatabase
            - experimentalDesign
            - postProcessingService
            - resultDatabase
            - translator
            type: object
          status:
            description: SimulationExperimentStatus defines the observed state of
              SimulationExperiment.
            properties:
              message:
                type: string
              metrics:
                properties:
                  scenarioCount:
                    format: int64
                    minimum: 0
                    type: integer
                type: object
              phase:
                enum:
                - Pending
                - Provisioning
                - InProgress
                - Completed
                - Failed
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
